import type { TheoryArticle } from '@/engine/types'

export const lifecycle: TheoryArticle = { id:'th-lifecycle', topic:'React', title:'Жизненный цикл компонента: от монтирования до размонтирования',
  lead:'Fiber, три фазы — рендер, коммит, эффекты — точный порядок внутри коммита и как это соотносится со старыми методами классовых компонентов.',
  body:`
<h5>Три стадии жизни компонента</h5>
<table>
<tr><th>Стадия</th><th>Когда</th><th>Что происходит</th></tr>
<tr><td><b>Mount</b></td><td>первое появление в дереве</td><td>создаётся Fiber, инициализируется состояние, эффекты запускаются впервые</td></tr>
<tr><td><b>Update</b></td><td>любая из 4 причин ререндера (см. статью «Как React рендерит»)</td><td>компонент вызывается заново, React сравнивает результат со старым</td></tr>
<tr><td><b>Unmount</b></td><td>элемент исчез из дерева на этом месте</td><td>cleanup всех эффектов, Fiber и связанные DOM-узлы удаляются</td></tr>
</table>
<p>В статье «Как React рендерит и почему приложение тормозит» разобран путь <code class="i">setState → render → reconciliation → commit</code> на уровне «что вообще происходит». Здесь — на уровень глубже: что такое Fiber, из каких под-фаз состоит коммит и в каком порядке срабатывают эффекты по всему дереву.</p>

<h5>Fiber: единица работы, а не просто «виртуальный DOM»</h5>
<p>Каждому компоненту в дереве соответствует объект-<b>Fiber</b>: тип компонента, текущие пропсы, состояние, связный список хуков (см. статью про хуки), указатели на первого ребёнка, следующего соседа и родителя, и флаги — что именно сделать с этим узлом при коммите (вставить, обновить, удалить).</p>
<p>React держит <b>два дерева Fiber одновременно</b> — приём называется <b>double buffering</b>, как в графике: <code class="i">current</code> — то, что сейчас на экране, и <code class="i">work-in-progress</code> — то, что строится в фоне на основе нового состояния. Когда работа над WIP-деревом завершена, React мгновенно переключает указатель <code class="i">current</code> на него — это и есть момент коммита. Старое дерево не выбрасывается, а становится будущим WIP-деревом для следующего обновления: буферы переиспользуются.</p>
<div class="key">Именно из-за двух деревьев render phase можно безопасно прерывать и выбрасывать: вы строите WIP-копию, а экран всё ещё показывает <code class="i">current</code>. Ничего не сломается, даже если рендер отменят на середине работы.</div>

<h5>Render phase изнутри: work loop</h5>
<p>React проходит по дереву Fiber в два прохода на узел — <b>begin work</b> (спуск: вызвать функцию компонента, получить детей) и <b>complete work</b> (подъём: собрать список изменений для этого узла). Это обычный обход дерева в глубину. При конкурентном рендеринге (React 18+) этот обход может прерываться между узлами — React возвращает управление браузеру, чтобы обработать что-то срочное, и затем продолжает с того же места. Единица прерывания — один Fiber, а не компонент целиком: рендер одного тяжёлого компонента прервать нельзя, а вот переход к следующему соседу в дереве — можно.</p>
<p>Приоритет обновления определяет, в какую <b>лейну</b> (lane) оно попадает: ввод пользователя — самый высокий приоритет, обычный <code class="i">setState</code> — синхронный/default, обновления внутри <code class="i">startTransition</code> — низкий. React в первую очередь доводит до конца высокоприоритетные лейны, откладывая низкоприоритетные — это тот же механизм, что в статье про рендер описан как «конкурентность», здесь — как именно он устроен изнутри.</p>
<div class="note">Render phase обязана быть <b>чистой</b>: без мутаций внешнего состояния, без побочных эффектов. Причина не только в стиле — фаза может выполниться дважды (при прерывании и повторном проходе, а в dev-режиме под StrictMode ещё и намеренно) и обязана оба раза дать одинаковый результат.</div>

<h5>Commit phase: три под-фазы подряд, синхронно и без прерываний</h5>
<pre class="code">before mutation → mutation → layout → (браузер красит пиксели) → passive effects</pre>
<table>
<tr><th>Под-фаза</th><th>Что происходит</th><th>Классовый аналог</th></tr>
<tr><td><b>before mutation</b></td><td>снимок DOM до изменений — например, текущая позиция скролла</td><td><code class="i">getSnapshotBeforeUpdate</code></td></tr>
<tr><td><b>mutation</b></td><td>реальные изменения DOM: вставка/обновление/удаление узлов; старые <code class="i">ref</code> отвязываются (<code class="i">ref.current = null</code>)</td><td>—</td></tr>
<tr><td><b>layout</b></td><td>новые <code class="i">ref</code> привязываются; синхронно, ДО отрисовки браузером выполняются cleanup и сами <code class="i">useLayoutEffect</code></td><td><code class="i">componentDidMount</code> / <code class="i">componentDidUpdate</code></td></tr>
</table>
<p>Только после под-фазы layout браузер реально красит пиксели на экране. И лишь <b>затем</b>, уже асинхронно, React запускает <b>passive effects</b> — обычные <code class="i">useEffect</code>. Это объясняет разницу между <code class="i">useLayoutEffect</code> и <code class="i">useEffect</code> из статьи про хуки не как отвлечённое «правило», а как факт устройства: layout-эффект физически не может не успеть до отрисовки, потому что он — часть той же синхронной под-фазы коммита.</p>

<h5>Порядок эффектов по дереву</h5>
<p>При монтировании эффекты запускаются <b>снизу вверх</b> — сначала у самых глубоких детей, потом у родителей: к моменту, когда сработает эффект родителя, дети уже гарантированно смонтированы и их эффекты уже отработали. При размонтировании cleanup идёт в том же порядке — дети раньше родителей. При обновлении: сначала по всему затронутому поддереву отрабатывают cleanup предыдущих эффектов (снизу вверх), и только потом — новые эффекты (снова снизу вверх).</p>
<div class="trap">Частый источник багов — полагать, что эффект родителя увидит актуальный DOM или состояние ребёнка ещё до его монтирования. На mount это неверно в обратную сторону: если родительскому <code class="i">useLayoutEffect</code> нужно что-то от ребёнка (например, измерить его размер), это безопасно именно потому, что ребёнок гарантированно отрабатывает первым.</div>

<h5>Классовые методы жизненного цикла → хуки, сводная таблица</h5>
<table>
<tr><th>Класс</th><th>Хук-эквивалент</th><th>Нюанс</th></tr>
<tr><td><code class="i">constructor</code></td><td>ленивый инициализатор <code class="i">useState(() =&gt; ...)</code></td><td>функция вычисляется один раз, не на каждом рендере</td></tr>
<tr><td><code class="i">render</code></td><td>тело функции компонента</td><td>—</td></tr>
<tr><td><code class="i">componentDidMount</code></td><td><code class="i">useEffect(fn, [])</code></td><td>эффект — асинхронно после отрисовки; метод класса — синхронно в layout-под-фазе. Для точного паритета по таймингу нужен <code class="i">useLayoutEffect</code></td></tr>
<tr><td><code class="i">componentDidUpdate</code></td><td><code class="i">useEffect(fn, [deps])</code></td><td>в классе один метод на все обновления сразу, в хуках — явные зависимости на каждый эффект отдельно</td></tr>
<tr><td><code class="i">componentWillUnmount</code></td><td>функция, возвращённая из <code class="i">useEffect</code></td><td>—</td></tr>
<tr><td><code class="i">shouldComponentUpdate</code></td><td><code class="i">React.memo</code></td><td>—</td></tr>
<tr><td><code class="i">getDerivedStateFromProps</code></td><td>вычисление прямо в теле функции, без отдельного состояния</td><td>в большинстве случаев — признак, что состояние вообще не нужно: значение можно просто посчитать при рендере</td></tr>
<tr><td><code class="i">getSnapshotBeforeUpdate</code></td><td>точного эквивалента нет</td><td>редкий кейс (например, сохранить позицию скролла перед вставкой новых сообщений в чат) — эмулируют через <code class="i">useLayoutEffect</code>, осторожно относясь к таймингу</td></tr>
<tr><td><code class="i">getDerivedStateFromError</code> / <code class="i">componentDidCatch</code></td><td>хука нет — только класс</td><td>см. следующий раздел</td></tr>
</table>

<h5>Error Boundary: почему это до сих пор класс</h5>
<p>Хук не может поймать ошибку в рендере <b>дочернего</b> компонента: хуки работают только в контексте своего собственного Fiber, а исключение в чужом рендере прерывает работу до того, как ваш код получит шанс среагировать. React ловит такие ошибки на уровне reconciler'а и ищет ближайший класс выше по дереву с <code class="i">getDerivedStateFromError</code> (переключить состояние на фолбэк) и/или <code class="i">componentDidCatch</code> (залогировать). Поэтому в React 19 error boundary по-прежнему пишут классом либо берут готовый — например, пакет <code class="i">react-error-boundary</code>.</p>

<h5>Suspense: ещё один способ прервать render phase</h5>
<p>Если во время рендера компонент «бросает» промис (например, через <code class="i">use(promise)</code> или внутреннее API библиотеки данных), React не считает это ошибкой — это сигнал «результат ещё не готов». React отбрасывает недостроенное WIP-поддерево, показывает fallback ближайшего <code class="i">&lt;Suspense&gt;</code> и возвращается к попытке рендера, когда промис зарезолвится. Механически это тот же приём «выбросить WIP-дерево и начать заново», что и при обычном прерывании конкурентного рендера — просто триггер другой.</p>

<h5>StrictMode в dev — теперь понятно, зачем именно так</h5>
<p>Двойной вызов монтирования (<code class="i">mount → unmount → mount</code>, эффект → cleanup → эффект) из статьи про хуки — это симуляция того, что произойдёт на границе двух Fiber-деревьев при быстрой навигации туда-обратно или при Suspense-ретрае. React намеренно прогоняет цикл «<code class="i">current</code> ↔ work-in-progress» дважды с одним и тем же состоянием, чтобы эффекты без корректного cleanup проявили себя сразу в разработке, а не через месяц в проде у случайного пользователя с медленным интернетом.</p>

<h5>SSR и гидратация — отдельный вход в цикл</h5>
<p>На сервере React рендерит компоненты в строку HTML: render phase выполняется, а commit — нет, потому что нет DOM и коммитить некуда. Эффекты — и <code class="i">useLayoutEffect</code>, и <code class="i">useEffect</code> — на сервере не выполняются вовсе. На клиенте вместо обычного монтирования (создать все DOM-узлы с нуля) происходит <b>гидратация</b>: React строит Fiber-дерево, но переиспользует уже существующую разметку от сервера и просто навешивает на неё обработчики событий, вместо того чтобы создавать узлы заново. Если разметка не совпадает (например, из-за <code class="i">Math.random()</code> в рендере или разницы во времени на сервере и клиенте), React предупреждает об ошибке гидратации и на несовпавшем узле перерисовывает поддерево с нуля — уже как обычный клиентский mount, с полноценными эффектами.</p>` }
