import type { TheoryArticle } from '@/engine/types'

export const graphql: TheoryArticle = {
  id: 'th-graphql', topic: 'GraphQL и realtime',
  title: 'GraphQL, realtime и эволюция контрактов',
  lead: 'Как рассуждать о схеме, N+1, кешировании и доставке событий на собеседовании.',
  body: `
<p>GraphQL — это язык и runtime для запроса типизированного графа, а не «магия вместо REST». Клиент выбирает форму ответа, сервер контролирует схему и резолверы. Поэтому вместе с преимуществом гибкости появляются новые обязанности: ограничить сложность запроса, наблюдать стоимость поля и не допустить утечки данных через вложенный граф.</p>

<h5>Путь запроса</h5>
<ol><li>Парсер превращает текст в AST, валидатор проверяет схему и права на поля.</li><li>Планировщик запускает резолверы. Корневой resolver часто получает список сущностей, а дочерние поля могут создать N+1.</li><li>DataLoader собирает ключи в батч в пределах одного запроса и возвращает значения в исходном порядке. Его кэш нельзя бездумно делать глобальным.</li><li>Ответ содержит <code class="i">data</code> и, при частичном сбое, <code class="i">errors</code>; HTTP 200 сам по себе не означает, что бизнес-операция успешна.</li></ol>

<h5>Безопасность и стоимость</h5>
<p>Добавьте depth limit, complexity score, timeout и лимиты размера variables. Для публичного API полезны persisted queries: клиент отправляет hash разрешённой операции, а не произвольный документ. Авторизация проверяется на уровне поля/ресурса, потому что скрыть кнопку в UI недостаточно. Логи должны включать operation name и стоимость, но не персональные variables.</p>

<h5>Совместимость схемы</h5>
<p>Расширение типами и полями обычно обратно совместимо. Переименование — это период с двумя полями, <code class="i">@deprecated</code>, измерением потребителей и объявленной датой удаления. Не меняйте смысл поля и осторожно обращайтесь с nullability и enum: старый клиент может не знать новое значение.</p>

<h5>Realtime</h5>
<table><tr><th>Транспорт</th><th>Когда выбрать</th><th>Что продумать</th></tr>
<tr><td>SSE</td><td>Поток событий сервер → клиент</td><td>reconnect, Last-Event-ID, proxy timeout, heartbeat</td></tr>
<tr><td>WebSocket</td><td>Двунаправленный частый обмен</td><td>аутентификация, backpressure, reconnect, лимит соединений</td></tr>
<tr><td>Polling</td><td>Редкие изменения и простая инфраструктура</td><td>интервал, нагрузка, stale data, jitter</td></tr></table>
<p>События должны иметь offset/версию и идемпотентный consumer. После reconnect клиент запрашивает replay с последнего offset; если история недоступна, получает snapshot и продолжает с его версии. Одного TCP-порядка недостаточно после разрыва соединения.</p>

<div data-demo="query-cache"></div>
<h5>Сильная формулировка на собеседовании</h5>
<blockquote>«Я выберу GraphQL, если клиентам нужны разные представления связанного графа и готова дисциплина схемы. Для простого CRUD REST может быть дешевле. В любом варианте измерю стоимость запроса, поставлю лимиты и опишу стратегию эволюции контракта».</blockquote>`,
}
