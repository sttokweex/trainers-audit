import type { TheoryArticle } from '@/engine/types'

export const async: TheoryArticle = { id:'th-async', topic:'JavaScript', title:'Асинхронность: event loop, промисы, async/await',
  lead:'Модель выполнения JavaScript целиком — от стека вызовов до микрозадач. Из неё растёт половина вопросов на собеседовании.',
  body:`
<p>Почти все каверзные вопросы по JavaScript сводятся к одному: <b>что выполняется сейчас, а что потом, и в каком порядке это «потом» наступит</b>. Если держать в голове точную модель, такие задачи перестают быть загадками и решаются механически.</p>

<h5>Один поток и почему это не мешает</h5>
<p>В одном JS-контексте есть <b>один поток выполнения</b> и один стек вызовов. Две функции в нём не выполняются одновременно: между соседними строками синхронного кода никто не вклинится. Но <code class="i">worker_threads</code> и Web Workers создают другие контексты, а <code class="i">SharedArrayBuffer</code> допускает настоящие гонки данных между потоками.</p>
<p>Возникает вопрос: если поток один, как страница успевает грузить десяток картинок, слушать клики и держать WebSocket? Ответ в том, что <b>эту работу делает не движок JavaScript</b>. Сетевые запросы, таймеры, работа с диском — всё это выполняет окружение: браузер (через свои внутренние потоки) или libuv в Node.js. Движок только отдаёт задание и получает уведомление о готовности.</p>
<p>Схема такая: вы вызываете <code class="i">setTimeout</code> или <code class="i">fetch</code> — движок передаёт работу окружению и <b>немедленно продолжает выполнение</b>. Когда окружение закончило, оно кладёт ваш колбэк в очередь. Event loop — это бесконечный цикл, который проверяет: «стек пуст? тогда возьму следующее из очереди и положу в стек».</p>
<div class="key"><strong class="reading-key">Колбэк никогда не прервёт выполняющийся код.</strong> Он ждёт, пока стек полностью опустеет. Отсюда главный практический вывод: любая долгая синхронная операция замораживает <b>всё</b> — клики, анимации, входящие ответы сервера.</div>

<div data-demo="event-loop"></div>
<h5>Две очереди, а не одна</h5>
<p>Вот здесь начинается то, что отличает поверхностное понимание от настоящего. Очередей <b>две</b>, и у них разный приоритет. Ниже — что в них попадает, и <b>разбор каждого способа</b> туда что-то положить, потому что без этого список выглядит как заклинание.</p>

<h6>Что кладёт задачу в очередь МАКРОзадач</h6>
<table>
<tr><th>Способ</th><th>Где работает</th><th>Что делает</th></tr>
<tr><td><code class="i">setTimeout(fn, ms)</code></td><td>везде</td><td>выполнить <code class="i">fn</code> один раз, <b>не раньше</b> чем через <code class="i">ms</code></td></tr>
<tr><td><code class="i">setInterval(fn, ms)</code></td><td>везде</td><td>то же, но повторяя, пока не вызовут <code class="i">clearInterval</code></td></tr>
<tr><td>Обработчики событий</td><td>браузер</td><td>клик, ввод, скролл, загрузка изображения — браузер кладёт колбэк в очередь, когда событие произошло</td></tr>
<tr><td>Завершение I/O</td><td>Node</td><td>файл прочитан, пришёл ответ сети, запрос к БД вернулся — колбэк встаёт в очередь</td></tr>
<tr><td><code class="i">setImmediate(fn)</code></td><td><b>только Node</b></td><td>выполнить как можно раньше, но <b>после</b> текущего этапа ожидания ввода-вывода</td></tr>
<tr><td><code class="i">MessageChannel</code>, <code class="i">postMessage</code></td><td>браузер</td><td>сообщения между окнами и воркерами; это отдельный task source, и его порядок с таймерами зависит от окружения</td></tr>
</table>

<h6>Что кладёт задачу в очередь МИКРОзадач</h6>
<table>
<tr><th>Способ</th><th>Где</th><th>Что делает</th></tr>
<tr><td><code class="i">.then</code> / <code class="i">.catch</code> / <code class="i">.finally</code></td><td>везде</td><td>колбэк промиса — ставится в очередь в момент, когда промис <b>завершился</b></td></tr>
<tr><td>продолжение после <code class="i">await</code></td><td>везде</td><td><code class="i">await</code> — это сахар над <code class="i">.then</code>, поэтому весь код после него становится микрозадачей</td></tr>
<tr><td><code class="i">queueMicrotask(fn)</code></td><td>везде</td><td>явно положить функцию в очередь микрозадач, без создания промиса</td></tr>
<tr><td><code class="i">MutationObserver</code></td><td>браузер</td><td>колбэк наблюдателя за изменениями DOM</td></tr>
<tr><td><code class="i">process.nextTick(fn)</code></td><td><b>только Node</b></td><td>отдельная очередь с приоритетом <b>выше</b> промисов</td></tr>
</table>

<h6>Теперь подробно про каждый</h6>
<p><b><code class="i">setTimeout(fn, ms)</code></b> — «выполни функцию не раньше чем через <code class="i">ms</code> миллисекунд». Ключевое слово — <b>не раньше</b>: это не обещание точного времени, а минимальная задержка. Если в момент истечения таймера главный поток занят, колбэк подождёт в очереди.</p>
<pre class="code">const id = setTimeout(() =&gt; console.log('через секунду'), 1000)
clearTimeout(id)   // отменить, если ещё не выполнилось</pre>
<p>Две тонкости, о которых спрашивают. Первая: <code class="i">setTimeout(fn, 0)</code> не означает «сейчас» — это «в следующую макрозадачу, после всех микрозадач». Вторая: браузеры <b>принудительно поднимают задержку до ~4 мс</b>, если таймеры вложены друг в друга глубже пяти уровней (защита от busy-loop), а в фоновой вкладке интервал и вовсе урезается до секунды и реже.</p>

<p><b><code class="i">setInterval(fn, ms)</code></b> — то же, но повторно. Его подводный камень: тики планируются <b>независимо от того, успел ли выполниться предыдущий колбэк</b>. В одном JS-потоке колбэки не выполняются параллельно, но их вызовы могут задерживаться и накапливаться, появляется drift. Поэтому для повторяющейся асинхронной работы обычно берут рекурсивный <code class="i">setTimeout</code>:</p>
<pre class="code">// ❌ если запрос идёт 2 секунды, вызовы накопятся
setInterval(() =&gt; fetchData(), 1000)

// ✅ следующий запуск планируется только после завершения текущего
async function poll() {
  await fetchData()
  setTimeout(poll, 1000)
}
poll()</pre>

<p><b><code class="i">setImmediate(fn)</code></b> — <b>существует только в Node.js</b>, в браузере его нет (единственное исключение — старый Internet Explorer). Название обманчиво: это не «немедленно», а «в фазе <b>check</b>, которая идёт сразу после фазы ожидания ввода-вывода».</p>
<p>Зачем он нужен: чтобы отдать управление циклу событий, дав ему обработать готовый ввод-вывод, и вернуться к работе следующим шагом. Типичный приём — разбить тяжёлую синхронную обработку на порции, чтобы не блокировать сервер:</p>
<pre class="code">function processChunk(items, i = 0) {
  const end = Math.min(i + 1000, items.length)
  for (; i &lt; end; i++) heavyWork(items[i])

  if (i &lt; items.length) setImmediate(() =&gt; processChunk(items, i))
  // между порциями Node успеет обработать входящие запросы
}</pre>
<p>Отличие от <code class="i">setTimeout(fn, 0)</code>: на верхнем уровне программы их порядок <b>непредсказуем</b> (зависит от того, успела ли пройти миллисекунда), а <b>внутри I/O-колбэка <code class="i">setImmediate</code> всегда выполнится раньше</b> — потому что фаза check следует сразу за poll, а до фазы таймеров нужно пройти полный круг. Это любимый уточняющий вопрос на собеседовании.</p>
<div class="key"><b>Что такое «фаза» в Node:</b> это не новая очередь и не второй поток, а <strong class="reading-key">участок одного оборота libuv</strong>. В <code class="i">timers</code> запускаются просроченные таймеры, в <code class="i">poll</code> принимаются и исполняются готовые I/O-события, в <code class="i">check</code> — <code class="i">setImmediate</code>, в <code class="i">close callbacks</code> — уведомления о закрытии сокета. Между колбэками Node сначала выгребает <code class="i">process.nextTick</code>, затем промисы. Полная таблица фаз и поведение пустого <code class="i">poll</code> разобраны в статье «Node.js под капотом».</div>

<p><b><code class="i">queueMicrotask(fn)</code></b> — «положи эту функцию в очередь микрозадач». Нужен, когда хочется отложить действие <b>до конца текущего синхронного кода, но до любых таймеров и отрисовки</b>, и при этом не создавать промис ради этого.</p>
<pre class="code">// раньше писали так — работает, но создаёт лишний объект промиса
Promise.resolve().then(() =&gt; doLater())

// современный явный способ
queueMicrotask(() =&gt; doLater())</pre>
<p>Реальный сценарий: вы меняете несколько свойств объекта и хотите разослать одно уведомление об изменении, а не пять. Планируете микрозадачу с флагом — и она выполнится после того, как весь синхронный код отработает, но раньше, чем браузер что-либо отрисует. Ровно на этом принципе построен батчинг обновлений во многих библиотеках состояния.</p>

<p><b><code class="i">MutationObserver</code></b> — <b>браузерный API</b> для наблюдения за изменениями DOM: добавлением и удалением узлов, сменой атрибутов, изменением текста. Он существует, потому что старый способ (события <code class="i">DOMNodeInserted</code>) был чудовищно медленным.</p>
<pre class="code">const observer = new MutationObserver((mutations) =&gt; {
  for (const m of mutations) console.log(m.type, m.target)
})
observer.observe(document.body, { childList: true, subtree: true, attributes: true })
observer.disconnect()   // не забыть отписаться, иначе утечка</pre>
<p>Почему он в микрозадачах: изменения DOM <b>накапливаются</b> и отдаются колбэку одной пачкой после окончания синхронного кода. Если бы колбэк вызывался на каждое изменение, вставка тысячи узлов дала бы тысячу вызовов.</p>
<p>Где применяется на практике: интеграция со сторонними виджетами, которые правят DOM мимо вашего фреймворка; отслеживание появления элемента, который рисует чужой скрипт; автоподстройка высоты textarea. В React он почти не нужен — вы и так знаете, когда меняете дерево.</p>

<p><b><code class="i">process.nextTick(fn)</code></b> — <b>только Node</b>. Формально это <i>не</i> часть очереди микрозадач: у него собственная очередь, которая разгребается <b>перед</b> промисами.</p>
<pre class="code">process.nextTick(() =&gt; console.log('1 — nextTick'))
Promise.resolve().then(() =&gt; console.log('2 — промис'))
// вывод: 1, потом 2 — всегда</pre>
<p>Исторически он нужен был внутренним модулям Node, чтобы гарантированно выполнить что-то до продолжения цикла. В прикладном коде его практически не используют: во-первых, промисы понятнее, во-вторых, рекурсивный <code class="i">nextTick</code> способен <b>полностью заморозить</b> цикл событий — его очередь выгребается до конца, и ввод-вывод никогда не получит управление.</p>

<h6>Два соседних API, которые сюда не входят, но их путают</h6>
<p><b><code class="i">requestAnimationFrame(fn)</code></b> (браузер) — «выполни функцию <b>перед следующей отрисовкой кадра</b>», то есть примерно 60 раз в секунду. Это не макрозадача и не микрозадача: у кадра своя фаза в цикле. Для анимаций он лучше <code class="i">setTimeout(fn, 16)</code>, потому что синхронизирован с реальной частотой обновления экрана и <b>не выполняется в фоновой вкладке</b> — браузер экономит батарею.</p>
<pre class="code">function animate() {
  el.style.transform = \`translateX(\${x++}px)\`
  requestAnimationFrame(animate)     // следующий кадр
}
requestAnimationFrame(animate)</pre>
<p><b><code class="i">requestIdleCallback(fn)</code></b> (браузер) — «выполни, когда браузер будет свободен». Для некритичной работы: предзагрузка, отправка аналитики, прогрев кеша. Принимает <code class="i">timeout</code>, чтобы задача не откладывалась бесконечно.</p>
<p>Современная замена обоим для дробления длинных задач — <code class="i">scheduler.yield()</code> и <code class="i">scheduler.postTask()</code> с явными приоритетами.</p>

<div class="key">Сводка одной фразой: <b>макрозадача</b> — «отдельный заход цикла событий», между ними браузер может отрисовать кадр и обработать клик. <b>Микрозадача</b> — «доделать прямо сейчас, до того как кто-либо что-то увидит». Поэтому в микрозадачи кладут то, что должно завершиться атомарно с текущим кодом, а в макрозадачи — то, что можно отложить.</div>
<p>Порядок работы цикла:</p>
<pre class="code">┌─ выполнить ОДНУ макрозадачу (в самом начале — это ваш скрипт целиком)
│
├─ выгрести ВСЮ очередь микрозадач
│     включая те микрозадачи, которые появились по ходу разгребания
│
├─ при необходимости — отрисовать кадр
│     requestAnimationFrame → пересчёт стилей → layout → paint
│
└─ вернуться к началу: следующая макрозадача</pre>
<p>Обратите внимание на асимметрию: <b>макрозадача берётся строго одна, а микрозадачи выгребаются все до единой</b>. Причём если микрозадача породила новую микрозадачу, та тоже выполнится в этом же заходе, не дожидаясь следующего оборота цикла.</p>
<div class="warn">Отсюда неочевидное следствие: рекурсивно планируя микрозадачи, можно <strong class="reading-warn">навсегда заблокировать</strong> и рендеринг, и обработку событий — цикл никогда не выйдет из фазы разгребания. С <code class="i">setTimeout</code> такого не случится: каждый следующий колбэк — отдельная макрозадача, между ними браузер успеет и отрисовать кадр, и обработать клик.</div>

<h5>Короткий пример: микрозадача внутри таймера</h5>
<pre class="code">console.log('начало')
setTimeout(() =&gt; {
  console.log('таймер')
  Promise.resolve().then(() =&gt; console.log('микрозадача таймера'))
}, 0)
Promise.resolve().then(() =&gt; console.log('микрозадача скрипта'))
console.log('конец')</pre>
<p>Сначала полностью выполняется текущий скрипт, поэтому первые строки вывода — <code class="i">начало</code> и <code class="i">конец</code>. Затем браузер очищает очередь микрозадач: появляется <code class="i">микрозадача скрипта</code>. После этого запускается таймер и печатает <code class="i">таймер</code>.</p>
<p>Таймер добавил новую микрозадачу уже во время своего выполнения. Когда колбэк таймера закончился, браузер снова очищает очередь микрозадач <b>до следующего таймера и отрисовки</b>. Поэтому последняя строка — <code class="i">микрозадача таймера</code>.</p>
<p>Итог: <code class="i">начало → конец → микрозадача скрипта → таймер → микрозадача таймера</code>.</p>
<div class="note">Пример показывает главное правило без лишних деталей: <strong class="reading-note">после каждой одной макрозадачи браузер выполняет все готовые микрозадачи</strong>. Если микрозадача добавила ещё одну, браузер выполнит и её, прежде чем перейти к следующей макрозадаче.</div>

<h5>Промис — это объект-результат, а не отложенный вызов</h5>
<p>Распространённое заблуждение: «промис запускается, когда я делаю <code class="i">await</code>». Нет. Промис начинает работу <b>в момент создания</b>.</p>
<pre class="code">const p1 = api.getUser()    // запрос УЖЕ ушёл в сеть
const p2 = api.getOrders()  // и этот тоже — параллельно

const user   = await p1     // здесь мы только ЖДЁМ результат
const orders = await p2     // он, скорее всего, уже готов</pre>
<p>Этот код параллельный, хотя выглядит как последовательный. А вот такой — действительно последовательный, и это частая ошибка производительности:</p>
<pre class="code">const user   = await api.getUser()     // ждём 200 мс
const orders = await api.getOrders()   // только теперь начинаем, ещё 200 мс
// итого 400 мс вместо 200</pre>
<p>У промиса три состояния: <code class="i">pending</code>, <code class="i">fulfilled</code>, <code class="i">rejected</code>. Переход происходит <b>один раз и необратимо</b> — повторный вызов <code class="i">resolve</code> молча игнорируется. Это свойство делает промис безопасным: подписавшись на него дважды, вы гарантированно получите один и тот же результат.</p>

<div data-demo="promise-combinators"></div>
<h5>Четыре комбинатора и когда какой</h5>
<table>
<tr><th>Метод</th><th>Резолвится</th><th>Отклоняется</th><th>Когда применять</th></tr>
<tr><td><code class="i">all</code></td><td>когда все успешны</td><td>при первой же ошибке</td><td>все запросы обязательны — без любого страница бессмысленна</td></tr>
<tr><td><code class="i">allSettled</code></td><td>всегда, когда все завершились</td><td>никогда</td><td>частичный результат приемлем: дашборд, где один виджет может не загрузиться</td></tr>
<tr><td><code class="i">race</code></td><td colspan="2">по первому <i>завершившемуся</i>, успех это или ошибка</td><td>таймауты, гонка нескольких источников</td></tr>
<tr><td><code class="i">any</code></td><td>по первому успешному</td><td>только если упали все (<code class="i">AggregateError</code>)</td><td>несколько зеркал, подойдёт любое</td></tr>
</table>
<p><code class="i">Promise.all</code> имеет важную особенность: при ошибке он отклоняется <b>сразу</b>, но <strong class="reading-warn">не отменяет остальные запросы</strong> — они продолжают выполняться впустую. Если это важно, нужен <code class="i">AbortController</code>.</p>

<h5>Последовательно, параллельно или с ограничением</h5>
<pre class="code">// последовательно — когда каждый шаг зависит от предыдущего
// или когда нельзя нагружать внешний API
for (const id of ids) {
  await processOne(id)
}

// параллельно — независимые операции
const results = await Promise.all(ids.map(processOne))

// параллельно, но устойчиво к сбоям
const settled = await Promise.allSettled(ids.map(processOne))
const ok = settled.filter(r =&gt; r.status === 'fulfilled').map(r =&gt; r.value)
const failed = settled.filter(r =&gt; r.status === 'rejected')

// с ограничением параллельности — золотая середина
// 500 одновременных запросов положат и ваш сервер, и чужой API
const results = await pLimit(ids.map(id =&gt; () =&gt; processOne(id)), 5)</pre>
<p>Про ограничение стоит помнить отдельно: браузер и так держит максимум 6 соединений на домен по HTTP/1.1, а сервер на той стороне может ответить <code class="i">429</code>. <strong class="reading-warn">Параллельность — не всегда добро.</strong></p>

<div data-demo="debounce-throttle"></div>
<h5>Ловушка, на которой спотыкаются почти все</h5>
<pre class="code">const results = []
ids.forEach(async (id) =&gt; {
  results.push(await fetchOne(id))
})
console.log(results)   // [] — пусто!</pre>
<p><code class="i">forEach</code> ничего не знает про промисы. Он просто вызывает колбэк для каждого элемента, получает обратно промис и <b>выбрасывает его</b>. Цикл завершается мгновенно, до того как хоть один запрос закончился. Хуже того: ошибка внутри такого колбэка превратится в необработанное отклонение промиса.</p>
<p>То же касается <code class="i">map</code> без <code class="i">Promise.all</code>, <code class="i">filter</code> с асинхронным предикатом (он получит промис, а промис всегда истинный — отфильтруется всё) и <code class="i">reduce</code> без аккуратного накопления. Правильно — либо <code class="i">for...of</code> с <code class="i">await</code>, либо <code class="i">Promise.all(arr.map(...))</code>.</p>

<h5>Обработка ошибок</h5>
<p><code class="i">try/catch</code> ловит только то, что вы <b>дождались</b>. Промис без <code class="i">await</code> и без <code class="i">.catch</code> порождает <code class="i">unhandledRejection</code>: в браузере это ошибка в консоли, а в современных Node обычно приводит к аварийному завершению процесса (поведение можно изменить флагом <code class="i">--unhandled-rejections</code>).</p>
<pre class="code">// ❌ ошибка улетит мимо catch: мы не дождались промиса
try {
  doSomethingAsync()
} catch (e) { /* сюда никогда не попадём */ }

// ✅
try {
  await doSomethingAsync()
} catch (e) { handle(e) }

// ✅ «запустить и забыть», но осознанно
void doSomethingAsync().catch(err =&gt; logger.error(err))</pre>
<p>Ещё одна тонкость: <code class="i">.then(onSuccess, onError)</code> — не то же самое, что <code class="i">.then(onSuccess).catch(onError)</code>. Во втором случае <code class="i">catch</code> поймает ошибку, брошенную внутри <code class="i">onSuccess</code>; в первом — нет, потому что <code class="i">onError</code> относится к <i>предыдущему</i> звену цепочки.</p>

<h5>Отмена через AbortController</h5>
<p>Промисы нельзя отменить — это принципиальное ограничение. Но операцию, которая за промисом стоит, отменить можно.</p>
<pre class="code">const controller = new AbortController()

fetch('/api/data', { signal: controller.signal })
  .catch(e =&gt; { if (e.name !== 'AbortError') throw e })

controller.abort()          // соединение реально разрывается</pre>
<p>Это важно отличать от <code class="i">Promise.race</code> с таймером: <strong class="reading-warn">race лишь перестаёт ждать ответ</strong>, а сам запрос продолжает висеть, занимая соединение и нагружая сервер. <code class="i">AbortSignal</code> стал общим механизмом отмены: его принимают <code class="i">fetch</code>, <code class="i">addEventListener</code> (изящный способ массовой отписки), стримы Node и <code class="i">queryFn</code> в TanStack Query.</p>

<h5>Чек-лист для собеседования</h5>
<ul>
<li>Один поток, стек вызовов, работу делает окружение — а не движок.</li>
<li>Одна макрозадача, потом <b>все</b> микрозадачи, потом кадр.</li>
<li><code class="i">await</code> = микрозадача; код до первого await синхронный.</li>
<li>Промис стартует при создании, а не при await.</li>
<li><code class="i">forEach</code> не умеет ждать.</li>
<li>Необработанное отклонение роняет Node.</li>
<li>Отмена — это <code class="i">AbortController</code>, а не <code class="i">race</code>.</li>
</ul>` }
