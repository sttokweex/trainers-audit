import type { TheoryArticle } from '@/engine/types'

export const observability: TheoryArticle = { id:'th-observability', topic:'DevOps и процессы', title:'Наблюдаемость: логи, метрики, трейсы',
  lead:'Три столпа observability, структурированные логи, OpenTelemetry и распределённая трассировка, SLI/SLO — как понять, что происходит в проде, не читая код по коммиту.',
  body:`
<h5>Мониторинг против observability — не синонимы</h5>
<p><b>Мониторинг</b> отвечает на заранее известные вопросы: «жив ли сервис», «сколько 500-х за минуту» — набор дашбордов под предсказанные сценарии. <b>Observability</b> — это способность ответить на вопрос, который <b>заранее не предвидели</b>: «почему у пользователей из Казахстана на iOS 17 корзина иногда теряет один товар» — без нового деплоя с дополнительным логированием, только по уже собранным данным.</p>
<div class="key">Разница на практике: с мониторингом вы узнаёте, ЧТО сломалось. С observability — можете раскопать, ПОЧЕМУ, пройдя по одному запросу через все сервисы, которые он затронул.</div>

<h5>Три столпа</h5>
<table>
<tr><th>Столп</th><th>Отвечает на</th><th>Инструменты</th></tr>
<tr><td><b>Логи</b></td><td>что именно произошло, с деталями</td><td>Pino/Winston, ELK, Loki</td></tr>
<tr><td><b>Метрики</b></td><td>сколько, как часто, с какой скоростью — числа во времени</td><td>Prometheus, Grafana</td></tr>
<tr><td><b>Трейсы</b></td><td>как запрос прошёл через систему и где застрял</td><td>OpenTelemetry, Jaeger, Tempo</td></tr>
</table>
<p>Они дополняют друг друга: метрика в Grafana показывает всплеск задержки на эндпоинте → трейс конкретного медленного запроса показывает, что 90% времени ушло на вызов внешнего провайдера платежей → лог этого спана содержит его <code class="i">requestId</code> и текст ответа. Без связки вы либо тонете в логах, либо видите красивый график без единой подсказки о причине.</p>

<h5>Структурированные логи</h5>
<pre class="code">// ❌ строка — искать можно только текстовым поиском, ничего не агрегировать
console.log(\`User \${userId} created order \${orderId} for \${amount}\`)

// ✅ структура — можно фильтровать, агрегировать, строить алерты
logger.info({ event: 'order.created', userId, orderId, amount, requestId }, 'order created')</pre>
<p>Структурированный лог — это по сути маленький JSON-объект, а не строка для человека. Требования к хорошему логу:</p>
<ul>
<li><b>Уровни</b>: <code class="i">error</code> — требует внимания сейчас, <code class="i">warn</code> — потенциальная проблема, <code class="i">info</code> — значимые бизнес-события, <code class="i">debug</code> — детали для расследования, выключены в проде по умолчанию.</li>
<li><b>Correlation id</b> (обычно <code class="i">X-Request-Id</code>, сквозной с заголовков HTTP) — единственный способ собрать все логи одного запроса из кучи параллельных, если сервис обрабатывает тысячи запросов в секунду.</li>
<li><b>Не логировать</b> пароли, токены, номера карт — те же правила, что в статье про безопасность.</li>
<li><b>Логировать событие, а не текст для отладки</b>: <code class="i">'payment.failed'</code> с полями лучше, чем <code class="i">'что-то пошло не так с оплатой пользователя 42'</code> — первое можно агрегировать и на него можно поставить алерт, второе только читать глазами.</li>
</ul>
<div class="warn">Логирование само по себе не бесплатно: <code class="i">console.log</code> в горячем пути синхронно блокирует event loop на запись в stdout. На нагруженном сервисе логгер должен уметь буферизовать и писать асинхронно (Pino это делает через отдельный поток по умолчанию).</div>

<h5>Метрики: три типа, которых достаточно почти всегда</h5>
<table>
<tr><th>Тип</th><th>Поведение</th><th>Пример</th></tr>
<tr><td><b>Counter</b></td><td>только растёт, сбрасывается лишь при рестарте</td><td>число обработанных запросов, ошибок</td></tr>
<tr><td><b>Gauge</b></td><td>может расти и падать — снимок текущего значения</td><td>занятая память, число открытых соединений, размер очереди</td></tr>
<tr><td><b>Histogram</b></td><td>распределение значений по корзинам</td><td>время ответа — чтобы считать перцентили</td></tr>
</table>
<p>Счётчик ошибок сам по себе мало что говорит — важно отношение к общему числу запросов (error rate). А для задержки <b>среднее — плохая метрика</b>: один запрос на 30 секунд среди тысячи быстрых почти не сдвинет среднее, но убьёт опыт того самого пользователя. Поэтому смотрят на <b>перцентили</b>: p50 (медиана — типичный опыт), p95 и p99 (что чувствуют самые невезучие пользователи — обычно именно они первыми напишут в поддержку).</p>
<pre class="code">http_requests_total{route="/api/orders", status="500"} 42
http_request_duration_seconds{route="/api/orders", quantile="0.99"} 1.8</pre>

<h5>OpenTelemetry: как это работает изнутри</h5>
<p>OpenTelemetry (OTel) — открытый стандарт для сбора логов, метрик и трейсов, единый для любого языка и любого бэкенда хранения (Jaeger, Tempo, Datadog, любой другой — меняется только <b>экспортёр</b>, код инструментирования не трогают).</p>
<h6>Span — единица трассировки</h6>
<p><b>Span</b> — один отрезок работы с именем, временем начала/конца, атрибутами и статусом: «выполнить SQL-запрос», «дождаться ответа платёжного провайдера», «обработать HTTP-запрос целиком». Спаны образуют дерево через <code class="i">parentId</code>: HTTP-спан родитель для спана SQL-запроса внутри обработчика, тот — для спана Redis-кеша, и так далее. Всё дерево одного запроса — это и есть <b>трейс</b>, объединённый общим <code class="i">traceId</code>.</p>
<pre class="code">const tracer = trace.getTracer('orders-service')

async function createOrder(dto: CreateOrderDto) {
  return tracer.startActiveSpan('createOrder', async (span) =&gt; {
    span.setAttribute('order.itemsCount', dto.items.length)
    try {
      const order = await this.repo.save(dto)   // авто-инструментация создаст дочерний спан на SQL
      span.setStatus({ code: SpanStatusCode.OK })
      return order
    } catch (e) {
      span.recordException(e)
      span.setStatus({ code: SpanStatusCode.ERROR })
      throw e
    } finally {
      span.end()
    }
  })
}</pre>
<p><b>Авто-инструментация</b> — самая практичная часть: библиотеки для Nest/Express/pg/TypeORM/Redis сами оборачивают вызовы в спаны без единой правки бизнес-кода — достаточно один раз подключить SDK при старте процесса. Ручные спаны (как выше) добавляют только там, где важна именно бизнес-граница операции, которую авто-инструментация не видит.</p>
<h6>Распространение контекста между сервисами</h6>
<p>Когда сервис A вызывает сервис B по HTTP, <code class="i">traceId</code> и <code class="i">spanId</code> родителя передаются в заголовке <code class="i">traceparent</code> (стандарт W3C Trace Context). Сервис B начинает свои спаны как дочерние по отношению к пришедшему — и весь путь запроса через несколько сервисов собирается в один трейс, хотя каждый сервис ничего не знает о внутренностях другого.</p>
<div class="note">Часто путают трассировку и <code class="i">X-Request-Id</code> из логов. Это родственные, но разные вещи: request id — плоская строка для склейки логов текстовым поиском; trace/span id — структура с деревом вложенных операций и точным временем каждой. На практике в лог кладут оба — trace id как раз и связывает лог с деревом спанов в Jaeger.</div>

<h5>Health-checks: не всё, что называется health, одинаково полезно</h5>
<table>
<tr><th>Проверка</th><th>Вопрос</th><th>Что делает оркестратор при провале</th></tr>
<tr><td><b>Liveness</b></td><td>процесс вообще жив, не завис ли event loop</td><td>перезапускает контейнер</td></tr>
<tr><td><b>Readiness</b></td><td>готов ли принимать трафик прямо сейчас (БД доступна, кеш прогрет)</td><td>убирает из балансировщика, но не перезапускает</td></tr>
</table>
<p>Смешивать их — частая ошибка: если liveness проверяет доступность БД, а база на 10 секунд подвисла, оркестратор начнёт убивать и пересоздавать <b>все</b> поды разом, хотя проблема была не в приложении. Liveness должен проверять только «жив ли сам процесс», readiness — «готов ли обслуживать запросы».</p>

<h5>SLI, SLO и error budget</h5>
<p><b>SLI</b> (Service Level Indicator) — измеримая метрика: доля успешных ответов, p99 задержки. <b>SLO</b> (Service Level Objective) — целевое значение для SLI: «99.9% запросов быстрее 300 мс за 30 дней». <b>Error budget</b> — допустимый объём несоответствия SLO: при 99.9% это 43 минуты простоя в месяц, которые <b>можно</b> потратить осознанно — на рискованный релиз, миграцию, эксперимент.</p>
<div class="key">Практический смысл error budget — он переводит «давайте будем осторожнее» в конкретное число. Бюджет не исчерпан — можно катить рискованные изменения быстрее. Бюджет сгорел — заморозка нешаблонных релизов до восстановления. Это снимает вечный спор между «скорость фич» и «стабильность»: решение принимает цифра, а не мнение самого убедительного в комнате.</div>

<h5>Алерты: на что стоит будить человека ночью</h5>
<p>Правило: алерт должен быть <b>actionable</b> — из него сразу понятно, что делать. Алерт «CPU 80%» будит и ничего не говорит: может, это нормальный пик. Алерт «error rate &gt; 5% пять минут подряд, p99 &gt; SLO» — уже сигнал реальной деградации пользовательского опыта.</p>
<p>Симптомы (то, что чувствует пользователь: ошибки, задержка) важнее причин (то, что чувствует система: CPU, память) — именно по симптомам стоит будить дежурного, а причины смотрят уже при расследовании через трейсы и логи.</p>` }
