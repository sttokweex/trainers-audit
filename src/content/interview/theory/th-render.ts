import type { TheoryArticle } from '@/engine/types'

export const render: TheoryArticle = { id:'th-render', topic:'React', title:'Как React рендерит и почему приложение тормозит',
  lead:'Полный путь от setState до пикселей, и что с этим делать, когда становится медленно.',
  body:`
<h5>Полный цикл обновления</h5>
<pre class="code">setState()
   ↓  React планирует обновление с определённым приоритетом
RENDER PHASE      вызвать функции компонентов, получить дерево элементов
   ↓              чистая фаза: можно прервать, отбросить и запустить заново
RECONCILIATION    сравнить новое дерево со старым, собрать список правок
   ↓
COMMIT PHASE      применить правки к DOM, вызвать useLayoutEffect
   ↓              синхронно, прервать нельзя
БРАУЗЕР           style → layout → paint → composite
   ↓
useEffect         после отрисовки, асинхронно</pre>
<p>Важно развести два понятия, которые постоянно смешивают. <b>Рендер</b> в терминологии React — это вызов функции вашего компонента и получение описания UI (обычных JS-объектов). <b>Отрисовка</b> — то, что делает браузер с реальным DOM. Компонент может рендериться десять раз подряд, и если результат тот же, браузер не тронет ни одного пикселя.</p>
<div class="key">Ререндер сам по себе не страшен. Дорого — тяжёлые вычисления внутри него и последующие мутации DOM. Прежде чем бороться с ререндерами, стоит убедиться, что они действительно чего-то стоят.</div>

<h5>Реконсиляция: как работает сравнение</h5>
<p>Полноценное сравнение двух деревьев — задача O(n³). React использует O(n) за счёт двух допущений, и они объясняют всё его поведение.</p>
<p><b>Первое: элементы разных типов дают разные деревья.</b> Если на том же месте был <code class="i">&lt;div&gt;</code>, а стал <code class="i">&lt;span&gt;</code>, или был компонент <code class="i">Foo</code>, а стал <code class="i">Bar</code>, React <b>размонтирует всё поддерево</b> и создаст заново. Состояние внутри теряется полностью.</p>
<pre class="code">// ❌ компонент пересоздаётся каждый рендер — тип всегда «новый»
function Parent() {
  function Child() { return &lt;input /&gt; }   // новая функция = новый тип
  return &lt;Child /&gt;
}
// введённый в input текст будет теряться при каждом рендере Parent</pre>
<p><b>Второе: внутри списка элементы сопоставляются по <code class="i">key</code>.</b> Одинаковый key и одинаковый тип — значит, это тот же компонент: React переиспользует его состояние и DOM-узел.</p>

<div data-demo="react-keys"></div>
<h5>Почему индекс — плохой ключ</h5>
<p>Список <code class="i">[Аня, Боря]</code> превратился в <code class="i">[Вова, Аня, Боря]</code>. С <code class="i">key={index}</code> элемент с ключом 0 раньше был Аней, а теперь Вова. React считает, что это <b>тот же самый компонент с изменившимися пропсами</b>: вместо одной вставки он перезаписывает содержимое всех трёх.</p>
<p>Последствия: состояние «прилипает» не к тому элементу (введённый текст, отмеченный чекбокс, позиция фокуса), срываются анимации, падает производительность. Индекс допустим только для статичных списков — без переупорядочивания, фильтрации и вставок в середину.</p>
<p>Обратная сторона медали полезна: <b>смена key — это способ принудительно размонтировать компонент</b>. Официально рекомендуемый приём для сброса состояния формы.</p>

<h5>Когда компонент перерисовывается</h5>
<ol>
<li>Изменилось его собственное состояние.</li>
<li><b>Перерисовался родитель</b> — по умолчанию всегда, независимо от того, изменились ли пропсы.</li>
<li>Изменился контекст, на который он подписан.</li>
<li>Обновился внешний стор, из которого он читает.</li>
</ol>
<p>Пункт 2 — источник главного заблуждения. «Пропсы не изменились» само по себе ничего не останавливает: нужен <code class="i">React.memo</code>. Причём memo сравнивает пропсы <b>поверхностно</b>, через <code class="i">Object.is</code> по каждому ключу.</p>

<h5>Приём, который недооценивают: поднять содержимое</h5>
<pre class="code">// ❌ Slow перерисовывается при каждом изменении count
function App() {
  const [count, setCount] = useState(0)
  return (
    &lt;div onClick={() =&gt; setCount(c =&gt; c + 1)}&gt;
      {count}
      &lt;Slow /&gt;
    &lt;/div&gt;
  )
}

// ✅ children создан ВНЕ перерисовывающегося компонента
function App() {
  return &lt;Counter&gt;&lt;Slow /&gt;&lt;/Counter&gt;
}
function Counter({ children }) {
  const [count, setCount] = useState(0)
  return &lt;div onClick={() =&gt; setCount(c =&gt; c + 1)}&gt;{count}{children}&lt;/div&gt;
}</pre>
<p><code class="i">children</code> — это уже готовый элемент, созданный в <code class="i">App</code>. Когда <code class="i">Counter</code> перерисовывается, объект элемента остаётся <b>тем же самым</b>, и React пропускает поддерево. Оптимизация без единого <code class="i">memo</code>, просто за счёт композиции.</p>

<h5>Контекст и лишние ререндеры</h5>
<p>При изменении значения провайдера перерисовываются <b>все</b> потребители этого контекста. Селективной подписки на часть значения нет, и <code class="i">React.memo</code> на промежуточных компонентах не спасает — обновление контекста «телепортируется» напрямую к потребителю.</p>
<pre class="code">// ❌ новый объект каждый рендер провайдера — все потребители обновятся
&lt;AppContext.Provider value={{ user, theme, cart, setCart }}&gt;

// ✅ мемоизировать значение
const value = useMemo(() =&gt; ({ user, theme }), [user, theme])

// ✅✅ разделить контексты: данные отдельно, функции отдельно
&lt;UserContext.Provider value={user}&gt;
  &lt;CartActionsContext.Provider value={actions}&gt;   // actions стабильны</pre>
<p>Разделение по частоте изменений даёт больше всего: тема меняется раз в сессию, корзина — постоянно, и им незачем жить в одном объекте.</p>

<h5>Автоматический батчинг</h5>
<p>React собирает несколько вызовов <code class="i">setState</code> в одно обновление. До 18-й версии это работало только внутри React-обработчиков; в промисах, <code class="i">setTimeout</code> и нативных слушателях каждый вызов давал отдельный рендер. React 18 с <code class="i">createRoot</code> включил батчинг <b>везде</b>.</p>
<pre class="code">const onFetch = async () =&gt; {
  await fetch('/api')
  setA(1)
  setB(2)   // React 17: два рендера. React 18: один
}</pre>
<p>Отказаться точечно можно через <code class="i">flushSync</code>, но это заставляет React рендерить синхронно и сводит на нет весь выигрыш.</p>

<h5>Конкурентность: приоритеты обновлений</h5>
<p>Главное нововведение React 18 — <b>прерываемый рендер</b>. React может приостановить работу над низкоприоритетным обновлением, обработать срочное (ввод текста) и вернуться к прерванному.</p>
<pre class="code">const [isPending, startTransition] = useTransition()

const onChange = (e) =&gt; {
  setQuery(e.target.value)                    // срочно: поле ввода
  startTransition(() =&gt; {
    setResults(filterHugeList(e.target.value)) // несрочно: можно прервать
  })
}</pre>
<p>Здесь ввод остаётся мгновенным, даже если перерисовка списка занимает 200 мс: React бросит незавершённый рендер списка, как только придёт новый символ. <code class="i">useDeferredValue</code> делает то же самое, но «снаружи» — когда обновление инициируете не вы.</p>
<div class="note">Важно понимать, что конкурентность не ускоряет рендер. Она улучшает <b>отзывчивость</b> за счёт приоритизации. Суммарной работы становится даже чуть больше.</div>

<h5>Длинные списки</h5>
<p>10 000 элементов в DOM — это сотни мегабайт памяти и заваленный layout. Никакая мемоизация здесь не поможет: проблема не в React, а в количестве узлов.</p>
<p>Единственное решение — <b>виртуализация</b>: рендерить только видимые элементы плюс небольшой запас. Библиотеки: <code class="i">@tanstack/react-virtual</code>, <code class="i">react-window</code>, <code class="i">react-virtuoso</code>. Дополнительно помогают пагинация, бесконечная подгрузка и CSS-свойство <code class="i">content-visibility: auto</code>.</p>

<h5>Порядок действий, когда тормозит</h5>
<ol>
<li><b>Измерить.</b> React DevTools Profiler с включённым «Record why each component rendered» прямо показывает, какой проп или хук вызвал обновление. Без этого вы гадаете.</li>
<li><b>Опустить состояние ниже.</b> Часто достаточно перенести <code class="i">useState</code> в тот компонент, который им реально пользуется — и половина дерева перестанет обновляться.</li>
<li><b>Разделить компонент</b>, изолировав часто меняющуюся часть.</li>
<li><b>Композиция через children</b> — бесплатно и без memo.</li>
<li><b>memo + стабильные пропсы</b> там, где это оправдано профилировщиком.</li>
<li><b>Виртуализация</b> списков.</li>
<li><b><code class="i">useTransition</code></b> для несрочного.</li>
<li><b>Web Worker</b> для действительно тяжёлых вычислений.</li>
</ol>
<p>И отдельно: часто проблема вообще не в рендере, а в размере бандла или в медленном API. Профилировщик отвечает на этот вопрос за минуту.</p>` }
