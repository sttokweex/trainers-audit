import type { TheoryArticle } from '@/engine/types'

export const browser: TheoryArticle = { id:'th-browser', topic:'Браузер', title:'От ввода URL до пикселей',
  lead:'Сквозная история одной загрузки страницы: DNS, TCP, TLS, HTTP, парсинг, рендеринг — и где на каждом шаге теряется время.',
  body:`
<p>Вопрос «что происходит, когда вы вводите URL» задают не ради зубрёжки. Он проверяет <b>широту</b>: видите ли вы систему целиком или знаете только свой слой. Хорошая тактика — дать каркас за минуту, а потом спросить, какую часть развернуть.</p>

<h5>Вся цепочка</h5>
<pre class="code"> 1. Разбор URL, проверка списка HSTS → возможно принудительный https
 2. DNS: кеш браузера → кеш ОС → резолвер → корень → TLD → авторитетный NS
 3. TCP: SYN → SYN-ACK → ACK                              1 RTT
 4. TLS: ClientHello → ServerHello → общий ключ           1 RTT (TLS 1.3)
 5. HTTP-запрос → балансировщик → приложение → БД → ответ
 6. HTML приходит потоком, парсер начинает работу, не дожидаясь конца
 7. HTML → DOM, параллельно preload scanner качает найденные ресурсы
 8. CSS → CSSOM  (блокирует рендеринг)
    JS           (блокирует парсинг, если без defer/async)
 9. Style → Layout → Paint → Composite
10. Гидратация, обработчики, дозагрузка данных</pre>

<h5>Шаг 2: DNS</h5>
<p>Перевод имени в IP. Ищется по цепочке кешей: браузер → ОС (и <code class="i">/etc/hosts</code>, который выигрывает у всего) → рекурсивный резолвер провайдера или публичный.</p>
<p>Если у резолвера ответа нет, он идёт <b>итеративно</b>: спрашивает корневой сервер → тот отвечает «иди к серверам <code class="i">.com</code>» → TLD-сервер отвечает «иди к <code class="i">ns1.example.com</code>» → авторитетный сервер отдаёт запись.</p>
<div class="note">Различие, которое любят спрашивать: <b>клиент делает рекурсивный запрос</b> (один вопрос — один готовый ответ), а <b>резолвер выполняет итеративные</b> запросы по цепочке. Работу делает резолвер, не ваш браузер.</div>
<p>Основные типы записей: <b>A</b>/<b>AAAA</b> (IPv4/IPv6), <b>CNAME</b> (алиас; нельзя на корне домена), <b>MX</b> (почта), <b>TXT</b> (SPF, DKIM, подтверждение владения), <b>NS</b>, <b>CAA</b> (кто может выпускать сертификаты).</p>
<p><b>TTL</b> определяет, сколько запись живёт в кешах по всему миру. Пока он не истёк, повлиять на резолверы вы не можете — отсюда правило переезда: <b>за сутки снизить TTL до 60–300 секунд</b>, переключить, убедиться, вернуть обратно.</p>
<p>Цена холодного резолва — 20–120 мс <b>до</b> отправки первого байта. Если страница тянет ресурсы с пяти доменов, вы платите это пять раз. Лечится <code class="i">dns-prefetch</code> и <code class="i">preconnect</code>. И ещё: <b>CDN работает через DNS</b> — авторитетный сервер отдаёт разный IP в зависимости от географии клиента.</p>

<h5>Шаги 3–4: TCP и TLS</h5>
<p>TCP-рукопожатие — 1 RTT. Затем TLS, который даёт три вещи: конфиденциальность (трафик зашифрован), целостность (нельзя незаметно подменить) и аутентичность (вы говорите с тем, чьё имя в адресной строке).</p>
<p>В TLS 1.3 рукопожатие укладывается в <b>1 RTT</b>, а при возобновлении сессии — в 0-RTT. Стороны независимо вычисляют общий сеансовый ключ по Диффи-Хеллману, <b>сам ключ по сети не передаётся</b>. Дальше работает быстрое симметричное шифрование.</p>
<p>Сертификат сервера подписан промежуточным удостоверяющим центром, тот — корневым, а корневые лежат в хранилище ОС. Ошибка «сертификат не доверен» почти всегда означает одно из трёх: истёк срок, сервер не отдал <b>промежуточный</b> сертификат, или имя домена не совпадает.</p>
<div class="warn">Частый вопрос с подвохом: шифрует ли HTTPS адрес? Путь и query-параметры — да, они внутри зашифрованного тела. А <b>доменное имя видно</b>: в DNS-запросе и в поле SNI открытым текстом. Плюс утекают размеры и тайминги трафика. Поэтому токен в query — плохая идея независимо от HTTPS: он осядет в логах сервера, в заголовке Referer и в истории браузера.</div>

<h6>TCP подробнее: почему это не просто «три пакета»</h6>
<p>Рукопожатием TCP не ограничивается — оно определяет поведение соединения на всё время его жизни, и это объясняет несколько вещей, которые иначе выглядят как магия.</p>
<p><b>Slow start.</b> Новое TCP-соединение не шлёт данные на полной скорости сразу — оно начинает с маленького «окна перегрузки» (congestion window) и удваивает его на каждом успешном раунде (экспоненциально), пока не начнутся потери пакетов, после чего рост становится линейным. Смысл — нащупать доступную пропускную способность канала, не обвалив его. Практическое следствие: <b>первые несколько килобайт в новом соединении идут медленнее</b>, чем те же байты в уже «прогретом». Именно поэтому переиспользование соединения — <code class="i">Connection: keep-alive</code> в HTTP/1.1, и тем более один общий коннект в HTTP/2 — не просто экономит на рукопожатиях, а избегает повторного slow start на каждый запрос.</p>
<p><b>Гарантия порядка — и её цена.</b> TCP обязан доставить байты <b>в том порядке</b>, в котором их отправили. Если пакет №3 потерялся, а №4 и №5 уже доехали, операционная система придержит их в буфере и не отдаст приложению, пока №3 не будет повторно передан и не займёт своё место. Это называется <b>head-of-line blocking на уровне транспорта</b>.</p>
<div class="key">Вот где это по-настоящему больно: HTTP/2 мультиплексирует множество независимых запросов в <b>одно</b> TCP-соединение. Логически запросы не связаны, но физически они делят один и тот же поток байтов — и потеря одного пакета блокирует <b>все</b> запросы в соединении, пока он не переотправится, даже те, чьи данные давно доехали. Это и есть причина, по которой понадобился HTTP/3.</div>
<p><b>Почему HTTP/3 пересел на UDP.</b> UDP не гарантирует ни порядок, ни доставку — «выстрелил и забыл», зато не блокирует ничего. QUIC (транспорт HTTP/3) строится поверх UDP и <b>сам</b> реализует надёжность и порядок — но <b>отдельно для каждого потока</b>, а не для соединения целиком. Потерянный пакет одного запроса задерживает только его поток; остальные продолжают доезжать и обрабатываться. Ровно это имеется в виду в таблице версий протокола (статья про HTTP и API) строкой «Head-of-line: решено» у HTTP/3.</p>
<p><b>TCP или UDP — общий критерий выбора</b>, который стоит уметь сформулировать не только применительно к HTTP: нужны гарантии доставки и порядка ценой задержки и накладных расходов — берите TCP (веб, API, файлы). Важнее низкая задержка, а потеря отдельного пакета не катастрофична и обрабатывается на уровне приложения — берите UDP (видеозвонки, трансляции, DNS-запросы, игры в реальном времени: устаревший кадр видео бесполезен, ждать его повторной отправки бессмысленно).</p>
<p><b>Nagle's algorithm</b> — TCP по умолчанию может слегка придерживать мелкие пакеты, склеивая их в один, чтобы снизить накладные расходы на служебные заголовки. Для интерактивного трафика с мелкими частыми сообщениями (WebSocket, SSH) это добавляет заметную задержку, поэтому такие соединения включают <code class="i">TCP_NODELAY</code>, отключая склейку ради мгновенной отправки.</p>

<h5>Шаги 6–8: парсинг и что чем блокируется</h5>
<p>HTML парсится <b>потоково</b>: браузер начинает строить DOM, не дожидаясь конца документа. Но два вида ресурсов вмешиваются в процесс.</p>
<ul>
<li><b>CSS блокирует рендеринг.</b> Браузер не покажет ничего, пока не построит CSSOM — иначе пользователь увидел бы вспышку нестилизованного текста. Отсюда практика: критический CSS инлайном, остальное — с атрибутом <code class="i">media</code> или асинхронно.</li>
<li><b>Синхронный script блокирует парсинг HTML</b>, потому что может вызвать <code class="i">document.write</code>. <code class="i">defer</code> — качать параллельно, выполнить после парсинга, в порядке подключения (дефолт для бандлов). <code class="i">async</code> — выполнить сразу по готовности, порядок не гарантирован (для независимой аналитики).</li>
<li><b>Шрифты блокируют отрисовку текста</b> на период до трёх секунд. <code class="i">font-display: swap</code> показывает запасной шрифт сразу ценой скачка вёрстки, <code class="i">optional</code> вообще не ждёт.</li>
</ul>
<p><b>Preload scanner</b> — отдельный механизм, о котором редко знают: пока основной парсер заблокирован скриптом, браузер вторым проходом сканирует документ вперёд и начинает качать найденные ресурсы. Именно поэтому ресурсы, подключаемые через JS, загружаются позже — сканер их не видит.</p>

<h5>Шаг 9: три вида работы рендеринга</h5>
<table>
<tr><th>Этап</th><th>Что делает</th><th>Цена</th><th>Что вызывает</th></tr>
<tr><td><b>Layout</b> (reflow)</td><td>вычисление геометрии</td><td>дорого</td><td>размеры, позиция в потоке, шрифт, добавление узлов</td></tr>
<tr><td><b>Paint</b></td><td>заливка пикселей в слои</td><td>средне</td><td>цвет, фон, тень, border-radius</td></tr>
<tr><td><b>Composite</b></td><td>склейка слоёв на GPU</td><td>дёшево</td><td><code class="i">transform</code>, <code class="i">opacity</code></td></tr>
</table>
<div class="key">Анимируйте только <code class="i">transform</code> и <code class="i">opacity</code>. Всё остальное дёргает главный поток каждый кадр — а на 60 fps у вас всего <b>16.6 мс на кадр</b>, из которых браузеру нужна часть на собственную работу.</div>
<h6>Layout thrashing</h6>
<p>Браузер старается откладывать пересчёт геометрии и делать его пачкой. Но если вы <b>читаете</b> свойство, зависящее от layout, он обязан пересчитать немедленно.</p>
<pre class="code">// ❌ чтение и запись вперемешку → пересчёт на каждой итерации
items.forEach(el =&gt; { el.style.height = el.offsetHeight * 2 + 'px' })

// ✅ сначала все чтения, потом все записи → один пересчёт
const heights = items.map(el =&gt; el.offsetHeight)
items.forEach((el, i) =&gt; { el.style.height = heights[i] * 2 + 'px' })</pre>
<p>Свойства, вызывающие принудительный layout: <code class="i">offsetTop/Width/Height</code>, <code class="i">clientWidth/Height</code>, <code class="i">scrollTop/Height</code>, <code class="i">getBoundingClientRect()</code>, <code class="i">getComputedStyle()</code>.</p>

<div data-demo="render-pipeline"></div>
<h5>Архитектура браузера</h5>
<p>Chrome многопроцессный: <b>browser process</b> (UI, сеть, диск), <b>renderer process</b> — по процессу на сайт (Site Isolation), запущенный в песочнице без доступа к файловой системе, <b>GPU process</b>, отдельные сервисы сети и хранилища.</p>
<p>Отсюда ответ на вопрос «почему зависшая вкладка не роняет остальные»: зависший JS блокирует только свой renderer-процесс. А Site Isolation — это ещё и защита от атак вроде Spectre: данные чужого сайта физически не лежат в вашем адресном пространстве.</p>
<p>Внутри renderer-процесса несколько потоков. <b>Main thread</b> — парсинг, JS, style, layout, paint; именно здесь всё и тормозит. <b>Compositor thread</b> — сборка слоёв и обработка скролла, поэтому страница с зависшим JS часто всё равно скроллится. Плюс потоки растеризации и ваши Web Workers.</p>

<h5>Как V8 выполняет JavaScript</h5>
<ol>
<li><b>Парсер</b> строит AST, откладывая разбор ещё не вызванных функций.</li>
<li><b>Ignition</b> — интерпретатор: сразу генерирует байткод и начинает исполнять.</li>
<li><b>TurboFan</b> — оптимизирующий JIT: «горячие» функции компилируются в машинный код на основе собранных типов.</li>
<li><b>Деоптимизация</b> — если предположение о типах нарушилось, код откатывается к байткоду.</li>
</ol>
<p>Практическое следствие: <b>мономорфный код быстрее</b>. Объекты со стабильной формой позволяют V8 использовать скрытые классы и инлайн-кеши; добавление свойств на лету и массивы с разнотипными элементами эту оптимизацию ломают.</p>
<p>Сборка мусора поколенческая: молодые объекты чистятся часто и быстро, выжившие переезжают в старое пространство, которое обрабатывается реже и с инкрементальными паузами.</p>

<h5>Модель origin</h5>
<p><b>Origin</b> = схема + хост + порт. Это граница почти всего в браузере: хранилищ, доступа к DOM чужого окна, политики CORS. Понимание origin разом объясняет, почему localStorage с http и https разные, почему нельзя дотянуться до содержимого чужого iframe и зачем вообще существует CORS.</p>

<h5>Хранилища</h5>
<table>
<tr><th></th><th>Объём</th><th>Уходит на сервер</th><th>Доступ из JS</th><th>Живёт</th></tr>
<tr><td>Cookie</td><td>~4 КБ</td><td><b>с каждым запросом</b></td><td>нет при HttpOnly</td><td>до Max-Age</td></tr>
<tr><td>localStorage</td><td>~5–10 МБ</td><td>нет</td><td>да, <b>синхронно</b></td><td>до удаления</td></tr>
<tr><td>sessionStorage</td><td>~5–10 МБ</td><td>нет</td><td>да, синхронно</td><td>до закрытия вкладки</td></tr>
<tr><td>IndexedDB</td><td>сотни МБ</td><td>нет</td><td>да, асинхронно</td><td>до удаления</td></tr>
<tr><td>Cache API</td><td>та же квота</td><td>нет</td><td>да, асинхронно</td><td>до удаления</td></tr>
</table>
<p><b>localStorage синхронный</b> — он блокирует главный поток, и сериализация большого объекта при каждом изменении стора даёт заметный лаг. Хранит только строки, а обращение к нему <b>может бросить исключение</b> в приватном режиме — поэтому <code class="i">try/catch</code> обязателен.</p>
<div class="warn">Это не безопасное хранилище. Всё, что лежит в браузере, пользователь может прочитать и изменить. Токены — в <code class="i">HttpOnly</code> куку, права — проверять на сервере.</div>

<h5>Где именно теряется время</h5>
<table>
<tr><th>Этап</th><th>Типичная цена</th><th>Чем лечить</th></tr>
<tr><td>DNS</td><td>0–120 мс</td><td><code class="i">preconnect</code>, меньше доменов</td></tr>
<tr><td>TCP + TLS</td><td>2 RTT</td><td>keep-alive, HTTP/2, CDN ближе к пользователю</td></tr>
<tr><td>TTFB</td><td>от 20 мс до секунд</td><td>индексы, кеш, устранение N+1 — это бэкенд</td></tr>
<tr><td>Загрузка</td><td>зависит от размера</td><td>brotli, code splitting, AVIF/WebP</td></tr>
<tr><td>Парсинг и выполнение JS</td><td>часто главный виновник</td><td>меньше JS, ленивая загрузка, воркеры</td></tr>
<tr><td>Layout / Paint</td><td>мс–десятки мс</td><td>виртуализация, transform вместо геометрии</td></tr>
</table>
<p>В DevTools вкладка Network → Timing раскладывает запрос ровно по этим фазам. Большой <b>TTFB</b> означает «чинить сервер», большой <b>Content Download</b> — «чинить размер», большой <b>Stalled</b> — «слишком много параллельных запросов».</p>

<h5>Core Web Vitals</h5>
<table>
<tr><th>Метрика</th><th>Что измеряет</th><th>Хорошо</th><th>Чем чинить</th></tr>
<tr><td><b>LCP</b></td><td>отрисовка самого крупного видимого элемента</td><td>≤ 2.5 с</td><td>preconnect, приоритет главной картинке, SSR, сжатие</td></tr>
<tr><td><b>INP</b></td><td>отзывчивость: от клика до отрисовки реакции</td><td>≤ 200 мс</td><td>дробить длинные задачи, виртуализация, воркеры</td></tr>
<tr><td><b>CLS</b></td><td>насколько «прыгает» вёрстка</td><td>≤ 0.1</td><td>размеры у картинок, резерв под баннеры, font-display</td></tr>
</table>
<p>Лабораторные замеры (Lighthouse) полезны, но реальные Core Web Vitals собираются с живых пользователей через <code class="i">PerformanceObserver</code> — на быстром ноутбуке с быстрым интернетом цифры всегда красивее правды.</p>` }
