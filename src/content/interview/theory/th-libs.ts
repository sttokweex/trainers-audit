import type { TheoryArticle } from '@/engine/types'

export const libs: TheoryArticle = { id:'th-libs', topic:'Архитектура фронта', title:'Экосистема: какие библиотеки и как их выбирать',
  lead:'Карта инструментов фронтенда и бэкенда, критерии выбора и типичные ошибки при подключении зависимостей.',
  body:`
<h5>Как выбирать библиотеку</h5>
<p>Вопрос «какую библиотеку взять» на собеседовании проверяет не знание названий, а наличие <b>критериев</b>. Вот они:</p>
<ol>
<li><b>Живость.</b> Дата последнего релиза, частота коммитов, число открытых issue и скорость ответа мейнтейнеров. Библиотека с последним релизом два года назад — риск.</li>
<li><b>Размер.</b> Проверьте на bundlephobia: 50 КБ ради одной функции форматирования дат — плохая сделка. Смотрите и на tree-shakeability.</li>
<li><b>Зависимости.</b> Пакет, тянущий за собой тридцать транзитивных зависимостей, — это тридцать потенциальных уязвимостей и конфликтов версий.</li>
<li><b>Типы.</b> Написана на TypeScript или хотя бы имеет качественные <code class="i">@types</code>.</li>
<li><b>Можно ли уйти.</b> Насколько глубоко библиотека проникнет в код и сколько будет стоить замена. Это аргумент в пользу адаптера-обёртки.</li>
<li><b>Нужна ли вообще.</b> Половина мелких утилит сегодня заменяется десятью строками или нативным API.</li>
</ol>
<div class="warn">Помните историю с <code class="i">left-pad</code>: удаление одиннадцатистрочного пакета сломало сборки половине интернета. Каждая зависимость — это доверие чужому коду, который выполнится у вас в CI и на проде. Для мелочей вроде <code class="i">isOdd</code> ответ всегда «напишите сами».</div>

<div data-demo="bundle-size"></div>
<h5>Фронтенд: состояние и данные</h5>
<table>
<tr><th>Задача</th><th>Варианты</th><th>Когда что</th></tr>
<tr><td>Серверные данные</td><td>TanStack Query, RTK Query, SWR</td><td>Query — если Redux не нужен; RTK Query — если Redux уже есть</td></tr>
<tr><td>Клиентское состояние</td><td>Redux Toolkit, Zustand, Jotai, Valtio</td><td>RTK — большие команды и devtools; Zustand — минимализм без boilerplate</td></tr>
<tr><td>Формы</td><td>react-hook-form, Formik</td><td>RHF — неконтролируемые поля, минимум ререндеров; Formik считается устаревающим</td></tr>
<tr><td>Валидация</td><td>zod, yup, valibot</td><td>zod — вывод типов и переиспользование на сервере; valibot легче по весу</td></tr>
<tr><td>Роутинг</td><td>React Router, TanStack Router</td><td>TanStack Router — типобезопасные маршруты и параметры</td></tr>
</table>
<p>Про <b>Zustand</b> стоит знать, даже если вы на Redux: он решает ту же задачу примерно в десять раз меньшим количеством кода и часто упоминается на собеседованиях как современная альтернатива. Redux Toolkit выигрывает на больших проектах за счёт devtools, middleware и предсказуемой структуры.</p>

<h5>Фронтенд: интерфейс</h5>
<ul>
<li><b>Headless-компоненты</b> — Radix UI, Headless UI, Ark UI. Дают поведение, доступность и управление фокусом без стилей. Это доминирующий подход: дизайн ваш, сложная логика (focus trap, навигация стрелками, aria-атрибуты) чужая и проверенная.</li>
<li><b>Готовые наборы</b> — MUI, Ant Design, Mantine. Быстрый старт, но кастомизация под свой дизайн часто оказывается дороже, чем собрать с нуля на headless.</li>
<li><b>shadcn/ui</b> — не библиотека, а набор компонентов, которые <b>копируются в ваш проект</b>. Нет зависимости, полный контроль над кодом. Построен на Radix и Tailwind.</li>
<li><b>Стили</b> — Tailwind (утилиты), CSS Modules (изоляция), vanilla-extract (типобезопасный CSS на этапе сборки). CSS-in-JS с рантаймом (styled-components, emotion) теряет популярность из-за издержек и плохой совместимости с серверными компонентами.</li>
<li><b>Анимации</b> — Motion (бывший Framer Motion) для деклараций, GSAP для сложных таймлайнов, CSS-переходы для простого.</li>
<li><b>Таблицы и списки</b> — TanStack Table (headless), TanStack Virtual или react-window для виртуализации.</li>
<li><b>Графики</b> — Recharts (просто), visx и D3 (гибко и сложно), ECharts (много типов из коробки).</li>
</ul>

<h5>Бэкенд на Node</h5>
<table>
<tr><th>Задача</th><th>Варианты</th><th>Комментарий</th></tr>
<tr><td>Фреймворк</td><td>NestJS, Fastify, Express, Hono</td><td>Nest — структура для больших команд; Fastify — скорость и схемы; Hono — edge и лёгкость</td></tr>
<tr><td>ORM</td><td>Prisma, Drizzle, TypeORM, Sequelize</td><td>Prisma — лучший DX и типы; Drizzle — ближе к SQL, легче; TypeORM — зрелый, но с особенностями</td></tr>
<tr><td>Валидация</td><td>zod, class-validator</td><td>class-validator нативен для Nest и Swagger; zod переиспользуется на клиенте</td></tr>
<tr><td>Очереди</td><td>BullMQ, Kafka, RabbitMQ</td><td>BullMQ поверх Redis покрывает большинство задач; Kafka — когда нужен персистентный лог</td></tr>
<tr><td>Логи</td><td>pino, winston</td><td>pino быстрее и сразу JSON</td></tr>
<tr><td>Тесты</td><td>Vitest, Jest, supertest, testcontainers</td><td>Vitest быстрее и делит конфиг с Vite</td></tr>
</table>
<p>Про <b>Prisma и Drizzle</b> полезно иметь мнение: Prisma даёт отличную типизацию и миграции, но работает через собственный движок и свой язык схемы; Drizzle — тонкий слой над SQL с типами, ближе к «просто SQL, но безопасно». TypeORM в ваших проектах — зрелый выбор, но у него есть известные шероховатости (поведение <code class="i">save</code>, разница <code class="i">take</code> и <code class="i">limit</code>, неочевидности с ленивыми связями).</p>

<h5>Инструменты сборки</h5>
<table>
<tr><th>Инструмент</th><th>Роль</th></tr>
<tr><td><b>Vite</b></td><td>дев-сервер на нативных ESM + сборка через Rollup. Мгновенный старт, потому что в разработке ничего не бандлится</td></tr>
<tr><td><b>esbuild</b></td><td>очень быстрый трансформер на Go; внутри Vite отвечает за пре-бандлинг зависимостей</td></tr>
<tr><td><b>SWC</b></td><td>быстрый компилятор на Rust, замена Babel; используется в <code class="i">@vitejs/plugin-react-swc</code> и в Nest</td></tr>
<tr><td><b>Rollup / Rolldown</b></td><td>бандлер для продакшена и для сборки библиотек</td></tr>
<tr><td><b>Webpack</b></td><td>зрелый, максимум возможностей, Module Federation; медленнее</td></tr>
<tr><td><b>Turborepo / Nx</b></td><td>оркестрация задач и кеш сборки в монорепе</td></tr>
</table>
<p>Ключевая идея Vite, которую стоит уметь объяснить: в режиме разработки браузер сам загружает модули через нативный <code class="i">import</code>, поэтому <b>время старта не зависит от размера проекта</b>. Бандлится только продакшен-сборка — там нужны минификация, tree-shaking и code splitting.</p>

<h5>Что уже есть нативно</h5>
<p>Частый вопрос: «зачем вам lodash?». Хороший ответ — показать, что вы знаете альтернативы:</p>
<pre class="code">_.cloneDeep(obj)      → structuredClone(obj)
_.uniq(arr)           → [...new Set(arr)]
_.groupBy(arr, fn)    → Object.groupBy(arr, fn)
_.flatten(arr)        → arr.flat(Infinity)
_.get(obj, 'a.b.c')   → obj?.a?.b?.c
_.debounce            → 5 строк своими руками
moment()              → Temporal API, date-fns, dayjs
axios                 → fetch + небольшая обёртка
uuid                  → crypto.randomUUID()
classnames            → clsx (в 10 раз меньше)</pre>
<p>Это не значит, что библиотеки не нужны: <code class="i">date-fns</code> для сложной работы с датами и таймзонами оправдан, а <code class="i">moment</code> сегодня — нет (он мутабельный, тяжёлый и в режиме поддержки).</p>

<h5>Качество зависимостей</h5>
<ul>
<li><b>Lock-файл в git</b> — обязательно. <code class="i">pnpm install --frozen-lockfile</code> в CI: сборка должна быть воспроизводимой.</li>
<li><b>Semver</b>: <code class="i">^1.2.3</code> разрешает minor и patch, <code class="i">~1.2.3</code> только patch, <code class="i">1.2.3</code> — точно. Для приложений разумны каретки плюс lock; для публикуемых библиотек — осторожнее.</li>
<li><b>Аудит</b>: <code class="i">pnpm audit</code>, Dependabot или Renovate для автоматических обновлений мелкими порциями. Редкие обновления «раз в год» превращаются в неподъёмный мажорный скачок.</li>
<li><b>Дубли версий</b>: <code class="i">pnpm why react</code> покажет, кто тянет вторую копию. Две копии React — сломанные хуки; две копии зависимости в бандле — лишние килобайты.</li>
<li><b>Peer dependencies</b> нужны именно для того, чтобы библиотека использовала <i>ваш</i> React, а не тащила свой.</li>
</ul>

<h5>Обёртка вокруг чужого кода</h5>
<p>Практический приём, который стоит упомянуть: библиотеку, проникающую глубоко в код, оборачивают в собственный тонкий модуль.</p>
<pre class="code">// shared/api/http.ts — единственное место, знающее про конкретный клиент
export const http = {
  get: &lt;T&gt;(url: string, opts?: Opts) =&gt; client.get&lt;T&gt;(url, opts),
  post: &lt;T&gt;(url: string, body: unknown) =&gt; client.post&lt;T&gt;(url, body),
}</pre>
<p>Тогда смена axios на fetch — правка одного файла, а не трёхсот импортов. Это тот же адаптер, только применённый к зависимости. Оборачивать имеет смысл не всё подряд, а то, что (а) используется повсеместно и (б) может быть заменено.</p>
<div class="key">Хороший ответ на «как вы принимаете решение о новой зависимости»: «смотрю, можно ли обойтись платформой; если нельзя — оцениваю живость, вес и глубину проникновения; если библиотека затрагивает много кода, прячу её за своим интерфейсом. И фиксирую решение в ADR, чтобы через год не спорить заново».</div>` }
