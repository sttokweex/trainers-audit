import type { TheoryArticle } from '@/engine/types'

export const oop: TheoryArticle = { id:'th-oop', topic:'ООП и принципы', title:'ООП, SOLID и паттерны проектирования',
  lead:'Принципы, которые спрашивают на каждом собеседовании уровня middle и выше — с примерами из реального кода, а не из учебника.',
  body:`
<h5>Четыре принципа ООП</h5>
<ul>
<li><b>Инкапсуляция</b> — сокрытие внутреннего состояния за интерфейсом. В TypeScript это <code class="i">private</code>/<code class="i">protected</code> (проверяются только компилятором) и настоящие приватные поля <code class="i">#field</code>. Практический смысл: внутренности можно свободно менять, пока публичный контракт тот же.</li>
<li><b>Наследование</b> — переиспользование через отношение «является». <code class="i">YooKassa</code> <i>является</i> платёжным провайдером.</li>
<li><b>Полиморфизм</b> — один интерфейс, разные реализации. Вызывающий код не знает, с какой именно работает.</li>
<li><b>Абстракция</b> — выделение существенного. Клиент знает «можно оплатить», а не «как формируется подпись запроса к банку».</li>
</ul>

<h5>Композиция вместо наследования</h5>
<p>Это главный современный тезис, и его обязательно стоит произнести. Наследование создаёт <b>жёсткую связь</b>: изменение родителя ломает всех потомков (проблема хрупкого базового класса), а иерархия глубже двух-трёх уровней становится нечитаемой.</p>
<pre class="code">// ❌ наследование ради переиспользования кода
class OrderService extends BaseServiceWithLoggerAndCache {}

// ✅ композиция: собрали из независимых частей
class OrderService {
  constructor(
    private readonly repo: OrderRepository,
    private readonly logger: Logger,
    private readonly cache: CacheService,
  ) {}
}</pre>
<p>Это ровно то, что делает <b>DI в NestJS</b>: зависимости внедряются, а не наследуются. И то же самое во фронтенде — React отказался от классовых миксинов и наследования HOC в пользу <b>хуков</b>, то есть композиции поведения.</p>

<div data-demo="strategy"></div>
<h5>SOLID</h5>
<h6>S — Single Responsibility</h6>
<p>У класса должна быть <b>одна причина для изменения</b>. Формулировка «делает одну вещь» неточна: речь о том, что у класса один заинтересованный в изменениях.</p>
<pre class="code">// ❌ три причины меняться: бизнес-правила, вёрстка письма, схема БД
class UserService {
  register(dto) { /* валидация + SQL + HTML письма */ }
}
// ✅
class UserService {
  constructor(repo: UserRepository, mailer: Mailer, validator: Validator) {}
}</pre>
<p><b>Симптом нарушения:</b> при изменении шаблона письма приходится править сервис пользователей.</p>
<h6>O — Open/Closed</h6>
<p>Открыт для расширения, закрыт для изменения: новое поведение не должно требовать правки существующего кода.</p>
<pre class="code">// ❌ каждый новый провайдер — правка switch
function pay(type, amount) {
  switch (type) { case 'yookassa': …; case 'tbank': … }
}
// ✅ новый провайдер = новый класс, старый код не трогаем
interface PaymentProvider { pay(amount: number): Promise&lt;Receipt&gt; }</pre>
<h6>L — Liskov Substitution</h6>
<p>Потомка можно подставить вместо родителя, не сломав программу. Нельзя усиливать предусловия и ослаблять постусловия.</p>
<pre class="code">// ❌ наследник запрещает то, что родитель разрешал
class ReadOnlyRepo extends Repo {
  save() { throw new Error('не поддерживается') }
}
// ✅ разделить: interface Readable / interface Writable</pre>
<p><b>Симптом:</b> метод наследника бросает «не поддерживается», или вызывающий код проверяет <code class="i">instanceof</code>, чтобы решить, что делать.</p>
<h6>I — Interface Segregation</h6>
<p>Лучше несколько узких интерфейсов, чем один толстый: клиент не должен зависеть от методов, которые не использует. <b>Симптом:</b> реализации с пустыми методами-заглушками.</p>
<h6>D — Dependency Inversion</h6>
<p>Модули верхнего уровня не зависят от нижнего — оба зависят от <b>абстракций</b>.</p>
<pre class="code">// ❌ сервис намертво привязан к конкретной реализации
class OrderService { private mailer = new SmtpMailer() }

// ✅ зависит от контракта, конкретика приходит извне
class OrderService {
  constructor(@Inject(MAILER) private readonly mailer: Mailer) {}
}</pre>
<p><b>Практическая проверка:</b> можно ли протестировать этот класс без сети и базы данных? Если да — DIP соблюдён. Это и есть главный аргумент в пользу DI-контейнера.</p>
<div class="note">Честная оговорка, которую ценят: SOLID — это ориентиры, а не догма. Слепое дробление на двадцать классов-обёрток вредит читаемости не меньше, чем god object. Принципы нужны, чтобы <b>раньше заметить проблему</b>, а не чтобы выполнять ритуал.</div>

<h5>DRY, KISS, YAGNI</h5>
<ul>
<li><b>DRY</b> — каждое <b>знание</b> имеет единственное представление в системе. Речь о знании, а не о совпадающих строчках кода.</li>
<li><b>KISS</b> — простое решение предпочтительнее умного. Код читают в десять раз чаще, чем пишут.</li>
<li><b>YAGNI</b> — не пишите «на будущее». Абстракция, сделанная впрок, почти никогда не подходит под реальное требование, когда оно приходит, но мешает уже сейчас.</li>
<li><b>Закон Деметры</b> — «не разговаривай с незнакомцами»: цепочка <code class="i">order.customer.address.city</code> ломается от любого изменения в глубине.</li>
<li><b>Fail fast</b> — падать сразу и громко (валидация env на старте), а не тихо продолжать с мусором.</li>
</ul>
<h6>Когда DRY вредит</h6>
<p>Об этом обязательно стоит сказать — это отличает зрелого инженера. Ошибка — устранять <b>случайное</b> совпадение кода. Две функции могут выглядеть одинаково сегодня и меняться по разным причинам завтра. Объединив их, вы связали два несвязанных требования: правка для одного клиента ломает другого.</p>
<p>Признак ошибочного DRY — появление флагов внутри «общей» функции: <code class="i">if (isAdmin)</code>, <code class="i">if (mode === 'compact')</code>. Каждый такой параметр говорит, что общность была мнимой.</p>
<p>Контрформулировки: <b>WET</b> («дублируй, пока не увидел третий случай») и слова Сэнди Метц — <b>«дублирование гораздо дешевле неправильной абстракции»</b>.</p>
<p>Практическое правило: дублирование <b>бизнес-правил и данных</b> почти всегда плохо (одна ставка НДС в двух местах — гарантированный баг). Дублирование <b>формы кода</b> часто нормально.</p>

<h5>Связность и связанность</h5>
<table>
<tr><th>Cohesion — внутри модуля</th><th>Coupling — между модулями</th></tr>
<tr><td>нужна <b>высокая</b>: всё работает на одну задачу</td><td>нужна <b>низкая</b>: модули знают минимум друг о друге</td></tr>
<tr><td>плохо: папка <code class="i">utils</code> со ста несвязанными функциями</td><td>плохо: правка в одном модуле ломает пять других</td></tr>
</table>
<div class="key">Если из всего разговора об архитектуре запомнить одно — запомните эту пару. На ней держатся и FSD, и модули Nest, и микросервисы. SOLID — это, по сути, набор конкретных способов повысить связность и понизить связанность.</div>

<h5>Паттерны, которые вы применяете каждый день</h5>
<pre class="code">Стратегия      подмена алгоритма          провайдеры оплаты, способы доставки
Наблюдатель    подписка на события        EventEmitter, socket.io, подписка стора
Фабрика        создание спрятано          useFactory в Nest, createStore
Адаптер        чужой интерфейс → свой     обёртки над SDK MinIO, YooKassa
Декоратор      добавить поведение         интерцепторы Nest, HOC, кеш-обёртка
Фасад          упростить сложное          сервисный слой над репозиториями
Репозиторий    доступ к данным            бизнес-логика не знает про SQL
Цепочка        последовательная обработка middleware, guards, pipes
Команда        действие как объект        Redux-экшены
Прокси         перехват доступа           Immer в RTK, реактивность
Компоновщик    дерево однотипных          React-компоненты, категории
Модуль         приватное + публичное      любой ES-модуль</pre>
<p><b>Как отвечать:</b> худший вариант — перечислить 23 паттерна GoF. Лучший — назвать <b>задачу, которую паттерн решает, и место в вашем коде</b>. Интервьюер проверяет, узнаёте ли вы паттерны в уже написанном коде.</p>
<div class="warn">Синглтон часто называют антипаттерном: он прячет зависимость (её не видно в сигнатуре), мешает тестам (глобальное состояние протекает между кейсами) и плохо живёт в многопоточной среде. В Node его почти всегда заменяет DI-контейнер или обычный экспорт из модуля — модуль и так синглтон.</div>

<h5>Антипаттерны</h5>
<p>God object (класс на две тысячи строк) · спагетти-код · магические числа (<code class="i">if (status === 3)</code> вместо именованной константы) · копипаст-программирование · преждевременная оптимизация · <b>преждевременная абстракция</b> · утечка абстракции (репозиторий возвращает объект ORM с ленивыми связями прямо в контроллер) · глобальное изменяемое состояние · «умный» код, который никто не поймёт через месяц.</p>

<h5>Функциональная сторона</h5>
<p>ООП и ФП не противники — в JavaScript их смешивают постоянно.</p>
<ul>
<li><b>Чистая функция</b>: результат зависит только от аргументов, нет побочных эффектов. Легко тестируется, кешируется, безопасна для повторного вызова. Именно поэтому React требует чистоты рендера: конкурентный рендеринг может прервать и перезапустить компонент.</li>
<li><b>Иммутабельность</b>: сравнение по ссылке за O(1), предсказуемость, time-travel debugging.</li>
<li><b>Функции высшего порядка</b>: <code class="i">map</code>, <code class="i">filter</code>, <code class="i">debounce</code>, HOC.</li>
<li><b>Композиция</b>: <code class="i">pipe(trim, toLowerCase, slugify)</code> вместо вложенных вызовов.</li>
</ul>
<p>Идея ФП не в запрете побочных эффектов — без них программа бесполезна, — а в том, чтобы <b>вытеснить их на края</b>: чистое ядро с бизнес-логикой, эффекты по периметру. Это называют <b>functional core, imperative shell</b>, и произнести это на собеседовании — сильный ход.</p>` }
