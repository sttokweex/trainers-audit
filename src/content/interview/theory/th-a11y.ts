import type { TheoryArticle } from '@/engine/types'

export const a11y: TheoryArticle = { id:'th-a11y', topic:'UX и доступность', title:'Доступность и качество интерфейса',
  lead:'Семантика, клавиатура, ARIA, состояния экрана и проектирование форм — практический минимум, который заметно отличает уровень.',
  body:`
<h5>Почему доступность спрашивают</h5>
<p>Три причины. Первая — законодательные требования (госсектор, крупные компании, европейский рынок). Вторая — доступная разметка лучше индексируется поисковиками и <b>проще тестируется</b>. Третья, главная для собеседования: понимание a11y обычно означает, что разработчик знает платформу, а не только React.</p>

<h5>Семантика — фундамент</h5>
<pre class="code">&lt;!-- ❌ --&gt;
&lt;div class="btn" onclick="submit()"&gt;Отправить&lt;/div&gt;
&lt;div class="input"&gt;&lt;div&gt;Email&lt;/div&gt;&lt;input type="text"&gt;&lt;/div&gt;

&lt;!-- ✅ --&gt;
&lt;button type="submit"&gt;Отправить&lt;/button&gt;
&lt;label for="email"&gt;Email&lt;/label&gt;
&lt;input id="email" type="email" autocomplete="email" required&gt;</pre>
<p>Нативные элементы дают <b>бесплатно</b>: попадание в порядок обхода Tab, обработку Enter и Space, роль для скринридера, состояние <code class="i">disabled</code>, участие в форме и отправку по Enter. <code class="i">&lt;div onClick&gt;</code> не даёт ничего — придётся руками добавлять <code class="i">tabindex</code>, <code class="i">role</code> и обработчики клавиш, и вы всё равно что-нибудь забудете.</p>
<div class="key">Первое правило ARIA — <b>не использовать ARIA</b>, если есть подходящий HTML-элемент. ARIA только описывает семантику, но не добавляет поведения: <code class="i">role="button"</code> не заставит элемент реагировать на Enter.</div>
<p>Структура документа: <code class="i">header</code>, <code class="i">nav</code>, <code class="i">main</code> (один на страницу), <code class="i">article</code>, <code class="i">aside</code>, <code class="i">footer</code>; заголовки <code class="i">h1–h6</code> без пропуска уровней — по ним пользователи скринридеров навигируют, как по оглавлению.</p>

<h5>Клавиатура</h5>
<ul>
<li>Всё интерактивное достижимо по Tab, порядок логичный. <code class="i">tabindex</code> только <code class="i">0</code> и <code class="i">-1</code>: положительные значения ломают естественный порядок.</li>
<li><b>Видимый фокус обязателен.</b> <code class="i">outline: none</code> без замены — худшее, что можно сделать. Современно: <code class="i">:focus-visible</code> — фокус виден при работе с клавиатуры и не мешает при клике мышью.</li>
<li><b>Модалка</b>: фокус переводится внутрь, запирается (focus trap), Esc закрывает, после закрытия фокус возвращается на кнопку-открыватель.</li>
<li>Скрытая ссылка «Перейти к содержимому» первой в DOM — экономит людям с клавиатурой десятки нажатий.</li>
</ul>
<p>Практическая проверка занимает пять минут: пройдите свою фичу только с клавиатуры. Если где-то потеряли фокус или не смогли что-то нажать — там баг.</p>

<h5>ARIA там, где нативного не хватает</h5>
<pre class="code">&lt;button aria-expanded="false" aria-controls="menu"&gt;Меню&lt;/button&gt;
&lt;div id="menu" role="menu" hidden&gt;…&lt;/div&gt;

&lt;div role="status" aria-live="polite"&gt;Сохранено&lt;/div&gt;    &lt;!-- не перебивает --&gt;
&lt;div role="alert" aria-live="assertive"&gt;Ошибка&lt;/div&gt;      &lt;!-- перебивает --&gt;

&lt;input aria-invalid="true" aria-describedby="err"&gt;
&lt;span id="err"&gt;Неверный формат email&lt;/span&gt;

&lt;button aria-label="Закрыть"&gt;&lt;IconX aria-hidden="true" /&gt;&lt;/button&gt;</pre>
<p><code class="i">aria-live</code> критичен именно для SPA: без перезагрузки страницы скринридер не узнает, что контент сменился. Тосты, результаты поиска и ошибки формы должны объявляться.</p>
<pre class="code">/* видно только скринридеру */
.sr-only {
  position: absolute; width: 1px; height: 1px;
  padding: 0; margin: -1px; overflow: hidden;
  clip: rect(0 0 0 0); white-space: nowrap; border: 0;
}</pre>

<h5>Контраст и визуальная подача</h5>
<ul>
<li>WCAG AA: <b>4.5:1</b> для обычного текста, <b>3:1</b> для крупного и для границ элементов управления.</li>
<li><b>Не передавайте смысл только цветом.</b> К красной рамке добавьте текст ошибки и иконку — около 8% мужчин имеют нарушения цветовосприятия.</li>
<li>Текст должен масштабироваться до 200% без поломки вёрстки: <code class="i">rem</code> вместо фиксированных <code class="i">px</code> для шрифтов.</li>
<li><code class="i">prefers-reduced-motion</code> — уважать настройку: у части людей анимации вызывают физический дискомфорт.</li>
</ul>
<p>Инструменты: <b>axe DevTools</b> и Lighthouse ловят 30–50% проблем автоматически, <code class="i">eslint-plugin-jsx-a11y</code> подсказывает прямо в редакторе, <code class="i">jest-axe</code> проверяет в тестах. Остальное — руками.</p>

<div data-demo="contrast"></div>
<h5>Пять состояний любого экрана</h5>
<ol>
<li><b>Загрузка</b> — данные идут.</li>
<li><b>Пусто</b> — запрос успешен, но данных нет.</li>
<li><b>Ошибка</b> — не удалось загрузить.</li>
<li><b>Частично</b> — часть данных есть, часть грузится или сломалась.</li>
<li><b>Успех</b> — то единственное, что обычно нарисовано в макете.</li>
</ol>
<p>Назвать этот список на собеседовании — сильный ход: он показывает продуктовое мышление, а не только «сверстаю по макету».</p>
<h6>Загрузка</h6>
<ul>
<li><b>Скелетоны вместо спиннеров</b>: сохраняют структуру, не дают скачка вёрстки и субъективно ощущаются быстрее.</li>
<li>Для действий короче 300 мс индикатор лучше не показывать — мелькание раздражает сильнее ожидания.</li>
<li>При <b>повторной</b> загрузке не заменяйте контент скелетоном: покажите ненавязчивый индикатор. Это ровно разница <code class="i">isPending</code> и <code class="i">isFetching</code> в React Query.</li>
<li>Кнопку на время отправки — в <code class="i">disabled</code>, иначе двойной сабмит и два заказа.</li>
</ul>
<h6>Пусто и ошибка</h6>
<p>Пустое состояние — не «Нет данных», а объяснение и следующий шаг: «У вас пока нет задач. Создайте первую». И различайте «пусто вообще» и «пусто по фильтру» — во втором случае предложите сбросить фильтр.</p>
<p>Ошибки — человеческим языком, без кодов: не «Error 500», а «Не удалось загрузить задачи. Попробуйте ещё раз». <b>Всегда давайте действие</b>: «Повторить», «Вернуться». Ошибки формы — рядом с полем, фокус на первое проблемное. И <b>никогда не теряйте введённые данные</b> при ошибке отправки.</p>
<h6>Цифры отзывчивости</h6>
<table>
<tr><th>До 100 мс</th><th>До 300 мс</th><th>До 1 с</th><th>Больше 1 с</th></tr>
<tr><td>ощущается мгновенным</td><td>заметно, но комфортно</td><td>нужен индикатор</td><td>нужен прогресс и возможность отменить</td></tr>
</table>

<div data-demo="ui-states"></div>
<h5>Проектирование форм</h5>
<p><b>Когда валидировать</b> — главный вопрос, и неправильный ответ заметен сразу:</p>
<ul>
<li><b>Не на каждое нажатие клавиши.</b> Ругать за «неполный email», пока человек его печатает, — самая частая ошибка.</li>
<li><b>По уходу с поля</b> — первая проверка.</li>
<li><b>По изменению — только после того</b>, как ошибка уже показана: человек исправляет и сразу видит, что стало хорошо.</li>
<li><b>По отправке</b> — обязательная финальная проверка с фокусом на первое проблемное поле.</li>
</ul>
<p>Эта стратегия называется <code class="i">mode: 'onTouched'</code> в react-hook-form и считается разумным дефолтом.</p>
<p>Остальные правила: <b>label всегда</b> (placeholder исчезает при вводе и не читается как подпись), ошибка под полем текстом плюс иконкой, требования к паролю <b>до</b> ввода, одна колонка, минимум полей — каждое лишнее снижает конверсию.</p>
<pre class="code">&lt;input
  type="email"
  inputmode="email"          /* правильная клавиатура на мобильном */
  autocomplete="email"       /* автозаполнение резко ускоряет ввод */
  aria-invalid={!!error}
  aria-describedby={error ? 'email-error' : undefined}
/&gt;
{error &amp;&amp; &lt;span id="email-error" role="alert"&gt;{error}&lt;/span&gt;}</pre>
<p>На мобильных ещё: <code class="i">font-size</code> у инпута не меньше 16px (иначе iOS зумит страницу при фокусе), цель нажатия не меньше 44×44 px, <code class="i">autocomplete="one-time-code"</code> для СМС-кода.</p>
<div class="warn">Защита от двойной отправки нужна на трёх уровнях, потому что первые два обходятся: <code class="i">disabled</code> на кнопке, блокировка повторной мутации на клиенте и <b>Idempotency-Key на сервере</b>.</div>

<h5>Оптимистичный UI и паттерн «Отменить»</h5>
<p>Оптимистичное обновление уместно там, где успех вероятен, а откат дёшев: лайк, отметка «выполнено», удаление из списка. Не применяйте к платежам и необратимым действиям.</p>
<p>И приём, который стоит знать: <b>«Отменить» вместо «Вы уверены?»</b>. Вместо диалога подтверждения удалите сразу и покажите тост «Удалено — Отменить» на пять секунд. Меньше трения в 99% случаев и полная безопасность в оставшемся проценте.</p>` }
