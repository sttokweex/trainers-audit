import type { TheoryArticle } from '@/engine/types'

export const realtime: TheoryArticle = { id:'th-realtime', topic:'API и сеть', title:'WebSocket, реалтайм и подходы к API: REST, GraphQL, tRPC',
  lead:'Как устроен WebSocket под капотом, чем Socket.IO лучше голого протокола, что выбрать для реалтайма — и как REST, GraphQL и tRPC решают одну и ту же задачу по-разному.',
  body:`
<h5>WebSocket: постоянное соединение вместо запрос-ответа</h5>
<p>HTTP — это всегда клиент спрашивает, сервер отвечает; сервер не может «просто взять и прислать» что-то сам. WebSocket переворачивает модель: после одного рукопожатия открывается <b>двунаправленный</b> канал, и обе стороны шлют сообщения когда угодно, без нового запроса на каждое.</p>
<pre class="code">GET /socket HTTP/1.1
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==

HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=</pre>
<p>Механика простая: обычный HTTP-запрос с заголовком <code class="i">Upgrade: websocket</code> и ответом <b>101</b> — «переключаю протокол». После этого то же TCP-соединение уже говорит не HTTP, а бинарным WebSocket-фреймовым протоколом. Из этого следует важная деталь: WebSocket <b>не подвержен CORS</b> в привычном смысле (это не fetch), но подвержен схожей проверке — сервер должен явно проверять заголовок <code class="i">Origin</code> при рукопожатии сам, иначе соединение может открыть кто угодно с чужого сайта.</p>

<h5>Socket.IO — не «просто WebSocket», а протокол поверх него</h5>
<p>Голый WebSocket API браузера — низкоуровневый: нет автопереподключения, нет комнат, нет fallback, если где-то по дороге прокси режет апгрейд протокола. Socket.IO добавляет прикладной слой поверх:</p>
<table>
<tr><th>Возможность</th><th>Зачем</th></tr>
<tr><td><b>Автопереподключение с backoff</b></td><td>мобильная сеть моргает постоянно — реконнект из коробки, с экспоненциальной задержкой между попытками</td></tr>
<tr><td><b>Fallback на long polling</b></td><td>если апгрейд до WebSocket не прошёл (старый прокси, антивирус) — работает, просто чуть медленнее</td></tr>
<tr><td><b>Комнаты (rooms)</b></td><td>сгруппировать сокеты и разослать событие только им: <code class="i">io.to('game:42').emit(...)</code> — без ручного перебора соединений</td></tr>
<tr><td><b>Неймспейсы</b></td><td>логическое разделение одного физического соединения: <code class="i">/chat</code>, <code class="i">/notifications</code> — разные обработчики, один порт</td></tr>
<tr><td><b>Подтверждения (ack)</b></td><td><code class="i">socket.emit('event', data, (response) =&gt; {...})</code> — request-response поверх событийной модели, когда он всё же нужен</td></tr>
</table>

<h5>Масштабирование: почему без адаптера сломается на втором инстансе</h5>
<div class="warn">WebSocket-соединение <b>привязано к конкретному процессу</b>, который его принял. Если у вас два инстанса за балансировщиком и пользователь A подключён к инстансу 1, а событие для него сгенерировано на инстансе 2 (например, действие другого игрока в реальном времени), инстанс 2 понятия не имеет о соединении пользователя A — событие просто потеряется.</p></div>
<p>Решение — <b>redis-adapter</b>: каждый инстанс подписывается на Pub/Sub канал в Redis (см. статью про кеш и очереди, раздел Pub/Sub). <code class="i">io.to('room').emit(...)</code> публикует сообщение в Redis, все инстансы его получают, и каждый рассылает своим локально подключённым сокетам, входящим в комнату.</p>
<pre class="code">import { createAdapter } from '@socket.io/redis-adapter'
import { createClient } from 'redis'

const pubClient = createClient({ url: process.env.REDIS_URL })
const subClient = pubClient.duplicate()
io.adapter(createAdapter(pubClient, subClient))
// теперь io.to('leaderboard:weekly').emit('update', data)
// долетит до сокета в любом инстансе, а не только в текущем</pre>
<p>Именно так устроены реалтайм-лидерборды при нескольких инстансах бэкенда: без адаптера они работали бы только для пользователей, случайно попавших на тот же процесс, что и источник события.</p>

<h5>Переподключение и восстановление состояния</h5>
<p>Реконнект — это не просто «открыть сокет заново». За время разрыва клиент мог пропустить события. Два подхода:</p>
<ul>
<li><b>Снимок + дельты</b>: при (пере)подключении клиент запрашивает текущее полное состояние (REST-эндпоинтом или начальным событием), а дальше применяет только новые события поверх него.</li>
<li><b>Connection state recovery</b> (Socket.IO 4.6+): сервер на короткое время помнит буфер последних событий по <code class="i">socket.id</code>, и при быстром реконнекте (например, экран телефона заблокировали на 20 секунд) клиент дополучает пропущенное автоматически.</li>
</ul>
<div class="key">Ключевой архитектурный вывод: реалтайм-событие почти никогда не должно быть <b>единственным</b> источником истины на клиенте. Всегда должен быть способ восстановить актуальное состояние с нуля (REST-запрос при монтировании/реконнекте) — иначе один пропущенный по сети пакет тихо рассинхронизирует интерфейс, и это крайне сложно диагностировать постфактум.</div>

<h5>WebSocket, SSE или long polling — таблица выбора</h5>
<table>
<tr><th></th><th>WebSocket</th><th>SSE (Server-Sent Events)</th><th>Long polling</th></tr>
<tr><td>Направление</td><td>двунаправленное</td><td>только сервер → клиент</td><td>эмуляция через повторные запросы</td></tr>
<tr><td>Протокол</td><td>отдельный, поверх TCP</td><td>обычный HTTP, <code class="i">text/event-stream</code></td><td>обычный HTTP</td></tr>
<tr><td>Автопереподключение</td><td>нужно писать самому (или Socket.IO)</td><td><b>встроено в браузер</b> (<code class="i">EventSource</code>)</td><td>вручную</td></tr>
<tr><td>Проходит через корпоративные прокси</td><td>иногда режут апгрейд</td><td>да, это просто HTTP</td><td>да</td></tr>
<tr><td>Когда брать</td><td>чат, игры, совместное редактирование — нужен клиент → сервер</td><td>уведомления, прогресс-бар, лента событий — только сервер → клиент</td><td>legacy-окружения без поддержки первых двух</td></tr>
</table>
<p>Частая ошибка выбора — тянуть WebSocket туда, где нужен только поток от сервера (прогресс генерации отчёта, уведомления). SSE проще (это просто HTTP-ответ, который не закрывается), не требует отдельной инфраструктуры вроде redis-adapter для одного направления и переподключается сам средствами браузера.</p>

<h5>REST, GraphQL, tRPC — одна задача, три разных компромисса</h5>
<p>Все три решают одно: как клиенту получить данные с сервера типобезопасно и без лишнего трафика. Разница — где происходит основной компромисс.</p>
<table>
<tr><th></th><th>REST</th><th>GraphQL</th><th>tRPC</th></tr>
<tr><td>Форма контракта</td><td>набор эндпоинтов, фиксированная форма ответа</td><td>схема (SDL), клиент сам описывает, какие поля нужны</td><td>обычные TypeScript-функции на сервере</td></tr>
<tr><td>Типобезопасность</td><td>нужен отдельный слой: OpenAPI-генерация или ручные типы</td><td>codegen из схемы</td><td><b>сквозная, без codegen</b> — типы выводятся напрямую из серверного кода</td></tr>
<tr><td>Недобор/перебор данных</td><td>частая проблема: либо лишние поля, либо новый эндпоинт под каждый экран</td><td>решена по определению — клиент запрашивает ровно то, что нужно</td><td>решена иначе: любая новая процедура — это просто новая функция, дёшево добавить узкую</td></tr>
<tr><td>Кешируемость HTTP</td><td>естественная (GET + <code class="i">Cache-Control</code>, см. статью про HTTP)</td><td>сложнее — обычно один <code class="i">POST /graphql</code> эндпоинт</td><td>обычно тоже один транспорт, но кешируется на уровне клиента (React Query поверх)</td></tr>
<tr><td>Независимость клиента и сервера</td><td>полная — любой язык на любом конце</td><td>полная</td><td><b>только TypeScript-монорепа</b> — типы шарятся импортом, а не по сети</td></tr>
<tr><td>Когда естественный выбор</td><td>публичный API, мобильные клиенты сторонних команд</td><td>много разных фронтендов с разными потребностями в данных с одного бэкенда</td><td>монорепа, один и тот же TypeScript-стек на клиенте и сервере</td></tr>
</table>
<p>Ключевая идея tRPC, которую стоит уметь объяснить: типобезопасность достигается <b>без сети и без генерации</b> — серверная процедура импортируется на клиенте как <i>тип</i> (не как код, компилятор вырезает всё, кроме сигнатур), поэтому автодополнение и проверка типов работают сразу, а рассинхронизация клиента и сервера физически невозможна — это одна и та же кодовая база.</p>
<pre class="code">// сервер
export const appRouter = router({
  getOrder: publicProcedure
    .input(z.object({ id: z.number() }))
    .query(({ input }) =&gt; ordersService.findOne(input.id)),
})
export type AppRouter = typeof appRouter   // ЭКСПОРТИРУЕТСЯ ТОЛЬКО ТИП

// клиент — ноль ручной типизации, всё выведено
const order = await trpc.getOrder.query({ id: 42 })
//    ^ тип уже Order, а не any</pre>
<div class="note">Обратная сторона: как только на другом конце оказывается не TypeScript-клиент из той же монорепы (мобильное приложение на Kotlin, партнёрская интеграция), преимущество tRPC исчезает — там снова нужен REST или GraphQL с явной схемой, понятной любому языку.</div>

<h5>GraphQL: N+1 и решение через DataLoader</h5>
<p>У GraphQL есть своя версия проблемы N+1 из статьи про базы данных: резолвер поля <code class="i">author</code> для списка из 50 постов наивно даст 50 отдельных запросов. <b>DataLoader</b> решает это батчингом и дедупликацией в пределах одного тика event loop: все запросы <code class="i">.load(id)</code>, вызванные синхронно, собираются в один batch-вызов с массивом id.</p>
<pre class="code">const userLoader = new DataLoader(async (ids: number[]) =&gt; {
  const users = await userRepo.find({ where: { id: In(ids) } })
  const byId = new Map(users.map(u =&gt; [u.id, u]))
  return ids.map(id =&gt; byId.get(id))   // порядок ответа ОБЯЗАН совпадать с порядком ids
})
// 50 вызовов author.load(id) внутри резолверов → один SQL-запрос с IN</pre>
<p>Требование «порядок ответа совпадает с порядком запроса» — не техническая деталь, а контракт DataLoader: он сопоставляет i-й элемент результата с i-м id из батча.</p>` }
