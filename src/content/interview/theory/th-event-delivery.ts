import type { TheoryArticle } from '@/engine/types'

export const eventDelivery: TheoryArticle = {
  id: 'th-event-delivery', topic: 'Кеш и очереди',
  title: 'Доставка событий: Kafka, повторы и идемпотентность',
  lead: 'Как не терять события, не выполнять их опасно дважды и объяснить компромиссы очереди на собеседовании.',
  body: `
<p>Представьте сервис заказов: он сохранил покупку, но процесс упал прежде, чем отправил письмо. Если письмо нельзя потерять, работа должна пережить перезапуск приложения. Для этого событие помещают в долговечное хранилище, а отдельный обработчик забирает его позже. Очередь отделяет «заказ принят» от «письмо отправлено» и сглаживает пики нагрузки.</p>

<h5>Очередь — это сохранённая работа</h5>
<p>У события обычно есть <b>идентификатор</b>, тип, время создания, версия схемы и полезные данные. Продюсер публикует его, брокер сохраняет, а consumer обрабатывает. В Kafka события лежат в логах топика; разные consumer group читают один топик независимо. Внутри одной группы партиция в каждый момент назначена одному consumer.</p>
<p>Партиции позволяют читать параллельно, но порядок гарантируется только <b>внутри одной партиции</b>. Если события одного заказа должны идти по очереди, используйте orderId как ключ партиционирования. Слишком мало партиций ограничивает параллелизм, а перекошенные ключи создают «горячую» партицию.</p>

<h5>Подтверждение и повторная доставка</h5>
<p>Consumer периодически сохраняет offset — позицию, до которой группа обработала журнал. Если записать offset <b>до</b> побочного эффекта, падение может потерять работу. Если записать <b>после</b> эффекта, между этими действиями процесс может упасть и получить событие повторно.</p>
<p>Поэтому обычная система выбирает доставку <b>как минимум один раз</b> (at-least-once): возможен повтор, зато работа не исчезает молча. «Ровно один раз» звучит заманчиво, но обещание ограничено конкретными границами транзакции брокера. Если consumer ещё пишет в отдельную базу или вызывает платёжный API, атомарности между всеми системами автоматически нет.</p>
<div class="note"><b>Главное правило:</b> проектируйте обработчик так, чтобы повтор того же события не повторил опасный бизнес-эффект.</div>

<h5>Идемпотентность — безопасный повтор</h5>
<p>Операция идемпотентна, если одинаковый запрос можно повторить, а итоговый эффект останется тем же. Для платежа клиент передаёт устойчивый <code class="i">idempotencyKey</code>. Сервер сохраняет ключ и результат в той же транзакции, что и запись платежа. При повторе он возвращает прежний результат, не списывая деньги снова.</p>
<p>Для событий часто используют таблицу обработанных ID (inbox/deduplication). Уникальное ограничение на <code class="i">eventId</code> делает проверку конкурентно-безопасной. Просто «сначала проверить, потом вставить» без транзакции опасно: два worker-а могут одновременно не найти запись и выполнить эффект оба.</p>

<h5>Почему возникает Transactional Outbox</h5>
<p>Наивный код сначала сохраняет заказ в БД, потом публикует событие. Между двумя действиями приложение может упасть. Если публиковать сначала, обратная проблема: событие о заказе есть, а транзакция заказа откатилась.</p>
<p><b>Outbox</b> решает разрыв одной локальной транзакцией: заказ и запись «надо опубликовать событие» сохраняются вместе в БД. Фоновый relay читает outbox, отправляет событие в брокер и помечает запись опубликованной. Relay тоже может повторить публикацию, поэтому consumer всё равно нужен идемпотентный. Это надёжная доставка, а не магия «ровно один раз».</p>

<h5>Повторы, backoff и DLQ</h5>
<p>Временный сетевой сбой можно повторить с растущей задержкой и случайным разбросом (exponential backoff + jitter). Задайте предел попыток, общий deadline и классифицируйте ошибки: повторять неверный JSON бесполезно, а повтор временного 503 может помочь. Иначе тысячи consumer-ов синхронно повторят запрос и усилят перегрузку.</p>
<p>После исчерпания попыток сообщение направляют в <b>dead-letter queue</b> (DLQ): там сохраняют причину и исходный payload, поднимают алерт и исправляют проблему до повторного запуска. DLQ — не мусорная корзина: нужен владелец, срок хранения, защита персональных данных и безопасный способ replay.</p>

<h5>Как рассуждать на собеседовании</h5>
<ol><li>Назовите бизнес-инвариант: что нельзя потерять или сделать дважды?</li><li>Опишите точку сохранения события, ключ партиции и кто читает его независимо.</li><li>Разберите падение между записью в БД, внешним эффектом и commit offset.</li><li>Добавьте outbox/inbox, идемпотентный ключ, ограниченные повторы и DLQ по необходимости.</li><li>Назовите метрики: lag группы, возраст старейшего сообщения, число повторов и размер DLQ.</li></ol>
<div data-demo="event-delivery"></div>
<p>Не выбирайте Kafka только потому, что она популярна. Если достаточно простой фоновой очереди с задержками и подтверждениями, более компактный брокер может быть дешевле в эксплуатации. Начинайте с гарантий и нагрузки, затем выбирайте инструмент.</p>`,
}
