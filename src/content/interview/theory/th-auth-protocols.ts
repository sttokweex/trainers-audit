import type { TheoryArticle } from '@/engine/types'

export const authProtocols: TheoryArticle = {
  id: 'th-auth-protocols', topic: 'Безопасность',
  title: 'OAuth 2.0, OpenID Connect и passkeys без путаницы',
  lead: 'Кто подтверждает личность, кто выдаёт доступ и от каких атак защищают state, nonce и PKCE.',
  body: `
<p>OAuth 2.0 отвечает на вопрос <b>«какому приложению разрешён доступ к ресурсу?»</b> OpenID Connect (OIDC) добавляет поверх OAuth способ сообщить приложению, <b>кто вошёл</b>. Поэтому OIDC — обычная основа кнопки «Войти через…», а не синоним любого JWT.</p>

<h5>Участники входа</h5>
<ul><li><b>Пользователь</b> проходит проверку у провайдера (authorization server).</li><li><b>Клиент</b> — ваше приложение, которое просит войти или получить ограниченные права.</li><li><b>Resource server</b> проверяет access token перед выдачей защищённых данных.</li></ul>
<p>В Authorization Code flow браузер уходит на страницу провайдера, пользователь подтверждает вход, а провайдер возвращает короткоживущий <b>authorization code</b>. Клиент обменивает code на токены. В SPA и мобильном приложении используйте <b>PKCE</b>: клиент создаёт случайный <code class="i">code_verifier</code>, отправляет его хеш как <code class="i">code_challenge</code>, а при обмене доказывает владение исходным значением. Перехваченный code без verifier бесполезен.</p>

<h5>State, nonce и PKCE — разные проверки</h5>
<ul><li><code class="i">state</code> связывает возврат браузера с начатым именно этим приложением запросом и защищает callback от подмены/CSRF. Генерируйте непредсказуемое значение, храните его кратковременно и сравнивайте при возврате.</li><li><code class="i">nonce</code> в OIDC связывает ID token с конкретной попыткой входа и мешает повторно подставить старый токен.</li><li><code class="i">PKCE</code> связывает authorization code с клиентом, который начал поток, и защищает код от перехвата.</li></ul>
<p>Они дополняют друг друга. Использование одного не делает остальные ненужными. Помимо этих значений проверьте подпись ID token, <code class="i">iss</code>, <code class="i">aud</code>, срок действия и точный redirect URI.</p>

<h5>Где хранить токены</h5>
<p>Долгоживущий токен в <code class="i">localStorage</code> доступен любому скрипту на странице: успешная XSS-атака может его прочитать и унести. <code class="i">HttpOnly</code>-cookie недоступна JavaScript, но браузер отправляет cookie автоматически, поэтому для изменяющих состояние запросов нужны защита от CSRF и подходящие <code class="i">SameSite</code>/<code class="i">Secure</code> флаги.</p>
<p>Для чувствительного веб-приложения часто выбирают <b>BFF</b> (backend for frontend): браузер хранит только защищённую сессию в HttpOnly-cookie, а сервер держит и обновляет OAuth токены. Это уменьшает последствия кражи токена из JavaScript, но добавляет серверную сессию и требует защитить cookie от CSRF. Универсального ответа «всегда localStorage» или «cookie решает всё» нет — объясните модель угроз.</p>

<h5>Passkeys: вход без общего пароля</h5>
<p>Passkey использует WebAuthn и криптографическую пару ключей. Устройство создаёт закрытый ключ, который остаётся у пользователя, и открытый ключ, который сервер сохраняет. При входе сервер выдаёт одноразовый challenge, устройство подписывает его, а сервер проверяет подпись и привязку к сайту. Секрет не отправляется на сервер, поэтому поддельный похожий домен не получает пароль для повторного использования.</p>
<p>Passkey привязан к origin (сайт + схема + домен + порт), а не только к картинке в адресной строке. Нужны сценарии восстановления и добавления нового устройства: иначе потеря телефона превращается в блокировку аккаунта. Важно различать синхронизируемые passkeys и аппаратные ключи организации: у них разные требования к восстановлению и управлению.</p>

<h5>Как ответить коротко и точно</h5>
<p>«OIDC подтверждает пользователя, OAuth выдаёт ограниченный доступ. В браузерном code flow использую PKCE, случайный state и OIDC nonce; callback сверяю, токен валидирую по issuer, audience, подписи и сроку. Выбор cookie или BFF зависит от угроз XSS/CSRF. Passkey заменяет общий пароль парой ключей и проверкой challenge, связанной с origin».</p>
<div data-demo="auth-flow"></div>
<div class="warn"><b>Не путайте:</b> JWT — формат токена, OAuth/OIDC — протоколы. Сам факт, что строка выглядит как JWT, ещё не доказывает, что она проверена.</div>`,
}
