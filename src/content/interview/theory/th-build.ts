import type { TheoryArticle } from '@/engine/types'

export const build: TheoryArticle = { id:'th-build', topic:'Браузер', title:'Сборка, модули и оптимизация бандла',
  lead:'Как работают бандлеры, что такое tree-shaking и code splitting, и как реально уменьшить вес приложения.',
  body:`
<h5>Зачем вообще бандлер</h5>
<p>Исторически — чтобы собрать сотни модулей в несколько файлов (в HTTP/1.1 каждый файл стоил соединения) и превратить современный синтаксис в понятный старым браузерам. Сегодня главные задачи другие: <b>минификация, tree-shaking, code splitting, обработка ассетов и подстановка переменных окружения</b>.</p>

<h5>ESM против CommonJS</h5>
<table>
<tr><th></th><th>CommonJS (<code class="i">require</code>)</th><th>ESM (<code class="i">import</code>)</th></tr>
<tr><td>Разрешение</td><td>в рантайме, динамически</td><td>статически, на этапе парсинга</td></tr>
<tr><td>Загрузка</td><td>синхронная</td><td>асинхронная</td></tr>
<tr><td>Экспорт</td><td>копия значения</td><td>живая привязка</td></tr>
<tr><td>Tree-shaking</td><td>практически невозможен</td><td>да</td></tr>
<tr><td>Top-level await</td><td>нет</td><td>да</td></tr>
</table>
<p>Ключ к пониманию tree-shaking именно здесь: <code class="i">import</code> <b>статически анализируем</b> — бандлер видит, какие именно экспорты используются, ещё до выполнения кода. <code class="i">require()</code> можно вызвать с вычисляемым путём внутри условия, и предсказать это невозможно.</p>
<p>Отсюда же и то, почему в ваших проектах клиент на ESM, а сервер на CommonJS: Nest и TypeORM опираются на декораторы и <code class="i">reflect-metadata</code>, и экосистема исторически собирается в CJS. На клиенте без ESM не было бы ни tree-shaking, ни нативных модулей в дев-режиме Vite.</p>

<h5>Tree-shaking: почему он часто не работает</h5>
<p>Удаление неиспользуемого кода требует уверенности, что удаление <b>безопасно</b>. Мешают три вещи:</p>
<ul>
<li><b>Побочные эффекты.</b> Если модуль при импорте что-то делает (регистрирует полифил, меняет прототип), выбросить его нельзя. Поле <code class="i">"sideEffects": false</code> в <code class="i">package.json</code> — это обещание библиотеки, что таких эффектов нет.</li>
<li><b>CommonJS-сборка</b> библиотеки. Если пакет отдаёт только CJS, shaking не сработает.</li>
<li><b>Импорт всего пакета</b>: <code class="i">import _ from 'lodash'</code> тянет всё. Нужно <code class="i">import debounce from 'lodash/debounce'</code> или пакет <code class="i">lodash-es</code>.</li>
</ul>
<pre class="code">// ❌ 70 КБ
import _ from 'lodash'
// ✅ 2 КБ
import debounce from 'lodash/debounce'

// ❌ вся библиотека иконок
import { Search } from 'some-icons'
// ✅ конкретный файл, если библиотека не tree-shakeable</pre>

<h5>Code splitting</h5>
<p>Разбиение бандла на части, загружаемые по требованию. Главный рычаг уменьшения первоначальной загрузки SPA.</p>
<pre class="code">// по маршрутам — даёт больше всего
const Settings = lazy(() =&gt; import('./pages/Settings'))

&lt;Suspense fallback={&lt;Skeleton /&gt;}&gt;
  &lt;Settings /&gt;
&lt;/Suspense&gt;

// тяжёлая библиотека — только когда действительно нужна
const onExport = async () =&gt; {
  const { utils, writeFile } = await import('xlsx')   // 400 КБ не в основном бандле
  writeFile(utils.book_new(), 'report.xlsx')
}</pre>
<p>Динамический <code class="i">import()</code> возвращает промис и создаёт отдельный чанк — это тот же механизм, на котором работает <code class="i">React.lazy</code>.</p>
<p>Улучшение, о котором стоит знать: <b>префетч по наведению</b>. Пользователь навёл курсор на ссылку — начинаем грузить чанк; к моменту клика он уже в кеше, и переход ощущается мгновенным.</p>

<div data-demo="bundle-size"></div>
<h5>Как найти, что весит</h5>
<ol>
<li><b>Анализатор бандла</b>: <code class="i">rollup-plugin-visualizer</code> для Vite, <code class="i">webpack-bundle-analyzer</code> для Webpack. Даёт карту размеров — обычно сразу видно две-три неожиданно жирные библиотеки.</li>
<li><b>Coverage</b> в DevTools — сколько процентов загруженного JS и CSS реально выполняется. Цифры обычно отрезвляют.</li>
<li><b>bundlephobia</b> — вес пакета до установки.</li>
<li><code class="i">pnpm why &lt;пакет&gt;</code> — кто тянет дубль или лишнюю зависимость.</li>
</ol>
<p>Типичные находки: moment с локалями (250 КБ), полная библиотека иконок, две версии одной зависимости, lodash целиком, тяжёлая библиотека графиков в основном чанке, забытые полифилы для браузеров, которые вы не поддерживаете.</p>

<h5>Vite: почему дев-сервер мгновенный</h5>
<p>Принципиальное отличие от Webpack: в режиме разработки Vite <b>ничего не бандлит</b>. Браузер сам загружает модули через нативный <code class="i">import</code>, а Vite на лету трансформирует запрошенный файл.</p>
<pre class="code">Webpack dev: собрать ВЕСЬ граф модулей → отдать бандл
             время старта растёт вместе с проектом

Vite dev:    отдать index.html → браузер запрашивает модули →
             трансформировать только запрошенное
             время старта почти не зависит от размера проекта</pre>
<p>Зависимости при этом всё же пре-бандлятся через esbuild — иначе сотни мелких CJS-модулей из <code class="i">node_modules</code> дали бы шквал HTTP-запросов.</p>
<p><b>HMR</b> (горячая замена модулей) в Vite тоже точечный: заменяется только изменённый модуль и его прямые зависимые, состояние приложения сохраняется.</p>
<p>Для продакшена Vite собирает через Rollup — там нужны минификация, tree-shaking, разбиение на чанки и хеши в именах.</p>

<h5>Инструменты и их роли</h5>
<table>
<tr><th>Инструмент</th><th>Написан на</th><th>Роль</th></tr>
<tr><td><b>esbuild</b></td><td>Go</td><td>очень быстрая трансформация и минификация; пре-бандлинг зависимостей в Vite</td></tr>
<tr><td><b>SWC</b></td><td>Rust</td><td>замена Babel; используется в <code class="i">plugin-react-swc</code> и в Nest</td></tr>
<tr><td><b>Rollup</b></td><td>JS</td><td>продакшен-сборка, эталон для библиотек</td></tr>
<tr><td><b>Webpack</b></td><td>JS</td><td>максимум возможностей, Module Federation; медленнее</td></tr>
<tr><td><b>tsc</b></td><td>TS</td><td>проверка типов (esbuild и SWC типы <b>не проверяют</b>, только вырезают)</td></tr>
</table>
<div class="warn">Важный практический момент: esbuild и SWC <b>удаляют типы, не проверяя их</b>. Поэтому в CI обязателен отдельный шаг <code class="i">tsc --noEmit</code> — иначе код с ошибками типов спокойно соберётся и уедет в прод. В ваших проектах это как раз скрипт <code class="i">type-check</code>.</div>

<h5>Что ещё влияет на вес и скорость</h5>
<ul>
<li><b>Сжатие</b>: brotli на 15–20% лучше gzip для текста. Настраивается на nginx или CDN.</li>
<li><b>Хеши в именах файлов</b> и <code class="i">Cache-Control: immutable</code> — см. статью про HTTP.</li>
<li><b>Разделение вендорного чанка</b>: библиотеки меняются реже кода приложения, поэтому их полезно вынести — пользователь не перекачивает React при каждом релизе.</li>
<li><b>Картинки</b>: AVIF и WebP, <code class="i">loading="lazy"</code>, явные размеры или <code class="i">aspect-ratio</code> (иначе CLS), <code class="i">srcset</code> для разных плотностей.</li>
<li><b>Шрифты</b>: <code class="i">woff2</code>, подмножество нужных символов, <code class="i">font-display: swap</code>, <code class="i">preload</code> для основного начертания.</li>
<li><b>Полифилы</b>: <code class="i">browserslist</code> определяет целевые браузеры. Поддержка IE11, которую никто не отменил в конфиге, добавляет сотни килобайт впустую.</li>
</ul>

<h5>Переменные окружения на клиенте</h5>
<pre class="code">// доступно в браузере, подставляется на этапе сборки
import.meta.env.VITE_API_URL

// ❌ этого в бандле нет и не будет
import.meta.env.SECRET_KEY</pre>
<div class="key">Всё, что попало в клиентский бандл, <b>видно пользователю</b> — это обычный текст в файле, который можно открыть. Секретов на фронтенде не существует. Туда можно класть только публичные значения: адрес API, DSN Sentry, публичный ключ аналитики.</div>

<h5>Порядок оптимизации</h5>
<ol>
<li>Измерить: анализатор бандла и Lighthouse. Без цифр — гадание.</li>
<li>Убрать лишнее: тяжёлые библиотеки, дубли, неправильные импорты, ненужные полифилы.</li>
<li>Разделить по маршрутам через <code class="i">lazy</code>.</li>
<li>Вынести редко меняющееся в отдельные чанки.</li>
<li>Настроить сжатие, кеш-заголовки, CDN.</li>
<li>Оптимизировать ассеты: картинки и шрифты часто весят больше, чем весь JS.</li>
<li>Префетч следующей вероятной страницы.</li>
</ol>` }
