import type { TheoryArticle } from '@/engine/types'

export const basicsControl: TheoryArticle = {
  id: 'th-basics-control', topic: 'База: условия и циклы',
  title: 'База JavaScript: условия, циклы и порядок действий',
  lead: 'Как программа выбирает ветку и повторяет действие — без заучивания сложного синтаксиса.',
  body: `
<p>Программа обычно делает три вещи: выполняет строку за строкой, <b>выбирает</b> нужный путь и <b>повторяет</b> часть работы. Понимание этих трёх действий помогает читать почти любой код.</p>

<h5>if выбирает между вариантами</h5>
<pre class="code">const age = 20
if (age >= 18) {
  console.log('Можно продолжать')
} else {
  console.log('Нужно разрешение')
}</pre>
<p>Условие внутри скобок должно дать логический ответ. Фигурные скобки показывают, какие строки относятся к выбранной ветке. Если веток больше двух, используйте <code class="i">else if</code> или <code class="i">switch</code>.</p>
<p>Для начинающего самый надёжный стиль — явная проверка: <code class="i">if (items.length === 0)</code>, а не загадочное <code class="i">if (!items.length)</code>. Короткие проверки удобны, когда вы уже умеете быстро прочитать их без догадок.</p>

<h5>for повторяет действие заданное число раз</h5>
<pre class="code">let total = 0
for (let i = 1; i <= 3; i += 1) {
  total += i
}
console.log(total) // 6</pre>
<p>В заголовке цикла три части: старт, условие продолжения и изменение счётчика. Если забыть менять счётчик или ошибиться в условии, цикл может выполняться бесконечно. Для перебора значений массива обычно понятнее <code class="i">for...of</code>:</p>
<pre class="code">for (const name of ['Аня', 'Ира']) {
  console.log(name)
}</pre>

<h5>break и continue</h5>
<p><code class="i">break</code> полностью завершает цикл. <code class="i">continue</code> пропускает оставшуюся часть текущего круга и переходит к следующему. Используйте их только тогда, когда это делает код яснее.</p>

<h5>Проверяйте границы</h5>
<p>Цикл по массиву часто использует условие <code class="i">i &lt; items.length</code>: последний допустимый индекс — <code class="i">length - 1</code>. Перед чтением <code class="i">items[i]</code> спросите себя, что будет для пустого массива и для последнего элемента.</p>
<p>Чтобы разобраться в незнакомом фрагменте, заведите маленькую таблицу: значение переменной до шага, условие, действие, значение после шага. Так легко проверить цикл вручную и не «схлопотать» off-by-one.</p>

<h5>Когда условие становится сложным</h5>
<p>Если в одном <code class="i">if</code> много <code class="i">&amp;&amp;</code>, <code class="i">||</code> и скобок, дайте части говорящие имена: <code class="i">isAdult</code>, <code class="i">hasPermission</code>. Это проще проверить тестами и объяснить другому человеку.</p>
<div class="note">Сначала добейтесь правильной логики на нескольких понятных значениях. Сократить запись можно после этого.</div>`,
}
