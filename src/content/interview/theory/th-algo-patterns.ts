import type { TheoryArticle } from '@/engine/types'

export const algoPatterns: TheoryArticle = { id:'th-algo-patterns', topic:'Алгоритмы', title:'Алгоритмы: приёмы и оценка сложности',
  lead:'Как распознать нужный приём по условию задачи, оценить сложность и не запутаться в границах.',
  body:`
<h5>O-нотация: что она на самом деле означает</h5>
<p>O-нотация описывает <b>асимптотический рост</b>: во сколько раз хуже станет, если данных станет в десять раз больше. Константы и младшие слагаемые отбрасываются — <code class="i">O(3n + 100)</code> это <code class="i">O(n)</code>. Она не говорит «сколько миллисекунд»: алгоритм O(n²) на сотне элементов может оказаться быстрее O(n log n) из-за меньшей константы.</p>
<table>
<tr><th>Сложность</th><th>Название</th><th>Пример</th><th>При n = 1 000 000</th></tr>
<tr><td>O(1)</td><td>константная</td><td>доступ по индексу, <code class="i">Map.get</code></td><td>1</td></tr>
<tr><td>O(log n)</td><td>логарифмическая</td><td>бинарный поиск, B-tree</td><td>~20</td></tr>
<tr><td>O(n)</td><td>линейная</td><td>один проход</td><td>10⁶</td></tr>
<tr><td>O(n log n)</td><td>линейно-логарифмическая</td><td>эффективная сортировка</td><td>2·10⁷</td></tr>
<tr><td>O(n²)</td><td>квадратичная</td><td>вложенные циклы</td><td>10¹² — не дождётесь</td></tr>
<tr><td>O(2ⁿ)</td><td>экспоненциальная</td><td>наивный перебор</td><td>невозможно</td></tr>
</table>
<h6>Сложность операций в JavaScript</h6>
<pre class="code">arr[i], push, pop                   O(1)
shift, unshift, splice              O(n)  — сдвигаются все элементы
includes, indexOf, find, filter     O(n)
sort                                O(n log n)
Map/Set: get, set, has, delete      O(1) в среднем
Object.keys, спред {...obj}         O(n)</pre>
<div class="warn">Скрытая O(n) внутри цикла — главный источник случайных O(n²). Классика: <code class="i">arr.includes()</code> в <code class="i">filter</code>, <code class="i">unshift</code> в цикле и особенно <code class="i">{...acc, [k]: v}</code> внутри <code class="i">reduce</code> — копирование всего аккумулятора на каждом шаге.</div>
<p>На собеседовании всегда называйте <b>и время, и память</b>, и проговаривайте размен: «за O(n) времени и O(n) памяти через Map; можно за O(1) памяти двумя указателями, но тогда нужен отсортированный вход».</p>

<div data-demo="bigo"></div>
<h5>Как распознать приём по условию</h5>
<table>
<tr><th>Формулировка в задаче</th><th>Приём</th></tr>
<tr><td>«найти пару», «есть ли дубликат», «сколько раз встречается»</td><td>хеш-таблица</td></tr>
<tr><td>«отсортированный массив», «пара с краёв», «на месте»</td><td>два указателя</td></tr>
<tr><td>«подмассив», «подстрока», «непрерывный отрезок»</td><td>скользящее окно</td></tr>
<tr><td>«отсортировано» + «найти границу или позицию»</td><td>бинарный поиск</td></tr>
<tr><td>«все комбинации», «все пути», «расставить»</td><td>backtracking</td></tr>
<tr><td>«кратчайший путь», «по уровням», «минимум шагов»</td><td>BFS</td></tr>
<tr><td>«сколько способов», «максимум при условии»</td><td>динамическое программирование</td></tr>
<tr><td>«топ-K», «K-й по величине», «медиана потока»</td><td>куча</td></tr>
<tr><td>«сумма на отрезке», «много запросов диапазона»</td><td>префиксные суммы</td></tr>
</table>

<h5>Приём 1: хеш-таблица — разменять память на время</h5>
<p>Самый универсальный приём вообще. Идея: вместо повторного поиска по массиву построить индекс за один проход.</p>
<pre class="code">// было O(n·m)
const common = a.filter(x =&gt; b.includes(x))

// стало O(n + m)
const setB = new Set(b)
const common = a.filter(x =&gt; setB.has(x))</pre>
<p>Это же лекарство от N+1 в бэкенде: вместо запроса на каждый элемент — один запрос с <code class="i">IN</code> и <code class="i">Map</code> по id. Алгоритмический приём и приём оптимизации API здесь буквально совпадают.</p>

<h5>Приём 2: два указателя</h5>
<pre class="code">// с краёв — палиндром, two sum в отсортированном массиве
let l = 0, r = n - 1
while (l &lt; r) {
  if (sum &lt; target) l++
  else if (sum &gt; target) r--
  else return [l, r]
}

// оба слева — удаление дубликатов на месте, разделение массива
let write = 0
for (let read = 0; read &lt; n; read++) {
  if (keep(arr[read])) arr[write++] = arr[read]
}</pre>
<p>Главная выгода — <b>O(1) дополнительной памяти</b>. Если интервьюер после рабочего решения спрашивает «а можно без дополнительной памяти?», почти всегда имеются в виду указатели.</p>

<div data-demo="binary-search"></div>
<h5>Приём 3: скользящее окно</h5>
<pre class="code">let left = 0, best = 0
for (let right = 0; right &lt; n; right++) {
  add(arr[right])                                  // впустили элемент
  while (!isValid()) { remove(arr[left]); left++ } // сузили до корректного
  best = Math.max(best, right - left + 1)
}</pre>
<p>Выглядит как вложенные циклы, но это <b>O(n)</b>: каждый указатель проходит массив максимум один раз. Внутренний <code class="i">while</code> в сумме выполнится не больше n раз за всё время.</p>
<p>Типичная ошибка — позволить левой границе двигаться назад. В задаче о самой длинной подстроке без повторов на строке <code class="i">'abba'</code> нужно <code class="i">left = Math.max(left, lastSeen + 1)</code>, иначе граница откатится к уже пройденной позиции.</p>

<h5>Приём 4: бинарный поиск</h5>
<p>Ломают его не на идее, а на границах. Держите в голове один инвариант и не смешивайте два стиля.</p>
<pre class="code">// включительные границы [lo, hi]
let lo = 0, hi = n - 1
while (lo &lt;= hi) {
  const mid = lo + Math.floor((hi - lo) / 2)
  if (arr[mid] === target) return mid
  if (arr[mid] &lt; target) lo = mid + 1
  else hi = mid - 1
}

// полуинтервал [lo, hi) — для поиска границы
let lo = 0, hi = n
while (lo &lt; hi) {
  const mid = lo + Math.floor((hi - lo) / 2)
  if (arr[mid] &lt; target) lo = mid + 1
  else hi = mid            // mid МОЖЕТ быть ответом — не отбрасываем
}
return lo                   // первый элемент &gt;= target</pre>
<p>Сочетание <code class="i">while (lo &lt;= hi)</code> с <code class="i">hi = mid</code> даёт бесконечный цикл — самая частая ошибка.</p>
<p>Отдельно стоит знать <b>бинарный поиск по ответу</b>: когда ответ монотонен («подходит / не подходит»), ищем границу. Пример: минимальное число воркеров, при котором очередь разгребается за час.</p>

<div data-demo="sliding-window"></div>
<h5>Приём 5: префиксные суммы</h5>
<pre class="code">const prefix = [0]
for (const x of arr) prefix.push(prefix.at(-1) + x)
const rangeSum = (i, j) =&gt; prefix[j + 1] - prefix[i]   // O(1) после O(n)</pre>
<p>Подготовка за линейное время превращает любой запрос суммы отрезка в константу. Работает и для двумерных массивов, и для подсчёта количества элементов с условием.</p>

<h5>Приём 6: backtracking</h5>
<pre class="code">function permute(arr, path = [], used = new Set(), out = []) {
  if (path.length === arr.length) { out.push([...path]); return out }

  for (let i = 0; i &lt; arr.length; i++) {
    if (used.has(i)) continue
    used.add(i); path.push(arr[i])    // выбрали
    permute(arr, path, used, out)     // пошли вглубь
    path.pop(); used.delete(i)        // ОТКАТИЛИ — в этом вся суть
  }
  return out
}</pre>
<p>Схема всегда одна: выбрать → углубиться → откатить. Ключевое — не забыть откат и копировать результат (<code class="i">[...path]</code>), иначе в ответе окажутся ссылки на один и тот же изменяющийся массив.</p>

<h5>Приём 7: обходы графа</h5>
<table>
<tr><th></th><th>BFS</th><th>DFS</th></tr>
<tr><td>Структура</td><td>очередь (FIFO)</td><td>стек или рекурсия (LIFO)</td></tr>
<tr><td>Порядок</td><td>по слоям</td><td>вглубь</td></tr>
<tr><td>Кратчайший путь</td><td><b>да</b> (невзвешенный граф)</td><td>нет</td></tr>
<tr><td>Память</td><td>O(ширины)</td><td>O(глубины)</td></tr>
<tr><td>Задачи</td><td>кратчайший путь, уровни</td><td>циклы, топосорт, компоненты связности</td></tr>
</table>
<p>BFS обходит по слоям, поэтому <b>первое достижение цели и есть кратчайший путь</b>. Две обязательные детали: множество <code class="i">visited</code> (иначе цикл зациклит обход) и пометка <b>при добавлении в очередь</b>, а не при извлечении — иначе узел попадёт в очередь многократно.</p>
<p>Где графы встречаются в работе: дерево компонентов React, разрешение зависимостей модулей, порядок миграций, обнаружение циклических зависимостей (то самое, что Nest ловит как circular dependency), дерево категорий и хлебные крошки.</p>

<div data-demo="bfs-dfs"></div>
<h5>Приём 8: динамическое программирование</h5>
<p>Подходит, когда задача разбивается на <b>перекрывающиеся подзадачи</b> и обладает оптимальной подструктурой. Суть — не считать одно и то же дважды.</p>
<pre class="code">// сверху вниз: рекурсия + мемоизация — проще придумать
function climb(n, memo = new Map()) {
  if (n &lt;= 1) return 1
  if (memo.has(n)) return memo.get(n)
  const r = climb(n - 1, memo) + climb(n - 2, memo)
  memo.set(n, r); return r
}

// снизу вверх: итерация — быстрее, нет риска переполнить стек
let prev = 1, cur = 1
for (let i = 2; i &lt;= n; i++) [prev, cur] = [cur, prev + cur]</pre>
<p>Порядок решения ДП-задачи: определить <b>состояние</b> (что означает <code class="i">dp[i]</code>) → выписать <b>переход</b> → определить <b>базу</b> → проверить порядок вычисления → оптимизировать память, если нужны только последние значения.</p>
<p>Шесть задач, покрывающих большинство собеседований: лестница/Фибоначчи, рюкзак, размен монет, наибольшая возрастающая подпоследовательность, расстояние Левенштейна, наибольшая общая подпоследовательность.</p>

<h5>Структуры данных: что выбрать</h5>
<table>
<tr><th>Структура</th><th>Сильна в</th><th>Когда брать</th></tr>
<tr><td>Массив</td><td>доступ по индексу O(1)</td><td>упорядоченные данные, итерация</td></tr>
<tr><td>Map / объект</td><td>поиск по ключу O(1)</td><td>индекс по id, кеш, счётчики</td></tr>
<tr><td>Set</td><td>проверка вхождения O(1)</td><td>«уже видели», дедупликация</td></tr>
<tr><td>Стек</td><td>LIFO O(1)</td><td>undo, парсинг, DFS</td></tr>
<tr><td>Очередь</td><td>FIFO O(1)</td><td>BFS, задачи, события</td></tr>
<tr><td>Связный список</td><td>вставка/удаление O(1)</td><td>LRU, fiber React, дек</td></tr>
<tr><td>Куча</td><td>min/max за O(log n)</td><td>топ-K, приоритетная очередь</td></tr>
<tr><td>Дерево</td><td>иерархия, поиск O(log n)</td><td>категории, DOM, индексы БД</td></tr>
</table>
<p>Практический способ выбора: спросить себя, <b>какая операция будет самой частой</b>. Поиск по ключу — Map. Проверка вхождения — Set. Обход по порядку — массив. Нужен минимум на каждом шаге — куча.</p>
<h6>Куча и приоритетная очередь — то, чего обычно не знают</h6>
<p><b>Куча</b> (heap) — бинарное дерево с одним правилом: <b>родитель не больше любого из потомков</b> (для min-heap). Отсюда следует, что <b>минимум всегда в корне</b> — достать его можно за O(1). Полного порядка в куче нет: она не отсортирована, она лишь гарантирует, что наверху лежит самый маленький элемент.</p>
<p>Хранят её в обычном массиве без всяких указателей: для элемента с индексом <code class="i">i</code> потомки лежат в <code class="i">2i+1</code> и <code class="i">2i+2</code>, а родитель — в <code class="i">(i-1)/2</code>.</p>
<pre class="code">        1              массив: [1, 3, 5, 7, 9, 8]
      /   \
     3     5           потомки индекса 0 → индексы 1 и 2
    / \   /            потомки индекса 1 → индексы 3 и 4
   7   9 8</pre>
<table>
<tr><th>Операция</th><th>Сложность</th><th>Как работает</th></tr>
<tr><td>посмотреть минимум</td><td>O(1)</td><td>это корень, элемент с индексом 0</td></tr>
<tr><td>вставить</td><td>O(log n)</td><td>добавить в конец и «всплывать» вверх, меняясь с родителем, пока меньше него</td></tr>
<tr><td>извлечь минимум</td><td>O(log n)</td><td>взять корень, поставить на его место последний элемент и «топить» вниз</td></tr>
<tr><td>найти произвольный</td><td>O(n)</td><td>куча для этого не предназначена</td></tr>
</table>
<p><b>Приоритетная очередь</b> — это куча с прикладным интерфейсом: «дай самый срочный элемент». В JavaScript встроенной нет, поэтому на собеседовании достаточно сказать, что вы возьмёте готовую реализацию или напишете на массиве.</p>
<p><b>Зачем она нужна.</b> Классическая задача «найти K самых больших элементов из миллиона». Сортировать весь массив — O(n log n) и память под миллион элементов. С кучей размера K: идём по элементам, держим кучу из K самых больших, каждый шаг — O(log K). Итого <b>O(n log K)</b> и память O(K). При K = 100 разница драматична.</p>
<p>Где встречается ещё: алгоритм Дейкстры (кратчайший путь во взвешенном графе), слияние K отсортированных списков, планировщик задач по приоритету, медиана потока чисел (через две кучи), сборка мусора в движках.</p>
<h6>Map или объект</h6>
<p>Map: ключи любого типа, гарантированный порядок вставки, <code class="i">size</code> за O(1), быстрее при частых добавлениях и удалениях, нет унаследованных ключей. Объект: удобнее для фиксированной структуры и сериализации в JSON. Отдельная ловушка объекта — ключи из прототипа: <code class="i">'toString' in {}</code> даёт <code class="i">true</code>, а ключ <code class="i">'__proto__'</code> способен сломать логику. Лечится <code class="i">Object.create(null)</code>.</p>

<h5>Порядок работы над задачей на собеседовании</h5>
<ol>
<li><b>Уточнить</b>: размер входа, диапазон значений, дубликаты, пустой вход, можно ли мутировать. Уточняющий вопрос — это плюс.</li>
<li><b>Назвать наивное решение</b> и его сложность. Это уже засчитывается.</li>
<li><b>Назвать приём</b> и почему он подходит.</li>
<li><b>Писать, проговаривая вслух.</b> Молчаливое кодирование — главная ошибка: оценивать нечего.</li>
<li><b>Прогнать на примере</b>, включая граничные случаи.</li>
<li><b>Назвать итоговую сложность</b> по времени и памяти и сказать, что улучшили бы в продакшене.</li>
</ol>` }
