import type { TheoryArticle } from '@/engine/types'

export const archPatterns: TheoryArticle = { id:'th-arch-patterns', topic:'Архитектура фронта', title:'Архитектурные паттерны приложений',
  lead:'MVC, Flux, слоистая и гексагональная архитектура, CQRS, Event Sourcing, Saga, Outbox, BFF — что это и когда применимо.',
  body:`
<p>Если паттерны проектирования — про классы и объекты, то архитектурные паттерны — про <b>устройство приложения целиком</b>: где проходят границы, куда текут зависимости и как части общаются между собой.</p>

<h5>MVC и его потомки</h5>
<table>
<tr><th>Паттерн</th><th>Суть</th><th>Где встречается</th></tr>
<tr><td><b>MVC</b></td><td>Model (данные и правила) — View (отображение) — Controller (обработка ввода)</td><td>классический серверный веб, Rails, Django, роль контроллера в Nest</td></tr>
<tr><td><b>MVP</b></td><td>Presenter берёт логику на себя, View пассивна</td><td>старые десктопные и мобильные приложения</td></tr>
<tr><td><b>MVVM</b></td><td>ViewModel + двустороннее связывание</td><td>Vue, Angular, WPF</td></tr>
<tr><td><b>Flux / Redux</b></td><td>однонаправленный поток: action → dispatcher → store → view</td><td>React-экосистема</td></tr>
</table>
<p>Почему React ушёл от MVC к <b>Flux</b>: в MVC с несколькими моделями и видами связи образуют паутину — изменение одной модели каскадно обновляет несколько видов, а те меняют другие модели. Отладка превращается в ад. Flux вводит жёсткое правило: <b>данные текут только в одну сторону</b>.</p>
<pre class="code">Действие пользователя
   ↓
  action  { type: 'task/toggled', payload: 42 }
   ↓
  reducer (чистая функция: state + action → новый state)
   ↓
  store
   ↓
  view (перерисовывается от нового состояния)
   ↓
Действие пользователя …</pre>
<p>Выгода: в любой момент можно ответить на вопрос «почему состояние стало таким» — достаточно посмотреть лог действий. Отсюда и time-travel debugging.</p>

<div data-demo="flux-flow"></div>
<h5>Слоистая архитектура</h5>
<pre class="code">Presentation   контроллеры, компоненты — общение с внешним миром
     ↓
Application    сценарии использования, оркестрация
     ↓
Domain         бизнес-сущности и правила
     ↓
Infrastructure БД, HTTP-клиенты, очереди, файлы</pre>
<p>Правило: слой знает только о слое <b>непосредственно ниже</b>. Простая и понятная модель, которая подходит большинству приложений. Слабое место — домен в ней зависит от инфраструктуры: сущность знает про ORM, а значит, бизнес-правила нельзя протестировать без базы.</p>

<h5>Гексагональная (порты и адаптеры) и чистая архитектура</h5>
<p>Исправляет именно эту проблему, <b>инвертируя зависимость</b>: домен объявляет интерфейс («порт»), а инфраструктура его реализует («адаптер»).</p>
<pre class="code">        ┌─────────────────┐
  HTTP →│                 │→ Postgres
  CLI  →│     ДОМЕН       │→ Redis
  Queue→│  (не знает ни   │→ S3
        │   о ком из них) │
        └─────────────────┘
      адаптеры входа   адаптеры выхода</pre>
<pre class="code">// порт объявлен в домене
export interface OrderRepository {
  findById(id: OrderId): Promise&lt;Order | null&gt;
  save(order: Order): Promise&lt;void&gt;
}

// адаптер живёт в инфраструктуре
export class TypeOrmOrderRepository implements OrderRepository { … }</pre>
<p><b>Правило зависимостей</b>: стрелки всегда направлены внутрь. Домен не импортирует TypeORM, React или axios.</p>
<p><b>Что это даёт:</b> бизнес-логика тестируется без базы и сети (подставили in-memory реализацию порта), замена хранилища не трогает домен, а по коду домена видно предметную область, а не фреймворк.</p>
<div class="warn">Честная оговорка: полная чистая архитектура с маппингом на каждом слое оправдана в больших доменах со сложными правилами. Для CRUD-сервиса это четыре лишних файла на эндпоинт и отрицательная отдача. Хороший инженер называет и границы применимости — это ценится выше, чем восторг.</div>

<h5>CQRS</h5>
<p>Command Query Responsibility Segregation — разделение моделей <b>чтения</b> и <b>записи</b>.</p>
<pre class="code">// запись: нормализованная модель, валидация, транзакции
await commandBus.execute(new CreateOrderCommand(dto))

// чтение: денормализованная витрина, оптимизированная под экран
const view = await queryBus.execute(new GetOrderListQuery(filters))</pre>
<p><b>Мотивация:</b> у чтения и записи разные требования. Записи нужны инварианты и транзакции; чтению — скорость и удобная под интерфейс форма. Попытка обслужить оба одной моделью приводит к компромиссам в обе стороны.</p>
<p>Лёгкая форма CQRS — просто разные DTO и разные методы для чтения и записи — полезна почти всем. Тяжёлая форма с отдельными базами и синхронизацией через события даёт <b>eventual consistency</b>: после записи данные появятся в витрине не мгновенно, и интерфейс должен это учитывать.</p>

<h5>Event Sourcing</h5>
<p>Хранить не текущее состояние, а <b>последовательность событий</b>, которые к нему привели. Состояние вычисляется проигрыванием событий.</p>
<pre class="code">// вместо строки balance = 1500 храним:
OrderCreated   { id, items, total: 2000 }
DiscountApplied{ id, amount: 500 }
OrderPaid      { id, method: 'card' }</pre>
<p><b>Плюсы:</b> полная история изменений (аудит из коробки), возможность восстановить состояние на любой момент, добавление новых витрин задним числом (просто переиграли события по-новому).</p>
<p><b>Минусы:</b> сложность, версионирование схемы событий (событие пятилетней давности надо уметь прочитать), «а как сделать простой JOIN» превращается в нетривиальный вопрос. Оправдан в финансах, учёте, там, где аудит — требование.</p>

<h5>Saga: транзакции между сервисами</h5>
<p>Распределённых транзакций в микросервисах практически нет. Вместо них — последовательность локальных транзакций с <b>компенсирующими действиями</b> на случай сбоя.</p>
<pre class="code">Заказ создан → Оплата проведена → Товар зарезервирован → Доставка назначена
                                          ✗ не хватило товара
                          ← Оплата возвращена ← Заказ отменён
                            (компенсация)      (компенсация)</pre>
<p>Два стиля: <b>хореография</b> (сервисы реагируют на события друг друга, нет центра — гибко, но трудно проследить сценарий) и <b>оркестрация</b> (координатор управляет шагами — понятнее, но появляется центральная точка).</p>
<p>Компенсация — не «откат»: деньги уже списали, их нужно <b>вернуть отдельной операцией</b>, и в истории останутся оба факта.</p>

<h5>Outbox: надёжная публикация событий</h5>
<p>Проблема, с которой сталкиваются все: нужно записать в базу и отправить событие в очередь. Если база записалась, а очередь упала — событие потеряно. Если наоборот — событие есть, данных нет. Двухфазный коммит между базой и брокером обычно недоступен.</p>
<pre class="code">// одна транзакция: и данные, и событие
await dataSource.transaction(async (m) =&gt; {
  await m.save(order)
  await m.save(OutboxEvent, {                 // та же БД, та же транзакция
    type: 'order.created',
    payload: { id: order.id },
  })
})

// отдельный воркер читает outbox и публикует в очередь,
// помечая отправленные. При сбое просто отправит повторно —
// поэтому потребитель обязан быть идемпотентным (at-least-once)</pre>
<p>Это один из самых практичных паттернов: он решает реальную проблему согласованности минимальными средствами.</p>

<h5>BFF — Backend for Frontend</h5>
<p>Отдельный тонкий бэкенд под каждый тип клиента: один для веба, другой для мобильного приложения.</p>
<p><b>Зачем:</b> у клиентов разные потребности. Мобильному нужно меньше полей и меньше запросов (дорогая сеть), вебу — больше данных за раз. Без BFF универсальное API обрастает флагами <code class="i">?include=...&amp;fields=...</code> и постепенно становится неуправляемым.</p>
<p>BFF агрегирует вызовы нескольких сервисов, приводит данные к удобной для экрана форме и прячет внутреннюю структуру бэкенда. В Nest это обычный модуль; tRPC-роутер в вашей монорепе, по сути, играет роль BFF для веб-клиента.</p>

<h5>Паттерны устойчивости</h5>
<table>
<tr><th>Паттерн</th><th>Что делает</th></tr>
<tr><td><b>Circuit Breaker</b></td><td>после серии ошибок перестаёт слать запросы к упавшему сервису, периодически пробуя «прощупать» его</td></tr>
<tr><td><b>Retry с backoff и jitter</b></td><td>повтор с растущей паузой и случайным разбросом, чтобы клиенты не долбили синхронно</td></tr>
<tr><td><b>Bulkhead</b></td><td>изоляция ресурсов: отдельный пул соединений на каждую зависимость, чтобы один упавший сервис не съел все</td></tr>
<tr><td><b>Timeout</b></td><td>без него зависший запрос держит соединение вечно</td></tr>
<tr><td><b>Graceful degradation</b></td><td>отключить второстепенную функцию вместо падения всей страницы</td></tr>
<tr><td><b>Idempotency key</b></td><td>безопасный повтор неидемпотентной операции</td></tr>
</table>

<h5>Клиентские архитектурные приёмы</h5>
<ul>
<li><b>Container / Presentational</b> — разделение «умных» компонентов (данные, логика) и «глупых» (только отображение). В эпоху хуков граница размылась, но идея жива: компонент, который и грузит данные, и рисует сложный UI, тяжело тестировать и переиспользовать.</li>
<li><b>Render props и HOC</b> — исторические способы переиспользования логики, вытесненные хуками.</li>
<li><b>Compound components</b> — связанные компоненты, разделяющие неявное состояние: <code class="i">&lt;Tabs&gt;&lt;Tab/&gt;&lt;TabPanel/&gt;&lt;/Tabs&gt;</code>. Так устроены Radix и Headless UI.</li>
<li><b>Headless-компоненты</b> — логика и доступность без стилей. Вы получаете поведение, а внешний вид пишете сами. Это сейчас доминирующий подход в ui-библиотеках.</li>
<li><b>State machine</b> (XState) — явное описание состояний и переходов. Отлично подходит для визардов, плееров, сложных форм: невозможные состояния становятся <b>непредставимыми</b>, а не просто «не должны случаться».</li>
</ul>

<h5>Как выбирать</h5>
<p>Главный вопрос — не «какая архитектура правильная», а <b>какую сложность вы готовы оплатить</b>. Каждый паттерн из этой статьи добавляет слоёв, файлов и порог входа. Разумный порядок: начать с простого слоистого монолита с чистыми границами модулей и вводить более сложные паттерны <b>по мере появления конкретной боли</b>.</p>
<div class="key">Формулировка, которая хорошо звучит на собеседовании: «архитектура — это набор решений, которые дорого менять. Поэтому обратимые решения я принимаю быстро, а необратимые — выбор базы, формат публичного API, схему данных — с анализом альтернатив и записью в ADR».</div>` }
