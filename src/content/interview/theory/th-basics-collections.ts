import type { TheoryArticle } from '@/engine/types'

export const basicsCollections: TheoryArticle = {
  id: 'th-basics-collections', topic: 'База: массивы и объекты',
  title: 'База JavaScript: массивы и объекты',
  lead: 'Когда взять список, когда — запись с полями, и как менять данные предсказуемо.',
  body: `
<p><b>Объект</b> хранит связанные значения под именованными ключами. <b>Массив</b> хранит упорядоченный список. Это два способа представить данные, которые встречаются в каждой задаче на JavaScript.</p>

<h5>Объект — запись с именованными полями</h5>
<pre class="code">const user = {
  name: 'Аня',
  age: 25,
  active: true,
}

console.log(user.name)       // 'Аня'
console.log(user['active'])  // true
user.age = 26                // поле можно обновить</pre>
<p>К точечной записи обращаются, когда имя поля известно заранее. Квадратные скобки нужны, если имя хранится в переменной: <code class="i">user[fieldName]</code>.</p>

<h5>Массив — список в заданном порядке</h5>
<pre class="code">const names = ['Аня', 'Ира', 'Лев']
console.log(names[0])       // 'Аня', первый элемент
console.log(names.length)   // 3
names.push('Миша')          // добавить в конец</pre>
<p>Нумерация начинается с нуля, поэтому последний индекс равен <code class="i">length - 1</code>. Пустой массив имеет длину 0. Перед доступом к элементу подумайте, может ли список оказаться пустым.</p>

<h5>map преобразует каждый элемент</h5>
<pre class="code">const prices = [10, 20, 30]
const withTax = prices.map(price => price * 1.2)
// [12, 24, 36]
// prices остался [10, 20, 30]</pre>
<p><code class="i">map</code> создаёт новый массив одинаковой длины. Используйте его, когда каждому элементу соответствует новое значение.</p>

<h5>filter оставляет подходящие элементы</h5>
<pre class="code">const ages = [12, 20, 17, 30]
const adults = ages.filter(age => age >= 18)
// [20, 30]</pre>
<p><code class="i">filter</code> создаёт новый массив только из элементов, для которых условие дало <code class="i">true</code>. Если нужно выполнить действие для каждого элемента и ничего не собирать, подходит <code class="i">forEach</code>.</p>

<h5>Изменение внутри и создание копии</h5>
<pre class="code">const first = { name: 'Аня' }
first.name = 'Ира' // меняем существующий объект

const renamed = { ...first, name: 'Лев' } // новый объект
const names = ['Аня', 'Ира']
const nextNames = [...names, 'Саша']      // новый массив</pre>
<p>Spread (<code class="i">...</code>) здесь копирует только верхний уровень. Вложенные объекты по-прежнему могут быть общими ссылками. Для начала запомните простое правило: присваивание массива или объекта переменной не копирует его содержимое.</p>

<h5>Сначала выберите правильную форму</h5>
<ul><li>Один пользователь с полями имя и возраст — объект.</li><li>Несколько пользователей в порядке показа — массив объектов.</li><li>Обработать все элементы — <code class="i">map</code>, выбрать часть — <code class="i">filter</code>.</li><li>Обновить один верхнеуровневый объект и сохранить старый — spread с новым полем.</li></ul>
<div class="note">Проверьте себя: объект отвечает на вопрос «какие поля у одной вещи?», массив — «какой у нас список вещей?».</div>
<div data-demo="js-foundation-lab"></div>`,
}
