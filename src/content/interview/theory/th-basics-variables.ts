import type { TheoryArticle } from '@/engine/types'

export const basicsVariables: TheoryArticle = {
  id: 'th-basics-variables', topic: 'База: переменные и значения',
  title: 'База JavaScript: переменные без путаницы',
  lead: 'Что такое переменная, чем отличаются let, const и var, когда значение можно менять и где его видно.',
  body: `
<p>Переменная — это <b>имя, по которому программа находит значение</b>. Например, имя <code class="i">price</code> может указывать на число 500. Переменные помогают не повторять значения и давать им понятные имена.</p>

<h5>Создать имя и положить значение</h5>
<pre class="code">let price = 500
price = 700          // можно присвоить новое значение

const currency = '₽'
// currency = '$'    // ошибка: новую строку этому имени присвоить нельзя</pre>
<p><code class="i">let</code> выбирают, когда само значение переменной будет переназначаться. <code class="i">const</code> — когда имя останется привязано к тому же значению. В обычном коде разумно начинать с <code class="i">const</code> и менять на <code class="i">let</code>, только если это действительно нужно.</p>
<div class="warn"><b>Важно:</b> <code class="i">const</code> запрещает переназначить имя, но не делает объект неизменяемым. <code class="i">const user = { name: 'Аня' }</code> разрешает <code class="i">user.name = 'Ира'</code>, но запрещает <code class="i">user = {}</code>.</div>

<h5>Зачем обычно не нужен var</h5>
<p><code class="i">var</code> — старый способ объявлять переменные. У него область видимости ограничена функцией, а не блоком в фигурных скобках. Это может неожиданно связывать друг с другом значения из разных частей кода. В новом коде используйте <code class="i">let</code> и <code class="i">const</code>; <code class="i">var</code> нужно узнавать в старых проектах и задачах на собеседовании.</p>
<pre class="code">if (true) {
  let message = 'видно только внутри блока'
  const answer = 42
}
// message и answer здесь недоступны</pre>

<h5>Объявить — не значит задать полезное значение</h5>
<p><code class="i">let result</code> создаёт переменную со значением <code class="i">undefined</code>: пока туда ничего не положили. Это отличается от явного <code class="i">null</code>, которым программист обычно обозначает «значения сейчас нет». Неиспользуемую переменную лучше не создавать, а при создании сразу дать понятное начальное значение.</p>

<h5>Присваивание читает текущее значение</h5>
<pre class="code">let count = 2
count = count + 1   // сначала читаем 2, затем записываем 3
count += 1          // короткая запись той же идеи
console.log(count)  // 4</pre>
<p>Знак <code class="i">=</code> здесь значит «сохрани справа вычисленное значение в имени слева», а не математическое равенство. Для сравнения используется <code class="i">===</code>.</p>

<h5>Порядок, в котором начинающему проще читать код</h5>
<ol><li>Объявляйте переменную рядом с первым использованием.</li><li>Давайте имя по смыслу: <code class="i">totalPrice</code>, а не <code class="i">x</code>.</li><li>Используйте <code class="i">const</code>, если не собираетесь менять привязку.</li><li>Используйте <code class="i">let</code>, если состояние действительно обновляется.</li><li>Не используйте <code class="i">var</code> в новом коде.</li></ol>
<div class="note">Когда базовые правила понятны, следующий шаг — выяснить, какие значения бывают и почему объект ведёт себя иначе, чем число.</div>`,
}
