import type { TheoryArticle } from '@/engine/types'

export const fsd: TheoryArticle = { id:'th-fsd', topic:'Архитектура фронта', title:'FSD и организация фронтенд-проекта',
  lead:'Зачем нужна методология, как устроен Feature-Sliced Design и как мигрировать на него постепенно.',
  body:`
<h5>Какую проблему решает структура папок</h5>
<p>Проблема не в том, что «код плохой». Проблема в двух вопросах, на которые через год никто не может ответить: <b>куда положить новый файл</b> и <b>что сломается, если я изменю вот это</b>.</p>
<p>Самая плохая структура — <b>по типам файлов</b>: <code class="i">/components</code>, <code class="i">/hooks</code>, <code class="i">/utils</code>, <code class="i">/types</code> на весь проект. Она выглядит аккуратно на старте, но добавление одной фичи требует правок в пяти папках, а по дереву каталогов невозможно понять, что вообще делает приложение. Хорошая структура рассказывает о предметной области, а не о фреймворке.</p>

<h5>Feature-Sliced Design: три уровня деления</h5>
<pre class="code">src/
├── app/         инициализация: провайдеры, роутер, глобальные стили, i18n
├── pages/       страницы — композиция виджетов под маршрут
├── widgets/     самостоятельные блоки UI (Header, TaskBoard, Sidebar)
├── features/    действия пользователя (ToggleTask, Login, AddToCart)
├── entities/    бизнес-сущности (Task, User: типы, карточка, запросы)
└── shared/      без бизнес-смысла (ui-kit, api-клиент, хелперы, конфиг)</pre>
<ol>
<li><b>Слои</b> — шесть папок верхнего уровня. Отвечают за меру ответственности.</li>
<li><b>Слайсы</b> — деление внутри слоя по предметной области: <code class="i">task</code>, <code class="i">user</code>, <code class="i">payment</code>. Имена задаёт бизнес.</li>
<li><b>Сегменты</b> — деление внутри слайса по техническому назначению.</li>
</ol>
<pre class="code">features/toggle-task/
├── ui/        ToggleButton.tsx
├── model/     стор, хуки, селекторы
├── api/       мутация
├── lib/       вспомогательные чистые функции
├── config/    константы
└── index.ts   ← public API слайса</pre>

<h5>Главное правило: импорты только вниз</h5>
<pre class="code">app → pages → widgets → features → entities → shared</pre>
<ul>
<li>Слой импортирует <b>только из слоёв ниже себя</b>. <code class="i">entities</code> ничего не знает про <code class="i">features</code>.</li>
<li><b>Слайсы одного слоя не импортируют друг друга.</b> Если <code class="i">features/toggle-task</code> понадобился <code class="i">features/edit-task</code> — это сигнал: общее нужно опустить в <code class="i">entities</code>, а композицию поднять в <code class="i">widgets</code>.</li>
<li><code class="i">shared</code> не знает ни о ком: там не может встречаться слово «task».</li>
</ul>
<div class="key">Следствие правила: <b>циклические зависимости становятся невозможными по конструкции</b>. И это не надежда на дисциплину — правило проверяется линтером (<code class="i">@feature-sliced/eslint-config</code> или <code class="i">eslint-plugin-boundaries</code>). Без автоматической проверки любые соглашения размываются за пару спринтов.</div>

<div data-demo="fsd-layers"></div>
<h5>Public API слайса</h5>
<p>Каждый слайс экспортирует наружу только то, что хочет отдать, через <code class="i">index.ts</code>. Импортировать <code class="i">features/toggle-task/ui/Button</code> напрямую запрещено — только <code class="i">features/toggle-task</code>.</p>
<p>Выгода в свободе: внутренности можно перестраивать как угодно, пока контракт тот же. Это та же идея, что поле <code class="i">exports</code> в <code class="i">package.json</code> и <code class="i">exports</code> модуля в NestJS — и, по сути, инкапсуляция на уровне файловой системы.</p>

<h5>Чем entity отличается от feature</h5>
<p>Вопрос, на котором плывут чаще всего. Простая мнемоника: <b>entity — существительное, feature — глагол</b>.</p>
<ul>
<li><b>Entity</b>: <code class="i">Task</code> — тип, карточка для отображения, запрос за списком, селекторы. Это «что есть в системе».</li>
<li><b>Feature</b>: <code class="i">ToggleTask</code>, <code class="i">CreateTask</code>, <code class="i">Login</code> — действие пользователя, меняющее состояние. Это «что можно сделать».</li>
</ul>
<p>Карточка задачи — entity. Кнопка «выполнено» на этой карточке — feature. Причём feature может импортировать entity (действие знает про сущность), но не наоборот.</p>

<h5>Куда положить: быстрый ответ</h5>
<pre class="code">Кнопка без бизнес-смысла?                 → shared/ui
Отображение сущности (карточка, аватар)?   → entities/&lt;name&gt;/ui
Действие пользователя?                     → features/&lt;action&gt;
Блок из нескольких фич и сущностей?        → widgets/&lt;block&gt;
Целый экран под маршрутом?                 → pages/&lt;page&gt;
Провайдер или настройка приложения?        → app</pre>

<h5>Как мигрировать постепенно</h5>
<p>Переписывать проект «за один заход» не нужно и обычно вредно. Рабочий порядок:</p>
<ol>
<li>Создать <code class="i">shared</code>, перенести туда ui-kit, api-клиент и хелперы. Уже полезно и ничего не ломает.</li>
<li>Выделить <code class="i">entities</code> для двух-трёх главных сущностей.</li>
<li>Новые фичи писать сразу по FSD, старый код не трогать.</li>
<li>Включить линтер границ в режиме предупреждений, через месяц — в режиме ошибок.</li>
<li>Настроить алиасы (<code class="i">@/shared/*</code>) в <code class="i">tsconfig</code> и <code class="i">vite-tsconfig-paths</code>, чтобы не было <code class="i">../../../</code>.</li>
</ol>

<h5>Типичные перекосы</h5>
<ul>
<li><b>Дробление ради дробления</b>: <code class="i">features/render-button</code>. Фича — про бизнес-действие, а не про любой UI-эффект.</li>
<li><b>Толстый <code class="i">shared</code></b>, куда сваливают всё подряд — та же свалка <code class="i">utils</code> под новым названием.</li>
<li><b>Кросс-импорты между слайсами</b> — самая частая утечка, постепенно возвращающая проект в хаос.</li>
<li><b>Внедрять все шесть слоёв сразу</b> в маленьком проекте. FSD не обязывает: нормально начать с <code class="i">app / pages / shared</code> и добавлять слои по мере роста.</li>
</ul>

<h5>Альтернативы и когда они уместны</h5>
<table>
<tr><th>Подход</th><th>Делит по</th><th>Сильно в</th><th>Слабо в</th></tr>
<tr><td><b>Atomic Design</b></td><td>размеру UI (atoms → organisms)</td><td>дизайн-система, библиотека компонентов</td><td>бизнес-логика: неясно, где живёт «оформить заказ»</td></tr>
<tr><td><b>FSD</b></td><td>бизнес-смыслу + слои</td><td>продуктовое приложение с растущим числом фич</td><td>порог входа, избыточно для маленького проекта</td></tr>
<tr><td><b>Модули по фичам</b></td><td>фичам, без слоёв</td><td>простота</td><td>без правил импорта быстро спутывается</td></tr>
<tr><td><b>Nx / модульный монолит</b></td><td>пакетам</td><td>жёсткие границы, кеш сборки</td><td>тяжёлая настройка</td></tr>
</table>
<p>На собеседовании не отвечайте «FSD лучше». Назовите <b>критерии хорошей структуры</b>: по папке видно, что делает приложение; изменение одной фичи трогает одну папку; направление зависимостей однонаправленное и проверяется автоматически; удаление фичи — это удаление папки.</p>

<h5>Микрофронтенды: когда это оправдано</h5>
<p>Разделение фронтенда на независимо разрабатываемые и <b>независимо деплоимые</b> приложения. Способы интеграции: Module Federation (подгрузка чужого кода в рантайме), Web Components, iframe, npm-пакеты, серверная композиция.</p>
<p><b>Критерий применимости организационный, а не технический</b>: несколько независимых команд, которым мешает общий релизный цикл; постепенная миграция легаси; разные стеки в одном продукте.</p>
<p>Для одной команды это чистый вред: дублирование зависимостей (три копии React в бандле), сложная отладка, рассинхрон дизайн-системы, общее состояние через костыли — и ноль выгоды. Монорепа с чёткими границами модулей даёт почти все плюсы без минусов.</p>
<div class="note">Если всё же микрофронтенды: <code class="i">shared: { react: { singleton: true } }</code> — иначе два React на странице и сломанные хуки; общение через события, а не общий стор; общая версионируемая дизайн-система; приложение не должно падать целиком, если удалённый модуль не загрузился.</div>

<h5>Где должна жить логика</h5>
<ul>
<li><b>Компонент</b> — отображение и обработка ввода. Появились вычисления скидок в JSX — логика не на месте.</li>
<li><b>Хук / model</b> — состояние и сценарии.</li>
<li><b>api</b> — общение с сервером и маппинг DTO в доменную модель.</li>
<li><b>lib</b> — чистые функции без зависимости от React: считать, форматировать, валидировать. Тестируются без рендера.</li>
</ul>
<p>Практический тест: <b>переживёт ли эта логика смену UI-фреймворка?</b> Если да — ей не место внутри компонента.</p>
<p>И отдельно про валидацию: бизнес-правила обязаны жить <b>на сервере</b> — клиент можно обойти. На клиенте валидация нужна только ради UX. В монорепе это решается общей zod-схемой: одно правило, два места применения.</p>` }
