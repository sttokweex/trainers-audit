import type { TheoryArticle } from '@/engine/types'

export const sysdesign: TheoryArticle = { id:'th-sysdesign', topic:'Системный дизайн', title:'Системный дизайн: как проектировать и как отвечать',
  lead:'Структура ответа, прикидка нагрузки, типовые компоненты и компромиссы — на разобранном примере.',
  body:`
<h5>Что на самом деле оценивают</h5>
<p>Правильного ответа в задаче системного дизайна не существует. Оценивают процесс: задаёте ли вы уточняющие вопросы, умеете ли прикидывать порядки величин, <b>называете ли компромиссы вслух</b>, не усложняете ли раньше времени и можете ли углубиться, когда попросят.</p>
<div class="warn">Главные ошибки: начать с «возьмём Kubernetes и Kafka» до выяснения нагрузки; молча рисовать схему; проектировать под миллиард пользователей, когда их тысяча; не спросить соотношение чтения и записи — а именно оно определяет всю архитектуру.</div>

<h5>Структура ответа</h5>
<ol>
<li><b>Уточнить требования (5 минут).</b> Функциональные: что должно работать? Нефункциональные: сколько пользователей, RPS, объём данных, требования к задержке и доступности?</li>
<li><b>Прикинуть масштаб</b> — грубые цифры на салфетке.</li>
<li><b>API</b> — два-три ключевых эндпоинта.</li>
<li><b>Модель данных</b> — таблицы, ключи, индексы.</li>
<li><b>Верхнеуровневая схема</b>.</li>
<li><b>Углубиться в одно-два узких места</b> — интервьюер подскажет, куда.</li>
<li><b>Проговорить компромиссы</b> — это и есть предмет оценки.</li>
</ol>

<h5>Типовая архитектура</h5>
<pre class="code">Клиент
  ↓
CDN — статика, кеш на периферии, ближе к пользователю
  ↓
Балансировщик — TLS, rate limit, маршрутизация
  ↓
API-инстансы (stateless, N штук)
  ↓                    ↘
Кеш (Redis)            Очередь (Bull/Kafka) → воркеры
  ↓
БД: мастер + реплики чтения
  ↓
Объектное хранилище (S3/MinIO) для файлов
  ↓
Наблюдаемость: логи, метрики, трейсы</pre>

<h5>Цифры, которые полезно помнить</h5>
<table>
<tr><th>Операция</th><th>Порядок</th></tr>
<tr><td>Обращение к памяти</td><td>~100 нс</td></tr>
<tr><td>Чтение из SSD</td><td>~100 мкс</td></tr>
<tr><td>Redis в той же сети</td><td>~0.5–1 мс</td></tr>
<tr><td>Простой запрос к БД</td><td>~1–10 мс</td></tr>
<tr><td>Round-trip внутри дата-центра</td><td>~0.5 мс</td></tr>
<tr><td>Round-trip через континент</td><td>~100–150 мс</td></tr>
</table>
<div class="key">Практический вывод из этой таблицы: <b>сеть дороже вычислений на порядки</b>. Поэтому N+1 убивает производительность, батчинг спасает, а CDN ближе к пользователю даёт больше, чем оптимизация кода.</div>
<h6>Прикидка</h6>
<pre class="code">1 млн пользователей, 10 действий в день
= 10 млн запросов / 86 400 с ≈ 116 RPS в среднем
пик ×5–10 → ~1000 RPS
один инстанс Node вытягивает ~500–2000 RPS на простых ручках
→ 2–4 инстанса с запасом. Кластер на 50 машин НЕ нужен.</pre>
<p>Такая оценка на салфетке ценится гораздо выше перечисления технологий. Она же защищает от переусложнения.</p>

<div data-demo="capacity"></div>
<div data-demo="consistency-tradeoff"></div>
<h5>Разбор: сервис коротких ссылок</h5>
<p><b>Требования:</b> создать короткую ссылку, перейти по ней, собрать статистику переходов. Не нужно: редактирование, кастомные домены.</p>
<p><b>Масштаб:</b> 100 млн ссылок в год, соотношение чтение/запись примерно 100:1 — значит, система <b>read-heavy</b>, и всё проектирование пойдёт вокруг чтения. 100 млн / 3·10⁷ секунд ≈ 3 записи в секунду, ~300 чтений, пики ×10. Объём: 100 млн × ~500 байт ≈ 50 ГБ в год — помещается в одну базу, шардирование пока не нужно.</p>
<p><b>API:</b> <code class="i">POST /api/links {url}</code> → <code class="i">{shortCode}</code>; <code class="i">GET /:code</code> → редирект.</p>
<p><b>Генерация кода</b> — ключевое решение, здесь и копают:</p>
<table>
<tr><th>Подход</th><th>Плюс</th><th>Минус</th></tr>
<tr><td>Хеш от URL, первые 7 символов base62</td><td>детерминированно, одинаковые ссылки схлопываются</td><td>коллизии, нужна проверка</td></tr>
<tr><td>Счётчик + base62</td><td>коллизий нет</td><td>коды предсказуемы (можно перебрать чужие), счётчик — узкое место</td></tr>
<tr><td>Случайные 7 символов + уникальный индекс</td><td>62⁷ ≈ 3.5·10¹² вариантов, непредсказуемо</td><td>редкие повторы, обрабатываются ретраем</td></tr>
</table>
<p>Обычно лучший компромисс — третий вариант.</p>
<p><b>Данные:</b> <code class="i">links(code PK, url, user_id, created_at, expires_at)</code>. Статистику переходов <b>не писать синхронно</b> при редиректе — иначе запись упрётся в базу при каждом переходе. Отправляем событие в очередь и агрегируем асинхронно.</p>
<p><b>Чтение:</b> Redis-кеш <code class="i">code → url</code> с длинным TTL: ссылки практически неизменяемы, попадание в кеш будет 95% и выше.</p>
<p><b>301 или 302?</b> Отличная ловушка. 301 кешируется браузером навсегда — быстро, но <b>статистика перестанет собираться</b> и ссылку нельзя будет отозвать. Для сервиса со статистикой — 302.</p>

<h5>Масштабирование: порядок действий</h5>
<ol>
<li><b>Измерить</b> и найти узкое место. Чаще всего это база, а не приложение.</li>
<li><b>Оптимизировать</b>: индексы, N+1, лишние запросы. Дешевле любого железа.</li>
<li><b>Кеш</b>: HTTP-заголовки, CDN для статики, Redis для данных.</li>
<li><b>Вертикально</b>: больше CPU и памяти. Просто, но упирается в потолок и стоит нелинейно.</li>
<li><b>Горизонтально</b>: больше инстансов. Требует <b>stateless</b>-приложения.</li>
<li><b>Реплики чтения</b>.</li>
<li><b>Шардирование</b> — последнее средство, резко усложняет всё.</li>
</ol>
<p><b>Stateless</b> — обязательное условие горизонтального масштабирования: сессии в Redis или JWT, файлы в объектном хранилище, WebSocket через redis-adapter. Иначе при добавлении второго пода половина пользователей начнёт «разлогиниваться».</p>
<p><b>Реплики</b> снимают нагрузку чтения, но появляется <b>replication lag</b>: пользователь создал задачу, страница читает с реплики, задачи там ещё нет. Лечение — read-your-own-writes: после записи этот пользователь какое-то время читает с мастера.</p>
<p><b>Шардирование</b> приносит сразу несколько проблем: кросс-шардовые запросы и JOIN, отсутствие распределённых транзакций, ребалансировка при добавлении шарда, горячие шарды при неравномерном ключе.</p>

<h5>CAP и PACELC</h5>
<p>При <b>сетевом разделении</b> (P — оно неизбежно в распределённой системе) приходится выбирать между согласованностью (C) и доступностью (A).</p>
<ul>
<li><b>CP</b>: лучше ошибка, чем неверные данные — платежи, склад, бронирование.</li>
<li><b>AP</b>: лучше слегка устаревшие данные, чем недоступность — лента, счётчик лайков, рекомендации.</li>
</ul>
<p>Уточнение, которое ценят: <b>CAP применим только в момент разделения сети</b>. В обычном режиме система может давать и C, и A. Более практичная модель — <b>PACELC</b>: при разделении выбираем A или C, а иначе (Else) — между задержкой (L) и согласованностью (C).</p>

<h5>Монолит или микросервисы</h5>
<table>
<tr><th>Монолит</th><th>Микросервисы</th></tr>
<tr><td>простой деплой и отладка, транзакции из коробки</td><td>независимый деплой и масштабирование частей</td></tr>
<tr><td>сложно масштабировать по частям</td><td>сетевые задержки, распределённые транзакции (saga)</td></tr>
<tr><td>границы размываются без дисциплины</td><td>сложная отладка и наблюдаемость</td></tr>
<tr><td><b>правильный старт почти всегда</b></td><td>оправданы, когда мешают границы команд</td></tr>
</table>
<p>Канонический ответ: <b>модульный монолит</b> с чёткими границами, из которого при необходимости вырезают сервисы. И тезис, который стоит произнести: «микросервисы — это в первую очередь организационное решение, а не техническое».</p>

<h5>Надёжность</h5>
<ul>
<li><b>Таймауты везде.</b> Запрос без таймаута висит вечно и выедает пул соединений — так падают сервисы целиком.</li>
<li><b>Ретраи с экспоненциальным backoff и jitter</b>, только для идемпотентных операций. Без jitter все клиенты ретраятся синхронно и добивают восстанавливающийся сервис.</li>
<li><b>Circuit breaker</b>: после серии ошибок перестать долбить упавший сервис на время.</li>
<li><b>Graceful degradation</b>: упали рекомендации — показать обычный список, а не белый экран.</li>
<li><b>Идемпотентность</b> обработчиков и dead letter queue для фоновых задач.</li>
<li><b>Backpressure</b>: ограничивать входящий поток, а не копить бесконечную очередь.</li>
</ul>

<h5>Наблюдаемость</h5>
<table>
<tr><th></th><th>Отвечает на вопрос</th></tr>
<tr><td><b>Логи</b></td><td>что конкретно произошло в этом запросе</td></tr>
<tr><td><b>Метрики</b></td><td>как система чувствует себя в целом, во времени</td></tr>
<tr><td><b>Трейсы</b></td><td>где именно ушло время в цепочке вызовов</td></tr>
</table>
<p>Логи — структурированные (JSON) и с <b>correlation id</b>: без него логи десяти подов невозможно собрать в историю одного запроса. Метрики — <b>RED</b> (Rate, Errors, Duration) и обязательно <b>перцентили</b>, а не среднее: среднее в 100 мс прекрасно уживается с тем, что каждый двадцатый пользователь ждёт пять секунд.</p>
<p>Трейсы через OpenTelemetry дают водопад: «200 мс из 250 ушло в один SQL-запрос». Это самый быстрый способ найти N+1 без гадания.</p>
<p>Алертить нужно на <b>симптомы для пользователя</b> (рост 5xx, p99 латентности), а не на каждый скачок CPU. Алерт, который звенит постоянно, перестают замечать — и пропускают настоящий инцидент.</p>` }
