import type { Question } from '@/engine/types'

/** Системный дизайн — 2 вопрос(ов) */
export const sistemnyDizaynQuestions: Question[] = [
  { id:'sd-approach', topic:'Системный дизайн', type:'theory', level:'senior',
    q:'Как отвечать на задачу системного дизайна? Разберите структуру ответа на примере «спроектируйте сервис коротких ссылок».',
    answer:`<h5>Структура ответа — держите её в голове</h5>
  <ol>
  <li><b>Уточнить требования (5 мин).</b> Не бросайтесь рисовать. Функциональные: что должно работать? Нефункциональные: сколько пользователей, RPS, объём данных, требования к задержке и доступности?</li>
  <li><b>Прикинуть масштаб.</b> Грубые цифры на салфетке.</li>
  <li><b>API.</b> Пара ключевых эндпоинтов.</li>
  <li><b>Модель данных.</b> Таблицы, ключи, индексы.</li>
  <li><b>Верхнеуровневая схема.</b> Клиент → CDN → балансировщик → сервис → кеш → БД.</li>
  <li><b>Углубиться в 1–2 узких места.</b> Интервьюер сам подскажет, куда.</li>
  <li><b>Проговорить компромиссы.</b> Это и есть предмет оценки.</li>
  </ol>
  <h5>Пример: сервис коротких ссылок</h5>
  <p><b>Требования:</b> создать короткую ссылку, перейти по ней, статистика переходов. Не нужно: редактирование, кастомные домены.</p>
  <p><b>Масштаб:</b> 100 млн ссылок в год, соотношение чтение/запись ~100:1 → это <b>read-heavy</b> система. 100 млн / 3·10⁷ с ≈ 3 записи/с, значит ~300 чтений/с, пики ×10. Объём: 100 млн × ~500 байт ≈ 50 ГБ в год — помещается в одну БД, шардирование пока не нужно. Это важный вывод: <b>не усложняйте без нужды</b>.</p>
  <p><b>API:</b> <code class="i">POST /api/links {url}</code> → <code class="i">{shortCode}</code>; <code class="i">GET /:code</code> → 301/302.</p>
  <p><b>Генерация кода</b> — ключевое решение, здесь и копают:</p>
  <ul>
  <li><b>Хеш от URL</b> (первые 7 символов base62) — детерминированно, но возможны коллизии, нужна проверка.</li>
  <li><b>Счётчик + base62</b> — коллизий нет, но коды предсказуемы (можно перебрать чужие ссылки) и счётчик — узкое место при нескольких инстансах.</li>
  <li><b>Случайные 7 символов</b> + уникальный индекс в БД, при конфликте — повтор. 62⁷ ≈ 3.5·10¹² вариантов, коллизии редки. Обычно лучший компромисс.</li>
  </ul>
  <p><b>Данные:</b> таблица <code class="i">links(code PK, url, user_id, created_at, expires_at)</code>. Статистику переходов <b>не писать синхронно</b> при редиректе — иначе запись упрётся в БД; шлём событие в очередь и агрегируем асинхронно.</p>
  <p><b>Чтение:</b> Redis-кеш <code class="i">code → url</code>, cache-aside с длинным TTL: ссылки практически неизменяемы, попадание в кеш будет 95%+.</p>
  <p><b>301 или 302?</b> Отличный вопрос-ловушка. 301 кешируется браузером навсегда → быстро, но <b>статистика перестанет собираться</b> и ссылку нельзя будет отозвать. Для сервиса со статистикой — 302.</p>
  <h5>Что оценивают на самом деле</h5>
  <p>Не «знает ли кандидат правильный ответ» — его нет. Оценивают: задаёте ли вы уточняющие вопросы, умеете ли прикидывать порядки величин, <b>называете ли компромиссы вслух</b>, не усложняете ли раньше времени и можете ли углубиться, когда попросят.</p>
  <div class="trap">Главные ошибки: начать с «возьмём Kubernetes и Kafka» до выяснения нагрузки; молча рисовать; проектировать под миллиард пользователей, когда их тысяча; не спросить соотношение чтения и записи — а именно оно определяет всю архитектуру.</div>` },
  { id:'sd-scale', topic:'Системный дизайн', type:'theory', level:'senior',
    q:'Как масштабировать веб-приложение? Вертикально/горизонтально, stateless, шардирование, CAP-теорема.',
    answer:`<h5>Порядок действий при росте нагрузки</h5>
  <ol>
  <li><b>Измерить</b> и найти узкое место. Чаще всего это БД, а не приложение.</li>
  <li><b>Оптимизировать</b>: индексы, N+1, лишние запросы. Дешевле любого железа.</li>
  <li><b>Кеш</b>: HTTP-заголовки, CDN для статики, Redis для данных.</li>
  <li><b>Вертикально</b>: больше CPU/RAM. Просто, но упирается в потолок и стоит нелинейно.</li>
  <li><b>Горизонтально</b>: больше инстансов за балансировщиком. Требует <b>stateless</b>-приложения.</li>
  <li><b>Реплики БД</b>: чтение с реплик, запись в мастер.</li>
  <li><b>Шардирование</b>: разрезать данные по ключу. Последнее средство — резко усложняет всё.</li>
  </ol>
  <h5>Stateless — обязательное условие</h5>
  <p>Инстанс не должен хранить состояние в памяти: сессии → Redis/JWT, загруженные файлы → S3/MinIO, WebSocket-соединения → redis-adapter, кеш в памяти процесса — только как дополнение, не как источник правды. Иначе при добавлении второго пода половина пользователей начнёт «разлогиниваться».</p>
  <h5>Реплики и отставание</h5>
  <p>Чтение с реплик снимает нагрузку, но появляется <b>replication lag</b>: пользователь создал задачу, страница читает с реплики и задачи там ещё нет. Лечение: read-your-own-writes (после записи этот пользователь какое-то время читает с мастера), либо критичные чтения сразу с мастера.</p>
  <h5>Шардирование</h5>
  <p>Разделение данных по ключу (по <code class="i">user_id</code>, по региону). Проблемы, которые появляются сразу: кросс-шардовые запросы и JOIN, <b>отсутствие распределённых транзакций</b>, ребалансировка при добавлении шарда (решается consistent hashing), горячие шарды при неравномерном ключе.</p>
  <h5>CAP-теорема</h5>
  <p>При <b>сетевом разделении</b> (P — оно неизбежно в распределённой системе) приходится выбирать между <b>C</b> (consistency — все видят одинаковые данные) и <b>A</b> (availability — система отвечает всегда).</p>
  <ul>
  <li><b>CP</b>: лучше ошибка, чем неверные данные — платежи, склад, бронирование.</li>
  <li><b>AP</b>: лучше слегка устаревшие данные, чем недоступность — лента, счётчик лайков, рекомендации.</li>
  </ul>
  <p>Важное уточнение, которое ценят: <b>CAP применим только в момент разделения сети</b>. В обычном режиме система может давать и C, и A. Более практичная модель — <b>PACELC</b>: «при разделении выбираем A или C, а иначе (Else) — между задержкой (Latency) и согласованностью».</p>
  <h5>Монолит vs микросервисы</h5>
  <table>
  <tr><th>Монолит</th><th>Микросервисы</th></tr>
  <tr><td>простой деплой и отладка, транзакции из коробки</td><td>независимый деплой и масштабирование частей</td></tr>
  <tr><td>сложно масштабировать по частям</td><td>сетевые задержки, распределённые транзакции (saga), сложная отладка</td></tr>
  <tr><td><b>правильный старт почти всегда</b></td><td>оправданы, когда мешают границы команд</td></tr>
  </table>
  <p>Канон: <b>модульный монолит</b> с чёткими границами, из которого при необходимости вырезают сервисы. «Микросервисы — это в первую очередь организационное решение».</p>` },
]
