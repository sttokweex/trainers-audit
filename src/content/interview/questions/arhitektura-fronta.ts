import type { Question } from '@/engine/types'

/** Архитектура фронта — 4 вопрос(ов) */
export const arhitekturaFrontaQuestions: Question[] = [
  { id:'arch-front', topic:'Архитектура фронта', type:'theory', level:'middle',
    q:'Как вы структурируете фронтенд-проект? Atomic Design, Feature-Sliced Design — в чём разница и что выбрать?',
    code:`// Atomic Design (так устроен ваш service-core/client)
  components/
    atoms/        Button, Input, Icon
    molecules/    SearchField, Card
    organisms/    Header, TaskList
    templates/    PageLayout
  pages/
  
  // Feature-Sliced Design
  app/        провайдеры, роутер, стили
  pages/      страницы
  widgets/    самостоятельные блоки страницы
  features/   пользовательские сценарии (добавить в корзину)
  entities/   бизнес-сущности (User, Task) + их UI и модель
  shared/     ui-kit, api, lib, config`,
    answer:`<h5>Atomic Design</h5>
  <p>Делит по <b>размеру и сложности UI</b>. Хорошо работает для дизайн-системы и библиотеки компонентов. Слабое место — бизнес-логика: непонятно, где живёт «оформить заказ», и organisms со временем превращаются в свалку.</p>
  <h5>Feature-Sliced Design</h5>
  <p>Делит по <b>бизнес-смыслу</b> с жёстким правилом импортов: <b>слой может импортировать только из слоёв ниже себя</b> (pages → widgets → features → entities → shared). Никаких обратных и «боковых» зависимостей.</p>
  <p>Плюсы: фича локализована в одной папке, её легко удалить или передать другой команде; циклические зависимости невозможны по конструкции; правило проверяется линтером. Минусы: порог входа, избыточно для маленького проекта.</p>
  <h5>Как отвечать</h5>
  <p>Не «FSD лучше», а по ситуации: <b>для дизайн-системы — atomic, для продуктового приложения с растущим числом фич — FSD</b>. Главное — назвать критерии хорошей структуры:</p>
  <ul>
  <li>По папке видно, <b>что делает приложение</b>, а не какой у него фреймворк.</li>
  <li>Изменение одной фичи трогает одну папку (high cohesion, low coupling).</li>
  <li>Направление зависимостей однонаправленное и проверяемое автоматически.</li>
  <li>Удаление фичи = удаление папки.</li>
  </ul>
  <div class="hint">Сильный ход: сказать, что <b>худшая структура — по типам файлов</b> (<code class="i">/hooks</code>, <code class="i">/utils</code>, <code class="i">/types</code> на весь проект). Одно изменение расползается на пять папок, и никакой связности.</div>` },
  { id:'fsd-full', topic:'Архитектура фронта', type:'theory', level:'senior',
    q:'Разберите Feature-Sliced Design подробно: слои, слайсы, сегменты, правило импортов, public API. Как мигрировать на FSD существующий проект?',
    code:`src/
  ├── app/                  инициализация: провайдеры, роутер, глобальные стили
  ├── pages/                страницы (композиция виджетов)
  │   └── task-list/
  │       ├── ui/
  │       └── index.ts      ← public API слайса
  ├── widgets/              самостоятельные блоки UI
  │   └── task-board/
  ├── features/             действия пользователя
  │   └── toggle-task/
  │       ├── ui/           ToggleButton.tsx
  │       ├── model/        стор, хуки
  │       ├── api/          мутация
  │       └── index.ts
  ├── entities/             бизнес-сущности
  │   └── task/
  │       ├── ui/           TaskCard.tsx
  │       ├── model/        типы, селекторы
  │       └── api/          запросы
  └── shared/               переиспользуемое без бизнес-смысла
      ├── ui/               Button, Input (дизайн-система)
      ├── api/              базовый клиент
      ├── lib/              хелперы
      └── config/`,
    answer:`<h5>Три уровня деления</h5>
  <ol>
  <li><b>Слои</b> (layers) — те самые 6 папок верхнего уровня. Отвечают за <b>меру ответственности</b>.</li>
  <li><b>Слайсы</b> (slices) — деление внутри слоя по предметной области: <code class="i">task</code>, <code class="i">user</code>, <code class="i">payment</code>. Имена задаёт бизнес, а не фреймворк.</li>
  <li><b>Сегменты</b> (segments) — деление внутри слайса по техническому назначению: <code class="i">ui</code>, <code class="i">model</code>, <code class="i">api</code>, <code class="i">lib</code>, <code class="i">config</code>.</li>
  </ol>
  <h5>Главное правило — импорты только вниз</h5>
  <pre class="code">app → pages → widgets → features → entities → shared</pre>
  <ul>
  <li>Слой может импортировать <b>только из слоёв ниже себя</b>.</li>
  <li><b>Слайсы одного слоя не импортируют друг друга.</b> Если <code class="i">features/toggle-task</code> нужен <code class="i">features/edit-task</code> — это сигнал, что общее надо опустить в <code class="i">entities</code> или поднять композицию в <code class="i">widgets</code>.</li>
  <li><code class="i">shared</code> не знает ни о ком: там не может быть слова «task».</li>
  </ul>
  <p>Следствие: <b>циклические зависимости невозможны по конструкции</b>, а правило проверяется автоматически — <code class="i">@feature-sliced/eslint-config</code> или <code class="i">eslint-plugin-boundaries</code>.</p>
  <h5>Public API слайса</h5>
  <p>Каждый слайс экспортирует только то, что хочет отдать, через <code class="i">index.ts</code>. Импортировать <code class="i">features/toggle-task/ui/Button</code> напрямую запрещено — только <code class="i">features/toggle-task</code>. Это даёт свободу менять внутренности без страха: снаружи виден лишь контракт. Ровно та же идея, что <code class="i">exports</code> в <code class="i">package.json</code> и <code class="i">exports</code> модуля Nest.</p>
  <h5>Чем entity отличается от feature</h5>
  <p>Вопрос, на котором чаще всего плывут. <b>Entity — это «существительное»</b> (Task, User: данные, типы, карточка для отображения). <b>Feature — это «глагол»</b>, действие пользователя, меняющее состояние (ToggleTask, Login, AddToCart). Карточка задачи — entity. Кнопка «выполнено» на этой карточке — feature.</p>
  <h5>Как мигрировать постепенно</h5>
  <ol>
  <li>Создать <code class="i">shared</code> и перенести туда ui-kit, api-клиент, хелперы. Уже даёт пользу и ничего не ломает.</li>
  <li>Выделить <code class="i">entities</code> для 2–3 главных сущностей.</li>
  <li>Новые фичи писать сразу по FSD, старый код не переписывать «за один заход».</li>
  <li>Включить линтер на границы в режиме warning, потом error.</li>
  <li>Настроить алиасы (<code class="i">@/shared/*</code>) в <code class="i">tsconfig</code> и <code class="i">vite-tsconfig-paths</code>.</li>
  </ol>
  <div class="trap">Типичный перекос — плодить слайсы на каждый чих: <code class="i">features/render-button</code>. FSD не обязывает использовать все слои: в маленьком проекте нормально обойтись <code class="i">app / pages / shared</code> и добавлять слои по мере роста. Архитектура должна отвечать сложности задачи, а не наоборот.</div>` },
  { id:'arch-microfe', topic:'Архитектура фронта', type:'theory', level:'senior',
    q:'Что такое микрофронтенды? Когда они оправданы, а когда это оверинжиниринг? Чем Module Federation отличается от iframe и npm-пакетов?',
    answer:`<h5>Суть</h5>
  <p>Разделение фронтенда на независимо разрабатываемые и <b>независимо деплоимые</b> приложения, которые собираются в одну страницу. Аналог микросервисов на клиенте.</p>
  <h5>Способы интеграции</h5>
  <table>
  <tr><th>Способ</th><th>Плюсы</th><th>Минусы</th></tr>
  <tr><td><b>iframe</b></td><td>полная изоляция стилей и JS</td><td>тяжело общаться, ломается роутинг, проблемы с a11y и адаптивом</td></tr>
  <tr><td><b>npm-пакеты</b></td><td>просто, типы работают</td><td>нет независимого деплоя: обновление = пересборка хоста</td></tr>
  <tr><td><b>Module Federation</b> (Webpack 5 / Vite plugin)</td><td>подгрузка чужого кода в рантайме, общие зависимости</td><td>сложная настройка, риск рассинхрона версий React</td></tr>
  <tr><td><b>Web Components</b></td><td>framework-agnostic, нативная изоляция через Shadow DOM</td><td>шероховатости с React-пропсами и событиями</td></tr>
  <tr><td><b>Server-side composition</b> (SSI, edge)</td><td>быстро для пользователя</td><td>нужна инфраструктура</td></tr>
  </table>
  <h5>Когда оправдано</h5>
  <p>Критерий <b>организационный, а не технический</b>: несколько независимых команд, которым мешает общий релизный цикл; необходимость постепенно мигрировать легаси; разные технологические стеки в одном продукте. Закон Конвея: архитектура повторяет структуру команд.</p>
  <h5>Когда это вред</h5>
  <p>Одна команда, один продукт. Тогда вы получаете все издержки — дублирование зависимостей (три копии React в бандле), сложный дебаг, рассинхрон дизайн-системы, общий стейт через костыли — и ноль выгоды. Для одной команды монорепа с чёткими границами модулей даёт почти все плюсы без минусов.</p>
  <h5>Что обязательно спросят</h5>
  <ul>
  <li><b>Общие зависимости</b>: <code class="i">shared: { react: { singleton: true, requiredVersion } }</code> — иначе два React на странице и сломанные хуки.</li>
  <li><b>Общение между микрофронтендами</b>: кастомные события или общая шина, но <b>не общий стор</b> — иначе это снова монолит, только хуже.</li>
  <li><b>Дизайн-система</b> обязана быть общей и версионируемой, иначе продукт распадётся визуально.</li>
  <li><b>Версионирование контрактов</b> и graceful degradation: если удалённый модуль не загрузился, приложение не должно падать целиком.</li>
  </ul>
  <div class="hint">Честный ответ на собесе: «микрофронтенды решают проблему масштабирования <b>команд</b>, а не кода. Если проблемы с командами нет — это оверинжиниринг». Такой ответ ценится выше, чем восторженный рассказ про Module Federation.</div>` },
  { id:'arch-clean', topic:'Архитектура фронта', type:'theory', level:'senior',
    q:'Чистая архитектура и слои в приложении: где должна жить бизнес-логика на фронте и на бэке? Что такое утечка абстракции?',
    answer:`<h5>Базовая идея слоёв</h5>
  <pre class="code">Домен (сущности, правила)      ← ничего не знает о внешнем мире
     ↑
  Приложение (сценарии, use cases)
     ↑
  Адаптеры (контроллеры, репозитории, UI)
     ↑
  Инфраструктура (БД, HTTP, очереди, фреймворк)</pre>
  <p>Правило зависимостей: <b>стрелки всегда внутрь</b>. Домен не импортирует TypeORM, React или axios. Инфраструктура реализует интерфейсы, объявленные внутри.</p>
  <h5>Где логика на бэке (Nest)</h5>
  <ul>
  <li><b>Контроллер</b> — только HTTP: разбор запроса, DTO, коды ответа. Никаких <code class="i">if</code> по бизнес-правилам.</li>
  <li><b>Сервис</b> — бизнес-логика. Не знает про <code class="i">req</code>/<code class="i">res</code>.</li>
  <li><b>Репозиторий</b> — доступ к данным. Возвращает доменные объекты, а не сырые строки БД.</li>
  </ul>
  <p>Честная оговорка для собеса: полная чистая архитектура с маппингом на каждом слое оправдана в больших доменах. Для CRUD-сервиса это лишние 4 файла на эндпоинт — и хороший инженер это признаёт.</p>
  <h5>Где логика на фронте</h5>
  <ul>
  <li><b>Компонент</b> — отображение и обработка ввода. Если в JSX появились вычисления скидок — логика не на месте.</li>
  <li><b>Хук / model-сегмент</b> — состояние и сценарии.</li>
  <li><b>api-сегмент</b> — общение с сервером и маппинг DTO в доменную модель.</li>
  <li><b>lib</b> — чистые функции без зависимости от React: считать, форматировать, валидировать. Их можно тестировать без рендера.</li>
  </ul>
  <p>Практический тест: <b>можно ли переиспользовать эту логику, если завтра поменяется UI-фреймворк?</b> Если да — она не должна быть внутри компонента.</p>
  <h5>Дублирование валидации</h5>
  <p>Бизнес-правила обязаны жить <b>на сервере</b> — клиент можно обойти. На клиенте валидация нужна только ради UX (мгновенная обратная связь). В монорепе это решается общей zod-схемой: одно правило, два места применения, ноль расхождений.</p>
  <h5>Утечка абстракции</h5>
  <p>Когда детали реализации «просачиваются» через интерфейс, который должен был их спрятать. Примеры:</p>
  <ul>
  <li>Репозиторий возвращает сущность TypeORM с ленивыми связями — и контроллер случайно триггерит запрос к БД при сериализации.</li>
  <li>API отдаёт поля ровно в структуре таблиц — изменение схемы БД ломает фронт.</li>
  <li>Хук возвращает внутренний объект React Query целиком, и компоненты начинают зависеть от версии библиотеки.</li>
  </ul>
  <div class="hint">Формулировка: «граница должна быть выражена в терминах предметной области, а не хранилища». Это одно предложение показывает, что вы понимаете архитектуру, а не пересказываете схему.</div>` },
]
