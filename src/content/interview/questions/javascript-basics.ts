import type { Question } from '@/engine/types'

/** Базовый маршрут JS: короткие задания по тем же пяти ступеням, что и теория. */
export const javascriptBasicsQuestions: Question[] = [
  {
    id: 'base-vars-binding', topic: 'База: переменные и значения', type: 'output', level: 'junior',
    q: 'Что напечатает код? Отдельно проследите значение переменной и поле объекта.',
    code: `
let total = 10
const settings = { limit: total }
total = 20
settings.limit = 5
console.log(total)
console.log(settings.limit)`,
    expected: '20\n5',
    hint: 'Число копируется при создании поля. Переназначение total не обновляет settings.limit.',
    answer: `<h5>Сначала сохраняется снимок значения</h5><p>При создании объекта выражение <code class="i">limit: total</code> читает число 10 и записывает его в поле. Потом переменной <code class="i">total</code> присваивают 20, но поле объекта остаётся 10 до отдельного изменения. Строка <code class="i">settings.limit = 5</code> меняет именно поле объекта. Поэтому выводится 20 и затем 5.</p><p><code class="i">const</code> здесь не мешает менять поле: запрет относится к присваиванию новой ссылки в <code class="i">settings</code>, а не к свойствам объекта.</p>`,
  },
  {
    id: 'base-vars-const', topic: 'База: переменные и значения', type: 'choice', level: 'junior',
    q: 'Какой вариант точнее всего объясняет <code class="i">const user = { name: "Аня" }</code>?',
    options: [
      { t: 'Нельзя присвоить новое значение имени user, но поле name можно изменить.', ok: true },
      { t: 'Все поля объекта автоматически становятся неизменяемыми.', ok: false },
      { t: 'Переменную user нельзя использовать внутри функции.', ok: false },
    ],
    hint: 'Разделите две операции: заменить значение переменной и изменить свойство объекта.',
    answer: `<h5>Что именно фиксирует const</h5><p><code class="i">const</code> не позволяет сделать <code class="i">user = другойОбъект</code>, потому что имя остаётся привязано к прежнему объекту. Сам объект не замораживается: <code class="i">user.name = 'Ира'</code> разрешено. Чтобы запретить изменения свойств, потребуются другие средства и подход; одного <code class="i">const</code> недостаточно.</p><p>В базовом коде используйте <code class="i">const</code>, если не переназначаете переменную, но помните, что массивы и объекты при этом всё ещё могут изменяться.</p>`,
  },
  {
    id: 'base-types-truthy', topic: 'База: типы и операторы', type: 'output', level: 'junior',
    q: 'Предскажите значения: пустой массив, строка "0", а затем результат операторов для числа 0.',
    code: `
console.log(Boolean([]), Boolean('0'))
console.log(0 ?? 9, 0 || 9)`,
    expected: 'true true\n0 9',
    hint: 'В JavaScript пустые массивы истинны. || проверяет falsy, а ?? только null и undefined.',
    answer: `<h5>Разберите каждое выражение отдельно</h5><p><code class="i">Boolean([])</code> даёт true: любой объект, включая пустой массив, считается истинным. Непустая строка <code class="i">'0'</code> тоже истинна — её содержимое не превращает её в число. Оператор <code class="i">??</code> сохраняет 0, потому что это заданное значение, а <code class="i">||</code> считает ноль ложным и подставляет 9.</p><p>Итого две строки: <code class="i">true true</code> и <code class="i">0 9</code>. Для допустимых нулевых значений настройку по умолчанию чаще задают через <code class="i">??</code>.</p>`,
  },
  {
    id: 'base-types-choice', topic: 'База: типы и операторы', type: 'choice', level: 'junior',
    q: 'Из API пришёл возраст строкой <code class="i">"18"</code>. Как безопаснее проверить, что он равен числу 18?',
    options: [
      { t: 'Явно преобразовать значение в число, проверить, что оно конечное, и сравнить с 18 через ===.', ok: true },
      { t: 'Использовать <code class="i">age == 18</code>, потому что == всегда надёжно исправляет вход.', ok: false },
      { t: 'Проверить только <code class="i">typeof age === "number"</code>, преобразовывать ничего не нужно.', ok: false },
    ],
    hint: 'Данные из сети часто строковые. Преобразуйте тип явно, затем проверьте, что получилось число.',
    answer: `<h5>Явная проверка проще для поддержки</h5><p><code class="i">Number('18')</code> преобразует строку в число 18; <code class="i">Number.isFinite</code> позволяет отвергнуть значения вроде NaN и Infinity. После этого строгое сравнение показывает и значение, и тип. Нестрогое <code class="i">==</code> скрывает преобразование, а строка с пробелами или другим содержимым может вести себя неожиданно.</p><p>На границе API полезно проверить и бизнес-диапазон: конечное число ещё не гарантирует допустимый возраст.</p>`,
  },
  {
    id: 'base-control-loop', topic: 'База: условия и циклы', type: 'output', level: 'junior',
    q: 'Какую сумму посчитает цикл, если прибавлять только чётные числа от 1 до 5?',
    code: `
let total = 0
for (let n = 1; n <= 5; n += 1) {
  if (n % 2 !== 0) continue
  total += n
}
console.log(total)`,
    expected: '6',
    hint: 'Выпишите числа по порядку. continue пропускает оставшуюся часть текущего прохода.',
    answer: `<h5>Пройдите цикл по значениям</h5><p>Цикл посещает 1, 2, 3, 4 и 5. У нечётных чисел остаток от деления на 2 не равен нулю, поэтому <code class="i">continue</code> сразу переводит цикл к следующему числу. К сумме прибавляются только 2 и 4, получается 6.</p><p>При отладке цикла удобно записать столбцы «n», «условие» и «total». Это помогает увидеть ошибку в границе или в том месте, где меняется счётчик.</p>`,
  },
  {
    id: 'base-control-choice', topic: 'База: условия и циклы', type: 'choice', level: 'junior',
    q: 'В списке нужно пропускать выключенных пользователей и остановиться на первом администраторе. Какие команды подходят?',
    options: [
      { t: '<code class="i">continue</code> для выключенного пользователя и <code class="i">break</code> после найденного администратора.', ok: true },
      { t: '<code class="i">break</code> для выключенного пользователя и <code class="i">continue</code> после администратора.', ok: false },
      { t: 'В обоих случаях нужен <code class="i">return</code>, он только начинает следующий проход.', ok: false },
    ],
    hint: 'Одна команда пропускает только текущий проход; другая завершает весь цикл.',
    answer: `<h5>Две команды — два масштаба действия</h5><p><code class="i">continue</code> заканчивает текущий круг и позволяет проверить следующего пользователя. <code class="i">break</code> полностью выходит из ближайшего цикла, поэтому его используют, когда искомый администратор уже найден. <code class="i">return</code> завершает функцию целиком, а не начинает следующий проход.</p><p>После цикла стоит проверить, найден ли администратор: если нет, не надо молча использовать неопределённый результат.</p>`,
  },
  {
    id: 'base-functions-return', topic: 'База: функции', type: 'output', level: 'junior',
    q: 'Что вернут две функции? Обратите внимание на фигурные скобки у стрелочной функции.',
    code: `
const double = n => n * 2
const alsoDouble = n => { n * 2 }
console.log(double(3), alsoDouble(3))`,
    expected: '6 undefined',
    hint: 'Одно выражение после стрелки возвращается само. Блок в фигурных скобках требует явного return.',
    answer: `<h5>Автоматический return есть только у выражения</h5><p>В <code class="i">n =&gt; n * 2</code> тело состоит из одного выражения, поэтому оно автоматически становится результатом функции. Вторая функция содержит блок в фигурных скобках, но в нём нет <code class="i">return</code>. Она выполняет умножение и выбрасывает его результат, а наружу возвращает undefined.</p><p>Если нужна блочная запись с результатом, напишите <code class="i">n =&gt; { return n * 2 }</code>. Это одна из самых частых ошибок при первом знакомстве со стрелочными функциями.</p>`,
  },
  {
    id: 'base-functions-manual', topic: 'База: функции', type: 'manual', level: 'junior',
    q: 'Опишите функцию <code class="i">calculateTotal(price, quantity)</code>: она должна вернуть сумму покупки. Что проверите, если цена или количество могут быть некорректными?',
    starter: 'Можно написать код или проговорить контракт: входы, проверки, результат.',
    hint: 'Сначала решите, какие входные значения допустимы. Не путайте печать в консоль и возврат значения.',
    solution: 'Функция принимает цену одной единицы и количество, проверяет, что оба значения являются конечными числами, цена неотрицательна, а количество — целое неотрицательное. Затем возвращает price * quantity. Я не печатаю результат внутри функции: return делает его доступным вызывающему коду. В реальном коде нужно решить, как сообщать об ошибке — исключением, результатом с признаком ошибки или валидацией на границе API.',
    answer: `<h5>Ожидаемый простой вариант</h5><pre class="code">function calculateTotal(price, quantity) {
  if (!Number.isFinite(price) || !Number.isInteger(quantity)) {
    throw new TypeError('Ожидаются цена и целое количество')
  }
  if (price < 0 || quantity < 0) {
    throw new RangeError('Значения не могут быть отрицательными')
  }
  return price * quantity
}</pre><p>Главное — функция возвращает значение, а не только вызывает <code class="i">console.log</code>. Проверки делают контракт явным. Для настоящих денежных расчётов нужно также выбрать точное представление валюты, а не полагаться на дробный number.</p>`,
  },
  {
    id: 'base-collections-copy', topic: 'База: массивы и объекты', type: 'output', level: 'junior',
    q: 'Что получится после поверхностного копирования массива и изменения вложенного объекта?',
    code: `
const first = [{ score: 1 }]
const copy = [...first]
copy[0].score = 2
const next = first.map(item => ({ score: item.score + 1 }))
console.log(first[0].score, copy === first, next[0].score)`,
    expected: '2 false 3',
    hint: 'Spread создал новый внешний массив, но вложенный объект пока тот же самый.',
    answer: `<h5>Копия массива неглубокая</h5><p>Spread создаёт новый массив, поэтому <code class="i">copy === first</code> возвращает false. Но элементом обоих массивов остаётся одна и та же ссылка на объект. Изменение через <code class="i">copy[0]</code> видно и из <code class="i">first[0]</code>, там score теперь равен 2.</p><p><code class="i">map</code> создаёт третий массив и в этой задаче также создаёт новый объект для элемента со значением 3. Вывод: <code class="i">2 false 3</code>. Вложенную копию приходится создавать явно, если её действительно нужно изолировать.</p>`,
  },
  {
    id: 'base-collections-choice', topic: 'База: массивы и объекты', type: 'choice', level: 'junior',
    q: 'Нужно превратить массив товаров в массив их названий, сохранив число элементов. Какой метод выбрать?',
    options: [
      { t: '<code class="i">map</code>: для каждого товара вернуть его name.', ok: true },
      { t: '<code class="i">filter</code>: он всегда преобразует поля каждого товара.', ok: false },
      { t: '<code class="i">forEach</code>: он автоматически возвращает новый массив.', ok: false },
    ],
    hint: 'Нужно получить новый элемент для каждого старого, а не убрать часть списка.',
    answer: `<h5>Назначение метода подсказывает выбор</h5><p><code class="i">map</code> вызывает функцию для каждого элемента и собирает возвращённые значения в новый массив той же длины. Для названий это будет <code class="i">products.map(product =&gt; product.name)</code>. <code class="i">filter</code> оставляет лишь элементы, прошедшие условие, а <code class="i">forEach</code> выполняет действия и возвращает undefined.</p><p>Если задача формулируется «преобразовать каждый», обычно это map; «оставить подходящие» — filter; «выполнить побочное действие» — forEach.</p>`,
  },
]
