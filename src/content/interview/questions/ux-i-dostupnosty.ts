import type { Question } from '@/engine/types'

/** UX и доступность — 3 вопрос(ов) */
export const uxIDostupnostyQuestions: Question[] = [
  { id:'ux-a11y', topic:'UX и доступность', type:'theory', level:'middle',
    q:'Что такое доступность (a11y) и как её обеспечить на практике? Разберите семантику, клавиатуру, ARIA и контраст.',
    code:`<!-- ❌ -->
  <div class="btn" onclick="submit()">Отправить</div>
  <div class="input"><div>Email</div><input type="text"></div>
  <img src="chart.png">
  
  <!-- ✅ -->
  <button type="submit">Отправить</button>
  <label for="email">Email</label>
  <input id="email" type="email" autocomplete="email" required>
  <img src="chart.png" alt="Рост выручки на 20% за квартал">`,
    answer:`<h5>Почему это спрашивают</h5>
  <p>Во-первых, это законодательное требование во многих странах и для госзаказа. Во-вторых, доступность коррелирует с качеством кода: семантичная разметка лучше индексируется поисковиками, проще тестируется и обычно означает, что разработчик понимает платформу, а не только React.</p>
  <h5>1. Семантика — фундамент</h5>
  <p>Нативные элементы дают <b>бесплатно</b>: фокус, обработку Enter/Space, роль для скринридера, состояние disabled, участие в форме. <code class="i">&lt;div onClick&gt;</code> не даёт ничего — придётся руками добавлять <code class="i">tabindex="0"</code>, <code class="i">role="button"</code>, обработчик клавиш. Правило: <b>первое правило ARIA — не использовать ARIA, если есть подходящий HTML-элемент</b>.</p>
  <p>Структура: <code class="i">header</code>, <code class="i">nav</code>, <code class="i">main</code> (один на страницу), <code class="i">article</code>, <code class="i">aside</code>, <code class="i">footer</code>; заголовки <code class="i">h1–h6</code> <b>без пропуска уровней</b>.</p>
  <h5>2. Клавиатура</h5>
  <ul>
  <li>Всё интерактивное достижимо по Tab, порядок логичный (не ломайте <code class="i">tabindex</code> положительными числами — только <code class="i">0</code> и <code class="i">-1</code>).</li>
  <li><b>Видимый фокус</b> обязателен. <code class="i">outline: none</code> без замены — худшее, что можно сделать. Современно: <code class="i">:focus-visible</code> — фокус виден при клавиатуре и не мешает при мыши.</li>
  <li>Модалка: фокус переводится внутрь, <b>запирается</b> (focus trap), Esc закрывает, после закрытия фокус возвращается на кнопку-открыватель.</li>
  <li>«Пропустить к содержимому» — скрытая ссылка первой в DOM.</li>
  </ul>
  <h5>3. ARIA — когда нативного не хватает</h5>
  <pre class="code">&lt;button aria-expanded="false" aria-controls="menu"&gt;Меню&lt;/button&gt;
  &lt;div id="menu" role="menu" hidden&gt;…&lt;/div&gt;
  
  &lt;div role="status" aria-live="polite"&gt;Сохранено&lt;/div&gt;      &lt;!-- не перебивает --&gt;
  &lt;div role="alert" aria-live="assertive"&gt;Ошибка&lt;/div&gt;         &lt;!-- перебивает --&gt;
  
  &lt;input aria-invalid="true" aria-describedby="err"&gt;
  &lt;span id="err"&gt;Неверный формат email&lt;/span&gt;
  
  &lt;button aria-label="Закрыть"&gt;&lt;IconX aria-hidden="true" /&gt;&lt;/button&gt;</pre>
  <p><code class="i">aria-live</code> критичен для SPA: скринридер не узнает о смене контента без перезагрузки страницы. Тосты, результаты поиска, ошибки формы должны объявляться.</p>
  <h5>4. Контраст и визуал</h5>
  <ul>
  <li>WCAG AA: <b>4.5:1</b> для обычного текста, <b>3:1</b> для крупного (от 18pt) и для границ элементов управления.</li>
  <li>Не передавайте смысл <b>только</b> цветом: к красной рамке добавьте текст ошибки и иконку (8% мужчин — дальтоники).</li>
  <li>Текст должен масштабироваться до 200% без поломки вёрстки → <code class="i">rem</code>, а не фиксированные <code class="i">px</code> для шрифтов.</li>
  <li><code class="i">prefers-reduced-motion</code> — уважать настройку: у части людей анимации вызывают тошноту.</li>
  </ul>
  <h5>Чем проверять</h5>
  <p>Lighthouse и axe DevTools (ловят ~30–50% проблем автоматически), <code class="i">eslint-plugin-jsx-a11y</code> на этапе разработки, <code class="i">jest-axe</code>/<code class="i">axe-playwright</code> в тестах. И главное — <b>ручная проверка</b>: пройдите форму только с клавиатуры и включите VoiceOver (Cmd+F5 на macOS). Это пятиминутное упражнение, которое на собесе звучит очень убедительно.</p>` },
  { id:'ux-states', topic:'UX и доступность', type:'theory', level:'middle',
    q:'Какие состояния должен предусматривать любой экран? Как правильно показывать загрузку, ошибки и пустоту?',
    answer:`<h5>Пять состояний любого экрана</h5>
  <ol>
  <li><b>Загрузка</b> — данные идут.</li>
  <li><b>Пусто</b> — запрос успешен, но данных нет.</li>
  <li><b>Ошибка</b> — не удалось загрузить.</li>
  <li><b>Частично</b> — часть данных есть, часть грузится или сломалась.</li>
  <li><b>Успех</b> — то единственное, что обычно рисует дизайнер.</li>
  </ol>
  <p>Назвать этот список на собесе — сильный ход: он показывает продуктовое мышление, а не только «сверстаю по макету».</p>
  <h5>Загрузка</h5>
  <ul>
  <li><b>Скелетоны вместо спиннеров</b>: сохраняют структуру, не дают скачка вёрстки (CLS) и субъективно ощущаются быстрее.</li>
  <li>Для действий &lt; 300 мс индикатор лучше не показывать вовсе — мелькание раздражает сильнее ожидания.</li>
  <li>При повторной загрузке (данные уже есть) не заменяйте контент скелетоном — покажите ненавязчивый индикатор. Это разница <code class="i">isPending</code> и <code class="i">isFetching</code> в React Query.</li>
  <li>Кнопку на время отправки — в <code class="i">disabled</code> + индикатор, иначе двойной сабмит и два заказа.</li>
  </ul>
  <h5>Пустое состояние</h5>
  <p>Не «Нет данных». Объясните <b>почему</b> пусто и дайте <b>следующий шаг</b>: «У вас пока нет задач. Создайте первую». Различайте «пусто вообще» и «пусто по фильтру» — во втором случае предложите сбросить фильтр.</p>
  <h5>Ошибки</h5>
  <ul>
  <li>Человеческим языком, без кодов и стектрейсов: не «Error 500», а «Не удалось загрузить задачи. Попробуйте ещё раз».</li>
  <li><b>Всегда давайте действие</b>: «Повторить», «Вернуться», «Написать в поддержку».</li>
  <li>Ошибки формы — <b>рядом с полем</b>, а не общим списком сверху; фокус на первое проблемное поле.</li>
  <li>Различайте: сеть отвалилась, сервер упал, нет прав, валидация не прошла. Разные ситуации — разные подсказки.</li>
  <li>Не теряйте введённые данные при ошибке отправки. Терять заполненную форму — непростительно.</li>
  </ul>
  <h5>Отзывчивость интерфейса — цифры, которые стоит знать</h5>
  <table>
  <tr><th>До 100 мс</th><th>До 300 мс</th><th>До 1 с</th><th>Больше 1 с</th></tr>
  <tr><td>ощущается мгновенным</td><td>заметная, но комфортная задержка</td><td>мысль не теряется, нужен индикатор</td><td>нужен прогресс и возможность отменить</td></tr>
  </table>
  <h5>Оптимистичный UI</h5>
  <p>Обновляем интерфейс сразу, откатываем при ошибке. Уместно для операций с высокой вероятностью успеха и низкой ценой отката: лайк, отметка «выполнено», удаление из списка. <b>Не применять</b> к платежам и необратимым действиям — там честное ожидание с подтверждением.</p>
  <div class="hint">Паттерн «Отменить» вместо «Вы уверены?»: вместо диалога подтверждения удалите сразу и покажите тост «Удалено — Отменить» на 5 секунд. Меньше трения в 99% случаев и безопасно в оставшемся 1%.</div>` },
  { id:'ux-forms', topic:'UX и доступность', type:'theory', level:'middle',
    q:'Как спроектировать хорошую форму? Когда валидировать, как показывать ошибки, что с мобильными?',
    code:`<input
    type="email"
    inputmode="email"
    autocomplete="email"
    name="email"
    aria-invalid={!!error}
    aria-describedby={error ? 'email-error' : undefined}
  />
  {error && <span id="email-error" role="alert">{error}</span>}`,
    answer:`<h5>Когда валидировать — главный вопрос</h5>
  <ul>
  <li><b>Не на каждое нажатие клавиши.</b> Ругать пользователя за «неполный email», пока он его печатает, — самая частая ошибка.</li>
  <li><b>По уходу с поля (onBlur)</b> — для первой проверки.</li>
  <li><b>По изменению (onChange)</b> — только <b>после</b> того, как ошибка уже показана: пользователь исправляет и сразу видит, что всё хорошо.</li>
  <li><b>По сабмиту</b> — обязательная финальная проверка, фокус на первое поле с ошибкой.</li>
  </ul>
  <p>Эта стратегия называется <code class="i">mode: 'onTouched'</code> в react-hook-form и считается дефолтом.</p>
  <h5>Правила подачи</h5>
  <ul>
  <li><b>Label всегда</b>, а не placeholder вместо него: placeholder исчезает при вводе, у него низкий контраст и его не читают скринридеры как подпись.</li>
  <li>Ошибка <b>под полем</b>, текстом + иконка + цвет (не только цвет).</li>
  <li>Требования к паролю показывайте <b>до</b> ввода, а не после неудачи.</li>
  <li>Одна колонка. Многоколоночные формы люди заполняют с ошибками.</li>
  <li>Минимум полей: каждое лишнее поле — минус к конверсии. Нужно ли отчество?</li>
  <li>Не запрещайте вставку из буфера в поле пароля — это вредит менеджерам паролей и безопасности.</li>
  </ul>
  <h5>Мобильные</h5>
  <ul>
  <li><code class="i">inputmode</code> — правильная клавиатура: <code class="i">numeric</code>, <code class="i">tel</code>, <code class="i">email</code>, <code class="i">decimal</code>, <code class="i">search</code>.</li>
  <li><code class="i">autocomplete</code> с правильным токеном (<code class="i">email</code>, <code class="i">new-password</code>, <code class="i">one-time-code</code> для СМС-кода, <code class="i">cc-number</code>) — автозаполнение резко ускоряет ввод.</li>
  <li><code class="i">font-size</code> у инпута не меньше 16px — иначе iOS зумит страницу при фокусе.</li>
  <li>Цель нажатия — не меньше 44×44 px.</li>
  </ul>
  <h5>Валидация: клиент и сервер</h5>
  <p>На клиенте — <b>ради UX</b> (мгновенная обратная связь). На сервере — <b>ради корректности</b>, всегда, без исключений. В монорепе схему валидации пишут один раз на zod и используют и там, и там; на клиенте её подключают в react-hook-form через resolver.</p>
  <div class="hint">Отдельный вопрос, который любят: <b>что происходит при двойном клике по «Отправить»</b>. Правильный ответ включает три уровня: <code class="i">disabled</code> на кнопке, защита от повторной мутации на клиенте и <b>Idempotency-Key на сервере</b> — потому что первые два обходятся.</div>` },
]
