import type { TheoryArticle } from '@/engine/types'

export const toolExcel: TheoryArticle = { id:'tool-excel', topic:'Инструменты', title:'Excel и данные: чем на самом деле занят ассистент',
lead:'Работа с выгрузками, контроль полноты данных, сверки, формулы, которые нужны ежедневно, и дисциплина работы с файлами в busy season.',
body:`
<h5>Первое правило работы с выгрузкой</h5>
<p>Любой файл от клиента сначала проверяется на <b>полноту и целостность</b>, и только потом анализируется. Иначе вы построите безупречный анализ на неполных данных.</p>
<ul>
<li>Итог выгрузки ЖХД должен сходиться с оборотами по ОСВ; итог ОСВ — с отчётностью.</li>
<li>Количество строк, диапазон дат, отсутствие скрытых строк и фильтров, отсутствие пустых ключевых полей.</li>
<li>Сумма дебетовых оборотов = сумма кредитовых оборотов (для ЖХД это встроенная проверка).</li>
<li>Файл сохраняется <b>в неизменном виде</b> отдельно, работа ведётся на копии — источник должен быть воспроизводим.</li>
</ul>

<h5>Формулы, без которых не обойтись</h5>
<table>
<tr><th>Задача</th><th>Инструмент</th></tr>
<tr><td>Подтянуть данные по ключу</td><td><code class="i">ВПР</code> / <code class="i">ПРОСМОТРX</code> (XLOOKUP), <code class="i">ИНДЕКС</code>+<code class="i">ПОИСКПОЗ</code> для нестандартных случаев</td></tr>
<tr><td>Суммировать по условиям</td><td><code class="i">СУММЕСЛИМН</code>, <code class="i">СЧЁТЕСЛИМН</code></td></tr>
<tr><td>Свернуть данные по аналитике</td><td>Сводная таблица — основной инструмент анализа ЖХД</td></tr>
<tr><td>Найти расхождения двух реестров</td><td><code class="i">ПРОСМОТРX</code> в обе стороны: что есть у нас и нет у них, и наоборот</td></tr>
<tr><td>Собрать данные из нескольких файлов</td><td>Power Query — если освоить, экономит недели за сезон</td></tr>
<tr><td>Выборка</td><td><code class="i">СЛЧИС</code> / <code class="i">СЛУЧМЕЖДУ</code> с фиксацией значений, либо систематический отбор через интервал (нужна воспроизводимость!)</td></tr>
<tr><td>Старение задолженности</td><td><code class="i">ЕСЛИМН</code> по количеству дней просрочки + сводная</td></tr>
</table>
<div class="trap"><b>Выборка должна быть воспроизводимой.</b> Если вы отобрали элементы функцией <code class="i">СЛЧИС</code> и не зафиксировали результат, при следующем пересчёте файла выборка изменится — и рабочий документ перестанет соответствовать выполненной работе. Фиксируйте значения и сохраняйте параметры отбора (seed, интервал, стартовую точку).</div>

<h5>Сверка двух реестров</h5>
<div data-demo="excel-recon"></div>

<h5>Дисциплина файлов</h5>
<ul>
<li><b>Имя файла</b>: участок, период, версия, дата. Никаких «Книга1_финал_финал2».</li>
<li><b>Отдельный лист с источником</b>: откуда файл, от кого, когда получен, как проверена полнота.</li>
<li><b>Все формулы видны</b>: не вбивайте числа руками поверх формул, проверяющий должен видеть расчёт.</li>
<li><b>Легенда тикмарков</b> — на том же листе, где они проставлены.</li>
<li><b>Нет «мёртвых» ссылок</b> на файлы с вашего рабочего стола: рабочий документ должен открываться у любого.</li>
</ul>

<h5>Busy season: как не сгореть</h5>
<p>Январь — апрель — пик нагрузки. Что реально помогает:</p>
<ul>
<li><b>Сначала запросы, потом работа.</b> Всё, что требует ответа клиента или третьей стороны (подтверждения, юристы, банки), отправляется в первый же день — это самые долгие цепочки.</li>
<li><b>Один список открытых вопросов</b> по проекту, а не в почте и мессенджерах.</li>
<li><b>Не копить непонятное.</b> Вопрос, заданный менеджеру через час, стоит 10 минут; через неделю — двух дней переделки.</li>
<li><b>Считать время по задачам</b>: вы быстро поймёте, где ваши реальные потери, и это же данные для разговора о бюджете следующего года.</li>
<li><b>Фиксировать выводы сразу</b>, пока процедура в голове. Писать рабочий документ через две недели — вдвое дольше.</li>
</ul>` }
