/* eslint-disable */
// @ts-nocheck — императивное демо, перенесено как есть; типизируется при переписывании в React
import type { LegacyDemo } from '@/engine/types'
import * as H from '@/demos/helpers'
const { dEl, dBtn, dShell, dField, dSelect, dRange, kpi, fmt, money, num, pct, th,
        barsH, barsV, lineC, stackRow, chartBox, legend, sv, mkSort, mkMatch, mkSteps,
        mkTree, calcShell, bindAll, ACC, ACC_KEYS, accLabel } = H as any

export const p2pWalk: LegacyDemo = root => {
  const N = [
    { n:'Заявка на закупку', ctl:'Заявка согласуется руководителем подразделения в пределах лимита и проверяется на соответствие бюджету.',
      risk:'Закупка без потребности; обход бюджета дроблением заявок на суммы ниже лимита согласования.',
      test:'Выборка заявок: наличие согласования нужного уровня, сопоставление с бюджетом, анализ заявок с суммами чуть ниже порогов.' },
    { n:'Выбор поставщика и заказ', ctl:'Тендерная процедура или конкурентный выбор из трёх предложений; поставщик заведён в справочник отдельной функцией.',
      risk:'Аффилированный поставщик, завышенные цены, откат; фиктивный поставщик в справочнике.',
      test:'Проверка процедуры выбора, анализ новых поставщиков за год, сверка реквизитов с ЕГРЮЛ, поиск совпадений адресов и учредителей с сотрудниками.' },
    { n:'Приёмка товара', ctl:'Приёмку осуществляет склад, независимый от закупок; оформляется приходный ордер с фиксацией количества и качества.',
      risk:'Оплата за товар, который не поступал; приёмка без проверки количества.',
      test:'Сверка приходных документов с заказом и счётом; для выборки — сверка с карточками складского учёта.' },
    { n:'Получение счёта и трёхстороннее сопоставление', ctl:'Бухгалтерия сверяет счёт, заказ и приходный ордер (3-way match); расхождения блокируют оплату.',
      risk:'Оплата по счёту без поставки; двойная оплата одного счёта; неверная сумма или ставка НДС.',
      test:'Повторное выполнение сопоставления по выборке; поиск дублей по номеру и сумме счёта в реестре платежей.' },
    { n:'Оплата', ctl:'Платёж подписывают два уполномоченных лица; реквизиты берутся из карточки поставщика, изменение реквизитов требует отдельного согласования.',
      risk:'Платёж на подменённые реквизиты; платёж без документов-оснований.',
      test:'Выборка платежей: наличие подписей, сверка реквизитов с договором, проверка изменений реквизитов в справочнике за период.' },
    { n:'Отражение в учёте', ctl:'Проводка формируется системой автоматически на основании документа; ручные корректировки ограничены и логируются.',
      risk:'Неверный счёт затрат; отнесение не в тот период (cut-off); ручная проводка в обход процесса.',
      test:'Тестирование журнальных проводок по участку: ручные записи, нетипичные корреспонденции, операции последних дней периода.' },
  ];
  mkSteps(root, 'Walkthrough процесса «Закупка → оплата»', {
    steps: N.map(x => ({ ...x, note:'Walkthrough — это не тест контроля. Одна операция, прогнанная целиком, подтверждает, что описание процесса в файле соответствует реальности.' })),
    render(panel, s, i){
      const chain = dEl('div', 'sort-b');
      N.forEach((x, j) => {
        const b = dEl('button', j === i ? 'pick' : null, (j + 1) + '. ' + x.n);
        b.type = 'button'; b.style.pointerEvents = 'none';
        chain.appendChild(b);
      });
      panel.appendChild(chain);
      const k = dEl('div', 'kpis');
      kpi(k, 'Контроль', s.ctl, '', 'acc');
      kpi(k, 'Риск, если контроля нет', s.risk, '', 'red');
      kpi(k, 'Как тестируем', s.test, '', 'grn');
      panel.appendChild(k);
    }
  });
}
