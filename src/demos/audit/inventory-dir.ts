/* eslint-disable */
// @ts-nocheck — императивное демо, перенесено как есть; типизируется при переписывании в React
import type { LegacyDemo } from '@/engine/types'
import * as H from '@/demos/helpers'
const { dEl, dBtn, dShell, dField, dSelect, dRange, kpi, fmt, money, num, pct, th,
        barsH, barsV, lineC, stackRow, chartBox, legend, sv, mkSort, mkMatch, mkSteps,
        mkTree, calcShell, bindAll, ACC, ACC_KEYS, accLabel } = H as any

export const inventoryDir: LegacyDemo = root => mkSort(root, 'Контрольный пересчёт: куда идти от чего', {
  intro: 'На инвентаризации аудитор всегда делает два теста. Определите, какую предпосылку закрывает каждое действие.',
  bins: [
    { id:'ex', label:'Существование' },
    { id:'co', label:'Полнота' },
  ],
  items: [
    { q:'Взяли позиции из инвентаризационной описи клиента → пошли на склад и пересчитали их физически.', bin:'ex',
      why:'от описи к полке (sheet-to-floor): проверяем, что учтённые запасы реально есть.' },
    { q:'Выбрали паллеты на складе → проверили, что они попали в опись с верным количеством.', bin:'co',
      why:'от полки к описи (floor-to-sheet): проверяем, что всё имеющееся учтено.' },
    { q:'Записали номера последних приходных и расходных документов на момент пересчёта.', bin:'co',
      why:'фиксация cut-off: далее проверяем, что операции до и после этих номеров попали в правильный период — предпосылка полноты и отнесения к периоду.' },
    { q:'Отметили зону с повреждёнными и пыльными коробками, простоявшими, по словам кладовщика, больше года.', bin:'ex',
      why:'формально позиции существуют, но наблюдение даёт вход в тест оценки: запасы могут требовать обесценения до чистой стоимости продажи.' },
  ] })
